---
phase: 03-implementacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/br/com/escola/matricula/ErpMatriculaApplication.java
  - src/main/resources/application.yml
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - DOM-04
  - DOM-05
  - DOM-06
  - DOM-07
  - DOM-08
  - DOM-09
  - DOM-10

must_haves:
  truths:
    - "O projeto compila com `mvn compile` sem erros"
    - "A classe principal `ErpMatriculaApplication` está no pacote raiz `br.com.escola.matricula`"
    - "`application.yml` configura datasource para `localhost:5432/erp_matricula`, Flyway habilitado, MyBatis com mapper-locations e type-aliases-package"
    - "O `pom.xml` declara todas as dependências da Fase 3 sem `spring-boot-starter-web`"
  artifacts:
    - path: "pom.xml"
      provides: "Projeto Maven com groupId=br.com.escola, artifactId=erp-matricula, Java 21, Spring Boot 3.5.3"
      contains: "mybatis-spring-boot-starter"
    - path: "src/main/java/br/com/escola/matricula/ErpMatriculaApplication.java"
      provides: "Main class com @SpringBootApplication + @MapperScan"
      exports: ["ErpMatriculaApplication"]
    - path: "src/main/resources/application.yml"
      provides: "Configuração de datasource, Flyway, MyBatis e logging"
      contains: "erp_matricula"
  key_links:
    - from: "ErpMatriculaApplication.java"
      to: "br.com.escola.matricula.infraestrutura.persistencia"
      via: "@MapperScan"
      pattern: "@MapperScan"
    - from: "application.yml"
      to: "src/main/resources/mapper/"
      via: "mybatis.mapper-locations"
      pattern: "mapper-locations"
---

<objective>
Criar a estrutura Maven do projeto e o bootstrap Spring Boot.

Propósito: Estabelecer o esqueleto de build (pom.xml com todas as dependências corretas) e a classe principal que inicializa o contexto Spring. Sem esta fundação, nenhuma das camadas subsequentes compila.

Saídas: pom.xml funcional, ErpMatriculaApplication.java, application.yml completo para Fase 3, e toda a estrutura de diretórios src/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-implementacao/03-CONTEXT.md
@.planning/phases/03-implementacao/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar pom.xml com todas as dependências da Fase 3</name>
  <files>pom.xml</files>

  <read_first>
    - .planning/phases/03-implementacao/03-CONTEXT.md (seção Bootstrap — D-06, D-07, D-08)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 4 — Flyway Migrations, nota crítica flyway-database-postgresql; seção 5 — application.yml completo)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção Don't Hand-Roll e Pitfalls 3, 4)
    - CLAUDE.md (seção "Bibliotecas — Versões Verificadas")
  </read_first>

  <action>
Criar o arquivo pom.xml na raiz do projeto com as seguintes especificações exatas (per D-06, D-07):

groupId: br.com.escola
artifactId: erp-matricula
version: 0.1.0-SNAPSHOT
packaging: jar
Java source/target: 21

Parent: spring-boot-starter-parent 3.5.3

Dependências obrigatórias (per D-07):
1. spring-boot-starter (core sem Tomcat — NÃO spring-boot-starter-web)
2. mybatis-spring-boot-starter versão 3.0.5 (groupId org.mybatis.spring.boot) — versão EXPLÍCITA, não gerenciada pelo BOM
3. postgresql (groupId org.postgresql, versão gerenciada pelo Spring Boot BOM — sem versão explícita)
4. flyway-core (groupId org.flywaydb, versão gerenciada pelo Spring Boot BOM)
5. flyway-database-postgresql (groupId org.flywaydb, versão gerenciada pelo Spring Boot BOM) — OBRIGATÓRIO para Flyway 10+ com PostgreSQL; sem esta dependência o Flyway falha ao conectar no PostgreSQL (Pitfall 3 do RESEARCH.md)
6. spring-boot-starter-test (scope test)

NÃO incluir: lombok, mapstruct, spring-boot-starter-web, spring-data-jpa, hibernate, spring-boot-starter-data-jpa.

Plugin obrigatório: spring-boot-maven-plugin (para `mvn spring-boot:run`).

Propriedades no pom.xml:
- java.version=21
- project.build.sourceEncoding=UTF-8
- project.reporting.outputEncoding=UTF-8

O arquivo deve ter um comentário de cabeçalho indicando que não inclui spring-boot-starter-web intencionalmente (demonstração DDD independente de HTTP — per D-07).
  </action>

  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && mvn validate -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
- pom.xml existe na raiz do projeto
- grep "spring-boot-starter-web" pom.xml retorna vazio (não incluído)
- grep "mybatis-spring-boot-starter" pom.xml retorna linha com versão 3.0.5
- grep "flyway-database-postgresql" pom.xml retorna resultado (dependência crítica presente)
- grep "lombok\|mapstruct\|spring-data-jpa\|hibernate" pom.xml retorna vazio
- mvn validate -q encerra com BUILD SUCCESS
  </acceptance_criteria>

  <done>pom.xml válido, mvn validate retorna BUILD SUCCESS, sem dependências proibidas</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar estrutura de diretórios, ErpMatriculaApplication.java e application.yml</name>
  <files>
    src/main/java/br/com/escola/matricula/ErpMatriculaApplication.java
    src/main/resources/application.yml
    src/main/resources/db/migration/.gitkeep
    src/main/resources/mapper/.gitkeep
    src/test/java/br/com/escola/matricula/.gitkeep
  </files>

  <read_first>
    - .planning/phases/03-implementacao/03-CONTEXT.md (D-01..D-08 — pacotes, bootstrap, application.yml)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 5 — Spring Boot sem Web, application.yml completo; seção Pitfall 4 e Pitfall 5)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 9 — MyBatisConfig e @MapperScan)
  </read_first>

  <action>
Criar a estrutura de diretórios e os arquivos de bootstrap per D-01..D-08.

1. Criar todos os diretórios necessários de uma vez:
   src/main/java/br/com/escola/matricula/dominio/modelo/
   src/main/java/br/com/escola/matricula/dominio/vo/
   src/main/java/br/com/escola/matricula/dominio/evento/
   src/main/java/br/com/escola/matricula/dominio/repositorio/
   src/main/java/br/com/escola/matricula/dominio/servico/
   src/main/java/br/com/escola/matricula/dominio/excecao/
   src/main/java/br/com/escola/matricula/aplicacao/
   src/main/java/br/com/escola/matricula/infraestrutura/persistencia/typehandler/
   src/main/java/br/com/escola/matricula/infraestrutura/eventos/
   src/main/java/br/com/escola/matricula/infraestrutura/config/
   src/main/resources/db/migration/
   src/main/resources/mapper/
   src/test/java/br/com/escola/matricula/

2. Criar ErpMatriculaApplication.java em src/main/java/br/com/escola/matricula/:
   Pacote: br.com.escola.matricula
   Anotações: @SpringBootApplication + @MapperScan("br.com.escola.matricula.infraestrutura.persistencia")
   Método main padrão: SpringApplication.run(ErpMatriculaApplication.class, args)
   Importações: org.mybatis.spring.annotation.MapperScan, org.springframework.boot.SpringApplication, org.springframework.boot.autoconfigure.SpringBootApplication
   Comentário Javadoc explicando que @MapperScan registra automaticamente os Mappers MyBatis — sem necessidade de configuração manual de SqlSessionFactory (per RESEARCH.md — Don't Hand-Roll).

3. Criar application.yml em src/main/resources/ com o conteúdo exato do RESEARCH.md seção 5:
   spring.datasource.url: jdbc:postgresql://localhost:5432/erp_matricula
   spring.datasource.username: matricula
   spring.datasource.password: matricula
   spring.datasource.driver-class-name: org.postgresql.Driver
   spring.flyway.enabled: true
   spring.flyway.locations: classpath:db/migration
   spring.main.web-application-type: none
   mybatis.mapper-locations: classpath:mapper/**/*.xml
   mybatis.type-aliases-package: br.com.escola.matricula.infraestrutura.persistencia
   mybatis.type-handlers-package: br.com.escola.matricula.infraestrutura.persistencia.typehandler
   mybatis.configuration.map-underscore-to-camel-case: false (per RESEARCH.md Pitfall 10 — aliases explícitos no SQL, sem auto-camelCase)
   mybatis.configuration.lazy-loading-enabled: false
   logging.level.br.com.escola.matricula: DEBUG
   logging.level.org.mybatis: DEBUG

   Adicionar comentários em cada seção explicando o propósito pedagógico (ex: "web-application-type: none — DDD não depende de HTTP, fase 3 demonstra isso").
  </action>

  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && ls src/main/java/br/com/escola/matricula/ && ls src/main/resources/ && grep "web-application-type" src/main/resources/application.yml</automated>
  </verify>

  <acceptance_criteria>
- ErpMatriculaApplication.java existe no caminho correto
- grep "@MapperScan" src/main/java/br/com/escola/matricula/ErpMatriculaApplication.java retorna resultado
- grep "web-application-type" src/main/resources/application.yml retorna "none"
- grep "map-underscore-to-camel-case" src/main/resources/application.yml retorna "false"
- grep "type-handlers-package" src/main/resources/application.yml retorna resultado
- Diretórios dominio/modelo/, dominio/vo/, dominio/evento/, dominio/repositorio/, dominio/servico/, dominio/excecao/, aplicacao/, infraestrutura/persistencia/, infraestrutura/persistencia/typehandler/, infraestrutura/eventos/, infraestrutura/config/ existem
- mvn compile -q retorna BUILD SUCCESS (sem classes .java além da main — compilação vazia é sucesso)
  </acceptance_criteria>

  <done>Estrutura de diretórios completa, ErpMatriculaApplication.java compilável, application.yml com configuração completa para Fase 3</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application.yml → banco de dados | Credenciais de desenvolvimento em texto plano no arquivo de configuração |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | application.yml (username/password) | accept | Credenciais são de desenvolvimento local (matricula/matricula), sem PII, sem acesso a produção. Fase 4 introduzirá variáveis de ambiente via Docker Compose. |
| T-03-SC | Tampering | pom.xml (flyway-database-postgresql, mybatis-spring-boot-starter) | mitigate | Versões fixadas no pom.xml com groupId verificados; Spring Boot BOM gerencia versões transitivas. Verificar que groupId é org.flywaydb e org.mybatis.spring.boot conforme STACK.md. |
</threat_model>

<verification>
Verificações gerais do plano:

1. mvn validate -q → BUILD SUCCESS
2. mvn compile -q → BUILD SUCCESS (estrutura de diretórios criada, main class compila)
3. grep -r "spring-boot-starter-web" pom.xml → vazio
4. grep "flyway-database-postgresql" pom.xml → presente
5. Estrutura de pacotes criada conforme D-01..D-04
</verification>

<success_criteria>
- pom.xml válido com Java 21, Spring Boot 3.5.3, mybatis-spring-boot-starter:3.0.5, flyway-core, flyway-database-postgresql, sem spring-boot-starter-web
- ErpMatriculaApplication.java no pacote br.com.escola.matricula com @SpringBootApplication + @MapperScan
- application.yml configurado com datasource erp_matricula, Flyway, MyBatis (mapper-locations, type-aliases-package, type-handlers-package, map-underscore-to-camel-case: false), logging DEBUG, web-application-type: none
- Toda a árvore de pacotes criada (dominio/*, aplicacao/, infraestrutura/*)
- mvn compile -q → BUILD SUCCESS
</success_criteria>

<output>
Criar `.planning/phases/03-implementacao/03-01-SUMMARY.md` ao finalizar
</output>
