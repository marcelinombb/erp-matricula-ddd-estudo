---
phase: 03-implementacao
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - src/main/resources/db/migration/V1__schema.sql
  - src/main/resources/db/migration/V2__seeds.sql
  - src/main/resources/mapper/MatriculaMapper.xml
  - src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRow.java
  - src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java
  - src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaMapper.java
  - src/main/java/br/com/escola/matricula/infraestrutura/persistencia/typehandler/CpfTypeHandler.java
  - src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java
  - src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRepositorioMyBatis.java
  - src/main/java/br/com/escola/matricula/infraestrutura/eventos/FinanceiroEventListener.java
  - src/main/java/br/com/escola/matricula/infraestrutura/eventos/AcademicoEventListener.java
  - src/main/java/br/com/escola/matricula/infraestrutura/config/MyBatisConfig.java
  - src/main/java/br/com/escola/matricula/infraestrutura/config/DomainServicesConfig.java
  - src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java
autonomous: true
requirements:
  - INF-01
  - INF-02
  - INF-03
  - INF-04
  - INF-05
  - INF-06
  - INF-07
  - APL-05

must_haves:
  truths:
    - "mvn spring-boot:run aplica 2 migrations Flyway e encerra sem erro (com PostgreSQL rodando)"
    - "MatriculaRow não tem métodos de negócio — apenas campos públicos ou getters simples"
    - "MatriculaMapper.xml usa notNullColumn='item_disciplina' na collection"
    - "Todos os parâmetros UUID no XML usam jdbcType=OTHER"
    - "FinanceiroEventListener e AcademicoEventListener têm @TransactionalEventListener"
    - "VerificadorElegibilidadeMatricula é criado via @Bean em DomainServicesConfig injetando MatriculaRepositorio"
    - "DemoRunner executa os três fluxos com os UUIDs dos seeds e imprime resultado no console"
  artifacts:
    - path: "src/main/resources/db/migration/V1__schema.sql"
      provides: "Schema PostgreSQL completo com 4 tabelas"
      contains: "CREATE TABLE matriculas"
    - path: "src/main/resources/mapper/MatriculaMapper.xml"
      provides: "ResultMap com JOIN + collection notNullColumn para reconstrução do Aggregate"
      contains: "notNullColumn"
    - path: "src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java"
      provides: "Conversão explícita MatriculaRow <-> Matricula"
      exports: ["toDomain", "fromDomain", "itemsFromDomain"]
    - path: "src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRepositorioMyBatis.java"
      provides: "Implementação de MatriculaRepositorio com estratégia replace-all"
      contains: "implements MatriculaRepositorio"
  key_links:
    - from: "MatriculaRepositorioMyBatis.java"
      to: "MatriculaRepositorio.java (domínio)"
      via: "implements"
      pattern: "implements MatriculaRepositorio"
    - from: "MatriculaMapper.xml"
      to: "MatriculaMapper.java"
      via: "namespace"
      pattern: "namespace=\"br.com.escola.matricula.infraestrutura.persistencia.MatriculaMapper\""
    - from: "MatriculaRowMapper.java"
      to: "MatriculaRow.java"
      via: "conversão domínio ↔ persistência"
      pattern: "MatriculaRow"
    - from: "FinanceiroEventListener.java"
      to: "AlunoMatriculado (evento de domínio)"
      via: "@TransactionalEventListener"
      pattern: "AlunoMatriculado"
---

<objective>
Implementar a camada de infraestrutura completa: schema PostgreSQL, Flyway migrations e seeds, MyBatis mapper XML, TypeHandlers, Row Objects, MatriculaRowMapper (conversão explícita), MatriculaRepositorioMyBatis, listeners stub, e o DemoRunner que executa os três fluxos.

Propósito: Conectar o domínio puro ao banco PostgreSQL sem que o domínio "saiba" sobre a persistência. A separação explícita via MatriculaRow/MatriculaRowMapper é o ponto pedagógico central do projeto — o aluno lê MatriculaRowMapper.java e vê exatamente onde domínio e persistência se separam.

Saídas: 14 arquivos de infraestrutura que completam o projeto — ao executar `mvn spring-boot:run` com PostgreSQL disponível, os três fluxos de negócio são executados com dados reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-implementacao/03-CONTEXT.md
@.planning/phases/03-implementacao/03-RESEARCH.md
@docs/adrs/ADR-001-mybatis-vs-jpa.md
@docs/adrs/ADR-003-referencia-por-id.md

<interfaces>
Da camada de domínio (03-02) e aplicação (03-03):

br.com.escola.matricula.dominio.repositorio.MatriculaRepositorio:
  Optional<Matricula> buscarPorId(MatriculaId id)
  List<Matricula> buscarPorAluno(AlunoId alunoId)
  boolean existeMatriculaAtiva(AlunoId alunoId, PeriodoLetivo periodo)
  void salvar(Matricula matricula)

br.com.escola.matricula.dominio.vo.PeriodoLetivo:
  record PeriodoLetivo(int ano, int semestre)
  Conversão para LocalDate: semestre 1 = inicio fev dia 1, fim jul dia 31
                             semestre 2 = inicio ago dia 1, fim dez dia 31

br.com.escola.matricula.dominio.modelo.StatusMatricula:
  sealed interface, permits Ativa, Cancelada, Concluida
  -- Ativa: String "ATIVA", Cancelada: "CANCELADA" + LocalDateTime, Concluida: "CONCLUIDA" + LocalDateTime

br.com.escola.matricula.aplicacao.MatricularAlunoUseCase:
  MatriculaId executar(MatricularAlunoCommand command)

br.com.escola.matricula.aplicacao.AdicionarDisciplinaUseCase:
  void executar(AdicionarDisciplinaCommand command)

br.com.escola.matricula.aplicacao.CancelarMatriculaUseCase:
  void executar(CancelarMatriculaCommand command)

Seeds UUID fixos (do RESEARCH.md seção 4):
  Aluno 1 (para matricular):  a0000000-0000-0000-0000-000000000001 / CPF 12345678901 / Maria Silva
  Aluno 2 (com matrícula):    a0000000-0000-0000-0000-000000000002 / CPF 98765432100 / João Santos
  Turma:                      b0000000-0000-0000-0000-000000000001 / Turma 2026-1 / 2026-02-01 a 2026-07-31
  Matrícula pré-existente:    c0000000-0000-0000-0000-000000000001 / João Santos / Turma 2026-1 / Matemática Básica
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Schema SQL, Seeds Flyway, MatriculaMapper.xml, Row Objects e MatriculaMapper interface</name>
  <files>
    src/main/resources/db/migration/V1__schema.sql
    src/main/resources/db/migration/V2__seeds.sql
    src/main/resources/mapper/MatriculaMapper.xml
    src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRow.java
    src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java
    src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaMapper.java
  </files>

  <read_first>
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 3 — Schema PostgreSQL completo com comentários pedagógicos; seção 4 — Flyway migrations V1 e V2 com seeds exatos)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 1 — ResultMap XML COMPLETO com todos os selects e inserts; Pitfall 1 — notNullColumn; Pitfall 2 — jdbcType=OTHER)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 9 — MatriculaRow, ItemMatriculaRow, MatriculaMapper interface)
    - docs/adrs/ADR-001-mybatis-vs-jpa.md (decisão MyBatis — referência para comentários no código)
    - docs/adrs/ADR-003-referencia-por-id.md (decisão sem FK — referência para comentários no schema)
    - .planning/phases/03-implementacao/03-CONTEXT.md (D-12 replace-all; D-13 MatriculaRow sem negócio; D-14 PeriodoLetivo para colunas DATE)
  </read_first>

  <action>
Criar o schema SQL, seeds e os artefatos MyBatis de infraestrutura de persistência.

V1__schema.sql — src/main/resources/db/migration/:
  Usar o schema EXATO da seção 3 do RESEARCH.md. Não inventar variações.
  Incluir TODOS os comentários SQL (COMMENT ON TABLE/COLUMN) que explicam a relação com o modelo de domínio.
  Decisões de schema a preservar com comentários:
  - aluno_id e turma_id SEM FOREIGN KEY (ADR-003 — referência por ID entre Aggregates)
  - itens_matricula COM FK para matriculas com ON DELETE CASCADE (entidade interna — ciclo de vida acoplado)
  - Unique index condicional WHERE status = 'ATIVA' (aluno pode ter matrículas canceladas no mesmo período)
  - status como VARCHAR(20) CHECK (não enum PostgreSQL — mais portável)

V2__seeds.sql — src/main/resources/db/migration/:
  Seeds com UUIDs fixos exatamente como na seção 4 do RESEARCH.md:
  - Aluno 1: a0000000-0000-0000-0000-000000000001 / CPF 12345678901 / Maria Silva / ativo=true
  - Aluno 2: a0000000-0000-0000-0000-000000000002 / CPF 98765432100 / João Santos / ativo=true
  - Turma 1: b0000000-0000-0000-0000-000000000001 / Turma 2026-1 / 2026-02-01 a 2026-07-31 / 30 vagas
  - Matrícula: c0000000-0000-0000-0000-000000000001 / aluno João / Turma 2026-1 / ATIVA
  - Item: c0000000-0000-0000-0000-000000000001 / disciplina "Matemática Básica"
  Comentários no SQL indicando qual fluxo cada seed habilita (matricular, adicionar, cancelar).

MatriculaMapper.xml — src/main/resources/mapper/:
  Usar o XML EXATO da seção 1 do RESEARCH.md. Não simplificar.
  Namespace: br.com.escola.matricula.infraestrutura.persistencia.MatriculaMapper
  ResultMap id="MatriculaResultMap" com:
  - id property="id" column="matricula_id"
  - result para todos os campos de MatriculaRow
  - collection property="itens" ofType="ItemMatriculaRow" notNullColumn="item_disciplina"
  CRÍTICO: notNullColumn="item_disciplina" é OBRIGATÓRIO (Pitfall 1 do RESEARCH.md — sem ele, matrículas sem itens ganham elemento fantasma na lista)
  Queries: buscarPorId, buscarPorAluno, existeMatriculaAtiva, inserirMatricula, atualizarMatricula, deletarItensPorMatriculaId, inserirItens
  CRÍTICO: Todos os parâmetros UUID devem usar #{param, jdbcType=OTHER} (Pitfall 2 — PostgreSQL UUID nativo)
  Comentários XML explicando cada decisão técnica (notNullColumn, jdbcType=OTHER, LEFT JOIN).

MatriculaRow.java — pacote br.com.escola.matricula.infraestrutura.persistencia:
  Classe simples (não record) com campos públicos — espelha exatamente as colunas da tabela matriculas.
  Campos: public UUID id; public UUID alunoId; public UUID turmaId; public LocalDate periodoInicio; public LocalDate periodoFim; public String status; public LocalDateTime canceladaEm; public LocalDateTime concluidaEm; public List<ItemMatriculaRow> itens = new ArrayList<>();
  SEM MÉTODOS DE NEGÓCIO — apenas campos. Esta é a regra de INF-06 e o ponto pedagógico: o contraste com Matricula.java (que tem adicionarDisciplina, cancelar, coletarEventos) é explícito.
  Javadoc: "Modelo relacional da tabela 'matriculas'. Sem lógica de negócio — compare com Matricula.java para ver a separação domínio/persistência (ADR-001, INF-06). Conversão realizada por MatriculaRowMapper."

ItemMatriculaRow.java — pacote br.com.escola.matricula.infraestrutura.persistencia:
  Campos: public UUID matriculaId; public String disciplina;
  SEM lógica de negócio.

MatriculaMapper.java — interface MyBatis no pacote br.com.escola.matricula.infraestrutura.persistencia:
  @Mapper annotation (org.apache.ibatis.annotations.Mapper)
  Assinaturas exatas da seção 9 do RESEARCH.md:
  MatriculaRow buscarPorId(@Param("id") UUID id);
  List<MatriculaRow> buscarPorAluno(@Param("alunoId") UUID alunoId);
  boolean existeMatriculaAtiva(@Param("alunoId") UUID alunoId, @Param("periodoInicio") LocalDate periodoInicio, @Param("periodoFim") LocalDate periodoFim);
  int inserirMatricula(MatriculaRow row);
  int atualizarMatricula(MatriculaRow row);
  int deletarItensPorMatriculaId(@Param("matriculaId") UUID matriculaId);
  int inserirItens(@Param("list") List<ItemMatriculaRow> itens);
  Javadoc na interface: "Mapper MyBatis para Matricula. Métodos mapeados para MatriculaMapper.xml. Não usar diretamente — usar MatriculaRepositorioMyBatis que implementa a interface de domínio."
  </action>

  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && grep "notNullColumn" src/main/resources/mapper/MatriculaMapper.xml && grep -c "jdbcType=OTHER" src/main/resources/mapper/MatriculaMapper.xml && mvn compile -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
- grep "notNullColumn=\"item_disciplina\"" src/main/resources/mapper/MatriculaMapper.xml retorna resultado (CRÍTICO — sem isso lista fantasma)
- grep -c "jdbcType=OTHER" src/main/resources/mapper/MatriculaMapper.xml retorna >= 5 (todos os parâmetros UUID usam jdbcType=OTHER)
- grep "implements\|extends\|public.*void\|public.*boolean\|public.*String\|public.*int\|public.*List" src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRow.java retorna VAZIO (sem métodos de negócio — somente campos públicos)
- grep "@Mapper" src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaMapper.java retorna resultado
- V1__schema.sql contém CREATE TABLE matriculas, alunos, turmas, itens_matricula
- V2__seeds.sql contém os 5 INSERT com UUIDs fixos
- mvn compile -q retorna BUILD SUCCESS
  </acceptance_criteria>

  <done>Schema SQL, seeds, MatriculaMapper.xml (com notNullColumn e jdbcType=OTHER), Row Objects e MatriculaMapper interface criados e compilando</done>
</task>

<task type="auto">
  <name>Tarefa 2: TypeHandler, MatriculaRowMapper, MatriculaRepositorioMyBatis, Listeners e DemoRunner</name>
  <files>
    src/main/java/br/com/escola/matricula/infraestrutura/persistencia/typehandler/CpfTypeHandler.java
    src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java
    src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRepositorioMyBatis.java
    src/main/java/br/com/escola/matricula/infraestrutura/eventos/FinanceiroEventListener.java
    src/main/java/br/com/escola/matricula/infraestrutura/eventos/AcademicoEventListener.java
    src/main/java/br/com/escola/matricula/infraestrutura/config/MyBatisConfig.java
    src/main/java/br/com/escola/matricula/infraestrutura/config/DomainServicesConfig.java
    src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java
  </files>

  <read_first>
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 2 — TypeHandlers: CpfTypeHandler COMPLETO; PeriodoLetivo = Opção C mapeamento inline, SEM TypeHandler customizado)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 9 — MatriculaRowMapper COMPLETO com toDomain, fromDomain, itemsFromDomain; MatriculaRepositorioMyBatis COMPLETO com replace-all)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 8 — Listeners Stub com @TransactionalEventListener; seção Pitfall 8 — Domain Service sem @Service; seção 5 — DemoRunner / CommandLineRunner)
    - .planning/phases/03-implementacao/03-CONTEXT.md (D-12 replace-all; D-13 MatriculaRowMapper; D-14 PeriodoLetivo conversão)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção Pitfall 9 — eventos não inicializados; Pitfall 10 — underscore camelCase)
    - docs/adrs/ADR-001-mybatis-vs-jpa.md (decisão MyBatis — referência para Javadocs)
  </read_first>

  <action>
Criar os artefatos restantes de infraestrutura que completam a camada de persistência, configuração e integração.

CpfTypeHandler.java — pacote br.com.escola.matricula.infraestrutura.persistencia.typehandler (per INF-05):
  Usar o código EXATO da seção 2 do RESEARCH.md.
  Extends BaseTypeHandler<Cpf>, anotações @MappedTypes(Cpf.class) e @MappedJdbcTypes(JdbcType.VARCHAR).
  Implementar os 4 métodos: setNonNullParameter, getNullableResult(ResultSet, String), getNullableResult(ResultSet, int), getNullableResult(CallableStatement, int).
  O TypeHandler é registrado automaticamente via mybatis.type-handlers-package no application.yml (conforme RESEARCH.md Don't Hand-Roll — sem configuração manual).
  NOTA: PeriodoLetivo NÃO tem TypeHandler customizado — usa mapeamento inline em MatriculaRowMapper (Opção C do RESEARCH.md, conforme CONTEXT.md Claude's Discretion).

MatriculaRowMapper.java — pacote br.com.escola.matricula.infraestrutura.persistencia (per INF-06):
  @Component para que Spring possa injetá-lo em MatriculaRepositorioMyBatis.
  Usar o código EXATO da seção 9 do RESEARCH.md.
  Método toDomain(MatriculaRow row): Matricula — conversão explícita Row→domínio. Conversão PeriodoLetivo inline:
    var periodo = new PeriodoLetivo(row.periodoInicio.getYear(),
                                    row.periodoInicio.getMonthValue() <= 6 ? 1 : 2);
    Esta conversão (DATE → ano+semestre) é uma regra de negócio explicitada no código (Opção C).
  Método fromDomain(Matricula m): MatriculaRow — conversão domínio→Row. Conversão PeriodoLetivo inversa:
    row.periodoInicio = LocalDate.of(m.getPeriodoLetivo().ano(), m.getPeriodoLetivo().semestre() == 1 ? 2 : 8, 1);
    row.periodoFim    = LocalDate.of(m.getPeriodoLetivo().ano(), m.getPeriodoLetivo().semestre() == 1 ? 7 : 12, 31);
  Método itemsFromDomain(MatriculaId matriculaId, List<ItemMatricula> items): List<ItemMatriculaRow>
  Método privado reconstruirStatus(MatriculaRow row): StatusMatricula — switch exaustivo sem default caso string conhecida.
  Javadoc na classe: "ÚNICO arquivo que conhece tanto MatriculaRow quanto Matricula. Compare com Matricula.java: um tem comportamento (adicionarDisciplina, cancelar), o outro é dados puros (ADR-001, INF-06). Esta separação explícita é o ponto pedagógico central do projeto."

MatriculaRepositorioMyBatis.java — pacote br.com.escola.matricula.infraestrutura.persistencia (per INF-07):
  @Repository, implements MatriculaRepositorio.
  Injeção por construtor de MatriculaMapper e MatriculaRowMapper.
  Implementar os 4 métodos da interface de domínio:
  - buscarPorId: mapper.buscarPorId(id.valor()), retorna Optional.ofNullable(row).map(rowMapper::toDomain)
  - buscarPorAluno: mapper.buscarPorAluno(alunoId.valor()).stream().map(rowMapper::toDomain).toList()
  - existeMatriculaAtiva: converte PeriodoLetivo para LocalDate (mesma lógica do MatriculaRowMapper.fromDomain), chama mapper.existeMatriculaAtiva()
  - salvar (replace-all per D-12):
    MatriculaRow row = rowMapper.fromDomain(matricula);
    if (mapper.atualizarMatricula(row) == 0) { mapper.inserirMatricula(row); }  // UPDATE se existente, INSERT se novo
    mapper.deletarItensPorMatriculaId(matricula.getId().valor());  // DELETE todos os itens
    List<ItemMatriculaRow> itens = rowMapper.itemsFromDomain(matricula.getId(), matricula.getDisciplinas());
    if (!itens.isEmpty()) { mapper.inserirItens(itens); }  // INSERT itens atuais
  Javadoc: "Implementação de MatriculaRepositorio usando MyBatis. Estratégia replace-all para ItemMatricula: DELETE + INSERT a cada save. Pedagogicamente explícita — o Aggregate é a unidade de consistência (D-12)."

FinanceiroEventListener.java — pacote br.com.escola.matricula.infraestrutura.eventos (per APL-05):
  @Component
  Logger: private static final Logger log = LoggerFactory.getLogger(FinanceiroEventListener.class)
  @TransactionalEventListener (org.springframework.transaction.event.TransactionalEventListener)
  Método aoMatricular(AlunoMatriculado evento): log.info("[BC Financeiro] Criando contrato de cobrança para matrícula {}", evento.matriculaId().valor())
  Método aoCancelar(MatriculaCancelada evento): log.info("[BC Financeiro] Processando cancelamento de cobrança para matrícula {}", evento.matriculaId().valor())
  Javadoc: "Listener stub do Bounded Context Financeiro. Usa @TransactionalEventListener (não @EventListener) para executar APÓS commit — garante que o evento representa um fato persistido. Implementação real: v2 BC-01."

AcademicoEventListener.java — pacote br.com.escola.matricula.infraestrutura.eventos (per APL-05):
  @Component, mesmo padrão do FinanceiroEventListener.
  @TransactionalEventListener aoMatricular(AlunoMatriculado evento): log "[BC Academico] Registrando vínculo aluno-turma para matrícula {}"
  @TransactionalEventListener aoDisciplinaAdicionada(DisciplinaAdicionada evento): log "[BC Academico] Atualizando histórico: disciplina {} adicionada"
  Javadoc semelhante: "Listener stub do BC Acadêmico. @TransactionalEventListener garante execução após commit. v2: BC-02."

MyBatisConfig.java — pacote br.com.escola.matricula.infraestrutura.config:
  @Configuration
  Classe vazia por ora — TypeHandlers são registrados via type-handlers-package no application.yml.
  Javadoc: "Configuração MyBatis. TypeHandlers registrados via mybatis.type-handlers-package no application.yml (não via @Bean aqui). Esta classe existe para futuras configurações de infraestrutura (Fase 4: DataSource customizado, etc.)."

DomainServicesConfig.java — pacote br.com.escola.matricula.infraestrutura.config:
  @Configuration
  @Bean VerificadorElegibilidadeMatricula verificadorElegibilidadeMatricula(MatriculaRepositorio repositorio):
    return new VerificadorElegibilidadeMatricula(repositorio);
  Javadoc: "Instancia o Domain Service VerificadorElegibilidadeMatricula como bean Spring. O Domain Service é Java puro (sem @Service) — esta @Bean o torna disponível para injeção nos UseCases sem adicionar Spring ao pacote dominio/. Padrão: framework na infra, domínio puro."

DemoRunner.java — pacote br.com.escola.matricula.infraestrutura.config:
  @Component, implements CommandLineRunner (org.springframework.boot.CommandLineRunner)
  Injeção por construtor de MatricularAlunoUseCase, AdicionarDisciplinaUseCase, CancelarMatriculaUseCase.
  Logger SLF4J para imprimir resultados.

  Método run(String... args) — executar os três fluxos com dados dos seeds:
  FLUXO 1 — Matricular Aluno (Maria Silva matrícula na Turma 2026-1):
    Criar Aluno Maria (UUID a0000000-...-000000000001, CPF 12345678901)
    Criar Turma (UUID b0000000-...-000000000001, Turma 2026-1, PeriodoLetivo 2026/1)
    MatricularAlunoCommand command = new MatricularAlunoCommand(aluno, turma, new PeriodoLetivo(2026, 1));
    MatriculaId novaId = matricularUseCase.executar(command);
    log.info("[DemoRunner] FLUXO 1: Matrícula criada com ID {}", novaId.valor());

  FLUXO 2 — Adicionar Disciplina (na matrícula pré-existente do João):
    MatriculaId matriculaExistente = new MatriculaId(UUID.fromString("c0000000-0000-0000-0000-000000000001"));
    AdicionarDisciplinaCommand cmd2 = new AdicionarDisciplinaCommand(matriculaExistente, new NomeDisciplina("Física Quântica"));
    adicionarUseCase.executar(cmd2);
    log.info("[DemoRunner] FLUXO 2: Disciplina adicionada à matrícula {}", matriculaExistente.valor());

  FLUXO 3 — Cancelar Matrícula (a matrícula recém-criada no Fluxo 1):
    CancelarMatriculaCommand cmd3 = new CancelarMatriculaCommand(novaId);
    cancelarUseCase.executar(cmd3);
    log.info("[DemoRunner] FLUXO 3: Matrícula {} cancelada", novaId.valor());

  Javadoc: "CommandLineRunner de demonstração — executa os 3 fluxos de negócio usando dados dos seeds (V2__seeds.sql). Permite verificar o funcionamento completo sem Controllers HTTP. Fase 4 adicionará os Controllers REST."
  </action>

  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && mvn compile -q 2>&1 | tail -5 && grep "@TransactionalEventListener" src/main/java/br/com/escola/matricula/infraestrutura/eventos/FinanceiroEventListener.java && grep "implements MatriculaRepositorio" src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRepositorioMyBatis.java</automated>
  </verify>

  <acceptance_criteria>
- grep "implements MatriculaRepositorio" src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRepositorioMyBatis.java retorna resultado
- grep "@TransactionalEventListener" src/main/java/br/com/escola/matricula/infraestrutura/eventos/FinanceiroEventListener.java retorna resultado
- grep "@TransactionalEventListener" src/main/java/br/com/escola/matricula/infraestrutura/eventos/AcademicoEventListener.java retorna resultado
- grep "@Bean" src/main/java/br/com/escola/matricula/infraestrutura/config/DomainServicesConfig.java retorna resultado (VerificadorElegibilidadeMatricula criado como bean)
- grep "CommandLineRunner" src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java retorna resultado
- grep -v "^.*[*#/]" src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRow.java | grep -c "public void\|public boolean\|public int\|public String\|public List" mostra apenas os campos públicos, sem métodos de negócio
- mvn compile -q retorna BUILD SUCCESS (projeto inteiro — domínio + aplicação + infraestrutura)
  </acceptance_criteria>

  <done>14 arquivos de infraestrutura completos: TypeHandler, MatriculaRowMapper, MatriculaRepositorioMyBatis, listeners stub com @TransactionalEventListener, DomainServicesConfig com @Bean para Domain Service, DemoRunner com os 3 fluxos — projeto inteiro compila</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Projeto Java 21 DDD completo implementado. Com PostgreSQL rodando em localhost:5432, é possível executar `mvn spring-boot:run` e ver os 3 fluxos de negócio executando no console.
  </what-built>
  <how-to-verify>
1. Inicie o PostgreSQL na porta 5432 com um banco erp_matricula e usuário matricula/matricula:
   docker run --name erp-postgres -e POSTGRES_DB=erp_matricula -e POSTGRES_USER=matricula -e POSTGRES_PASSWORD=matricula -p 5432:5432 -d postgres:16

2. Execute o projeto:
   cd /home/marcelino/Área\ de\ trabalho/erp-matricula && mvn spring-boot:run

3. Verifique nos logs:
   - "Successfully applied 2 migrations" (Flyway aplicou V1 e V2)
   - "[DemoRunner] FLUXO 1: Matrícula criada com ID ..." (UUID gerado)
   - "[DemoRunner] FLUXO 2: Disciplina adicionada à matrícula ..."
   - "[DemoRunner] FLUXO 3: Matrícula ... cancelada"
   - "[BC Financeiro] Criando contrato de cobrança ..." (listener stub ativado)
   - "[BC Academico] Registrando vínculo aluno-turma ..." (listener stub ativado)

4. Verificação estrutural:
   grep -r "import org.springframework" src/main/java/br/com/escola/matricula/dominio/
   → deve retornar VAZIO (critério de sucesso central da fase)

5. Verificação de compilação limpa:
   mvn compile -q
   → deve retornar BUILD SUCCESS sem warnings
  </how-to-verify>
  <resume-signal>
Digite "aprovado" se os 3 fluxos executaram com sucesso e o grep do domínio retornou vazio.
Descreva qualquer erro encontrado para correção antes de prosseguir para a Fase 4.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL (Flyway migrations) → PostgreSQL | Scripts executados com privilégios do usuário do banco |
| MatriculaMapper.xml → PostgreSQL | Queries parametrizadas enviadas ao banco |
| @TransactionalEventListener → contexto de negócio | Listeners recebem eventos com dados de domínio após commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | MatriculaMapper.xml — parâmetros SQL | mitigate | MyBatis usa PreparedStatement — parâmetros #{param} nunca são concatenados. SQL injection não é possível via parâmetros MyBatis. |
| T-03-07 | Tampering | Flyway V2__seeds.sql (UUIDs fixos) | accept | Seeds são para desenvolvimento/demo apenas. Não devem rodar em produção (Fase 4 terá profiles). UUIDs fixos não são segredo. |
| T-03-08 | Information Disclosure | DemoRunner (imprime MatriculaId no log) | accept | Logs de desenvolvimento com nível DEBUG. IDs de matrícula não são dados sensíveis neste contexto didático. |
| T-03-09 | Denial of Service | DemoRunner executa 3 fluxos no startup | accept | Executa uma única vez no contexto de inicialização. CommandLineRunner é design intencional para demo sem HTTP. |
| T-03-SC | Tampering | org.apache.ibatis (TypeHandler) | mitigate | mybatis-spring-boot-starter 3.0.5 verificado em Maven Central. TypeHandler é código próprio (CpfTypeHandler) — review manual da lógica de parse do CPF. |
</threat_model>

<verification>
Verificações finais da fase completa:

1. SUCESSO CRÍTICO (Success Criterion 1 do ROADMAP):
   grep -r "import org.springframework" src/main/java/br/com/escola/matricula/dominio/
   grep -r "import org.mybatis" src/main/java/br/com/escola/matricula/dominio/
   Ambos → VAZIO

2. Compilação:
   mvn compile -q → BUILD SUCCESS

3. MatriculaRow sem comportamento (Success Criterion 4):
   grep -c "public void\|public boolean\|public int" \
     src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRow.java
   → 0

4. Pitfall 1 mitigado:
   grep "notNullColumn" src/main/resources/mapper/MatriculaMapper.xml → presente

5. Pitfall 2 mitigado:
   grep -c "jdbcType=OTHER" src/main/resources/mapper/MatriculaMapper.xml → >= 5

6. Listeners com TransactionalEventListener:
   grep "@TransactionalEventListener" \
     src/main/java/br/com/escola/matricula/infraestrutura/eventos/FinanceiroEventListener.java
   → presente

7. Domain Service como @Bean (sem @Service no domínio):
   grep "@Bean" src/main/java/br/com/escola/matricula/infraestrutura/config/DomainServicesConfig.java → presente
   grep "@Service\|@Component" src/main/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatricula.java → VAZIO
</verification>

<success_criteria>
- Schema PostgreSQL V1 com 4 tabelas, comentários pedagógicos, sem FK aluno_id/turma_id em matriculas
- Seeds V2 com 2 alunos, 1 turma, 1 matrícula pré-existente com 1 disciplina (UUIDs fixos)
- MatriculaMapper.xml com notNullColumn="item_disciplina" e jdbcType=OTHER em todos os parâmetros UUID
- MatriculaRow com apenas campos, sem métodos de negócio
- CpfTypeHandler registrado via type-handlers-package (auto-scanning)
- MatriculaRowMapper com conversão explícita PeriodoLetivo inline (Opção C — sem TypeHandler customizado)
- MatriculaRepositorioMyBatis com replace-all implementado (UPDATE-or-INSERT + DELETE + INSERT)
- FinanceiroEventListener e AcademicoEventListener com @TransactionalEventListener
- DomainServicesConfig com @Bean para VerificadorElegibilidadeMatricula
- DemoRunner executa os 3 fluxos no startup
- mvn compile -q → BUILD SUCCESS
- Checkpoint: mvn spring-boot:run com PostgreSQL mostra os 3 fluxos nos logs
</success_criteria>

<output>
Criar `.planning/phases/03-implementacao/03-04-SUMMARY.md` ao finalizar
</output>
