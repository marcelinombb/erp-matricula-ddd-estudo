---
phase: 03-implementacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/br/com/escola/matricula/dominio/vo/Cpf.java
  - src/main/java/br/com/escola/matricula/dominio/vo/PeriodoLetivo.java
  - src/main/java/br/com/escola/matricula/dominio/vo/MatriculaId.java
  - src/main/java/br/com/escola/matricula/dominio/vo/AlunoId.java
  - src/main/java/br/com/escola/matricula/dominio/vo/TurmaId.java
  - src/main/java/br/com/escola/matricula/dominio/vo/NomeDisciplina.java
  - src/main/java/br/com/escola/matricula/dominio/modelo/StatusMatricula.java
  - src/main/java/br/com/escola/matricula/dominio/modelo/Aluno.java
  - src/main/java/br/com/escola/matricula/dominio/modelo/Turma.java
  - src/main/java/br/com/escola/matricula/dominio/modelo/ItemMatricula.java
  - src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java
  - src/main/java/br/com/escola/matricula/dominio/evento/AlunoMatriculado.java
  - src/main/java/br/com/escola/matricula/dominio/evento/DisciplinaAdicionada.java
  - src/main/java/br/com/escola/matricula/dominio/evento/MatriculaCancelada.java
  - src/main/java/br/com/escola/matricula/dominio/repositorio/MatriculaRepositorio.java
  - src/main/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatricula.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/DisciplinaJaMatriculadaException.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaCanceladaException.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/AlunoInativoException.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/PeriodoFechadoException.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaDuplicadaException.java
  - src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaNaoEncontradaException.java
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - DOM-04
  - DOM-05
  - DOM-06
  - DOM-07
  - DOM-08
  - DOM-09
  - DOM-10

must_haves:
  truths:
    - "grep -r 'import org.springframework' src/main/java/br/com/escola/matricula/dominio/ retorna VAZIO"
    - "grep -r 'import org.mybatis' src/main/java/br/com/escola/matricula/dominio/ retorna VAZIO"
    - "grep -r 'import jakarta' src/main/java/br/com/escola/matricula/dominio/ retorna VAZIO"
    - "Matricula.adicionarDisciplina() lança LimiteDisciplinasExcedidoException quando disciplinas.size() >= 6"
    - "Matricula.coletarEventos() retorna cópia e limpa a lista interna"
    - "LimiteDisciplinasExcedidoException tem campos int limite e int atual"
    - "VerificadorElegibilidadeMatricula é Java puro sem @Component nem @Service"
  artifacts:
    - path: "src/main/java/br/com/escola/matricula/dominio/vo/Cpf.java"
      provides: "Value Object CPF com validação de dígito verificador"
      contains: "record Cpf"
    - path: "src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java"
      provides: "Aggregate Root com invariantes encapsuladas e coleta de eventos"
      exports: ["criar", "adicionarDisciplina", "cancelar", "coletarEventos"]
    - path: "src/main/java/br/com/escola/matricula/dominio/modelo/StatusMatricula.java"
      provides: "Sealed interface com records internos Ativa, Cancelada, Concluida"
      contains: "sealed interface StatusMatricula"
    - path: "src/main/java/br/com/escola/matricula/dominio/repositorio/MatriculaRepositorio.java"
      provides: "Interface de repositório no domínio sem imports de framework"
      exports: ["buscarPorId", "buscarPorAluno", "existeMatriculaAtiva", "salvar"]
  key_links:
    - from: "Matricula.java"
      to: "dominio/excecao/"
      via: "adicionarDisciplina() lança exceções tipadas"
      pattern: "LimiteDisciplinasExcedidoException|DisciplinaJaMatriculadaException|MatriculaCanceladaException"
    - from: "Matricula.java"
      to: "dominio/evento/"
      via: "eventos.add() dentro dos métodos de negócio"
      pattern: "eventos.add"
    - from: "VerificadorElegibilidadeMatricula.java"
      to: "MatriculaRepositorio.java"
      via: "injeção por construtor"
      pattern: "MatriculaRepositorio"
---

<objective>
Implementar a camada de domínio completa — todos os Value Objects, Entidades, Aggregate Root, Domain Events, interface de Repositório, Domain Service e Exceções.

Propósito: Esta é a camada mais importante do projeto. Contém zero dependências de framework (sem Spring, sem MyBatis, sem Jakarta). É Java puro que encapsula as regras de negócio do Bounded Context Matrícula.

Saídas: 23 arquivos .java no pacote br.com.escola.matricula.dominio.* que compilam sem nenhum import de org.springframework, org.mybatis ou jakarta.persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-implementacao/03-CONTEXT.md
@.planning/phases/03-implementacao/03-RESEARCH.md
@docs/02-design-tatico/value-objects.md
@docs/02-design-tatico/entidades.md
@docs/02-design-tatico/agregados.md
@docs/02-design-tatico/domain-events.md
@docs/02-design-tatico/domain-services.md
@docs/02-design-tatico/repositorios.md
@docs/adrs/ADR-003-referencia-por-id.md
@docs/adrs/ADR-004-codigo-em-portugues.md
@docs/01-design-estrategico/linguagem-ubiqua.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Value Objects, StatusMatricula, Entidades e Domain Events</name>
  <files>
    src/main/java/br/com/escola/matricula/dominio/vo/Cpf.java
    src/main/java/br/com/escola/matricula/dominio/vo/PeriodoLetivo.java
    src/main/java/br/com/escola/matricula/dominio/vo/MatriculaId.java
    src/main/java/br/com/escola/matricula/dominio/vo/AlunoId.java
    src/main/java/br/com/escola/matricula/dominio/vo/TurmaId.java
    src/main/java/br/com/escola/matricula/dominio/vo/NomeDisciplina.java
    src/main/java/br/com/escola/matricula/dominio/modelo/StatusMatricula.java
    src/main/java/br/com/escola/matricula/dominio/modelo/Aluno.java
    src/main/java/br/com/escola/matricula/dominio/modelo/Turma.java
    src/main/java/br/com/escola/matricula/dominio/modelo/ItemMatricula.java
    src/main/java/br/com/escola/matricula/dominio/evento/AlunoMatriculado.java
    src/main/java/br/com/escola/matricula/dominio/evento/DisciplinaAdicionada.java
    src/main/java/br/com/escola/matricula/dominio/evento/MatriculaCancelada.java
  </files>

  <read_first>
    - docs/02-design-tatico/value-objects.md (especificação dos VOs — snippets Java 21 com construtor compacto)
    - docs/02-design-tatico/entidades.md (Aluno, Turma, ItemMatricula — equals/hashCode por identidade)
    - docs/02-design-tatico/agregados.md (StatusMatricula como sealed interface, ciclo de vida da Matrícula)
    - docs/02-design-tatico/domain-events.md (catálogo AlunoMatriculado, DisciplinaAdicionada, MatriculaCancelada)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 6 — Java 21 Features: records, sealed interface, compact constructor; seção 7 — tabela VOs e assinaturas de entidades)
    - docs/adrs/ADR-004-codigo-em-portugues.md (nomes em português — OBRIGATÓRIO)
    - docs/01-design-estrategico/linguagem-ubiqua.md (glossário — fonte dos nomes corretos)
  </read_first>

  <action>
Criar todos os Value Objects, StatusMatricula, Entidades e Domain Events com as especificações abaixo. REGRA ABSOLUTA: nenhum arquivo pode ter import de org.springframework.*, org.mybatis.* ou jakarta.*. Verificar com grep após cada arquivo.

PACOTE dominio.vo/ — Value Objects como records Java 21 (per DOM-01, per RESEARCH.md seção 7):

Cpf.java — pacote br.com.escola.matricula.dominio.vo:
  record Cpf(String valor) com construtor compacto que:
  - valida não-nulo (Objects.requireNonNull)
  - normaliza para apenas dígitos: valor = valor.replaceAll("[^0-9]", "")
  - valida comprimento 11 dígitos
  - valida algoritmo módulo 11 (implementar verificação real dos dois dígitos verificadores)
  - método formatado() retornando "xxx.xxx.xxx-xx"
  Usar java.util.Objects. Javadoc explicando por que CPF é VO (imutabilidade, identidade por valor).

PeriodoLetivo.java — pacote br.com.escola.matricula.dominio.vo:
  record PeriodoLetivo(int ano, int semestre) com construtor compacto que:
  - valida ano >= 2000
  - valida semestre em {1, 2}
  IMPORTANTE: manter como (ano, semestre) — não (LocalDate, LocalDate). A conversão para DATE do banco é feita pelo MatriculaRowMapper no Wave 3 (Opção C do RESEARCH.md).
  Javadoc explicando que a conversão para colunas periodo_inicio/periodo_fim ocorre na camada de infraestrutura (MatriculaRowMapper), não no VO.

MatriculaId.java, AlunoId.java, TurmaId.java — pacote br.com.escola.matricula.dominio.vo:
  Cada um: record XxxId(java.util.UUID valor) com construtor compacto validando não-nulo.
  Javadoc explicando por que IDs tipados existem (ADR-003 — referência por ID entre Aggregates).

NomeDisciplina.java — pacote br.com.escola.matricula.dominio.vo:
  record NomeDisciplina(String valor) com construtor compacto que:
  - valida não-nulo
  - valida não-branco (valor.isBlank())
  - valida comprimento máximo 100 caracteres

PACOTE dominio.modelo/ — StatusMatricula e Entidades:

StatusMatricula.java — pacote br.com.escola.matricula.dominio.modelo:
  sealed interface StatusMatricula permits StatusMatricula.Ativa, StatusMatricula.Cancelada, StatusMatricula.Concluida
  Com records internos:
  - record Ativa() implements StatusMatricula {}
  - record Cancelada(java.time.LocalDateTime canceladaEm) implements StatusMatricula {}
  - record Concluida(java.time.LocalDateTime concluidaEm) implements StatusMatricula {}
  Javadoc explicando como o pattern matching exaustivo elimina `if (status == "ATIVA")`.

Aluno.java — pacote br.com.escola.matricula.dominio.modelo (per DOM-02):
  Classe (não record) — tem ciclo de vida, estado mutável (ativo)
  Campos: private final AlunoId id; private final Cpf cpf; private final String nome; private boolean ativo;
  Construtor com todos os campos.
  equals() e hashCode() POR IDENTIDADE (apenas usando id) — padrão de Entidade DDD.
  Métodos: getId(), getCpf(), getNome(), estaAtivo(), desativar().
  Javadoc explicando diferença entre Entidade e VO: Aluno tem identidade, CPF não.

Turma.java — pacote br.com.escola.matricula.dominio.modelo (per DOM-03):
  Classe com campos: private final TurmaId id; private final String nome; private final PeriodoLetivo periodoLetivo; private final int vagasMaximas;
  Construtor com todos os campos.
  Método periodoEstaAberto(): compara LocalDate.now() com as datas calculadas a partir do PeriodoLetivo. Semestre 1 = fev (dia 1) a jul (dia 31). Semestre 2 = ago (dia 1) a dez (dia 31). Retorna true se LocalDate.now() está dentro do período.
  equals() e hashCode() por TurmaId.
  Getters, sem setters públicos.

ItemMatricula.java — pacote br.com.escola.matricula.dominio.modelo:
  record ItemMatricula(NomeDisciplina disciplina) — entidade interna imutável sem ID próprio.
  Javadoc explicando que é entidade interna do Aggregate, nunca referenciada diretamente de fora.

PACOTE dominio.evento/ — Domain Events como records (per DOM-06, D-11):

AlunoMatriculado.java:
  record AlunoMatriculado(MatriculaId matriculaId, AlunoId alunoId, TurmaId turmaId, PeriodoLetivo periodoLetivo, java.time.LocalDateTime ocorridoEm)
  SEM interface base, SEM anotações Spring — per D-11.

DisciplinaAdicionada.java:
  record DisciplinaAdicionada(MatriculaId matriculaId, AlunoId alunoId, NomeDisciplina disciplina, java.time.LocalDateTime ocorridoEm)

MatriculaCancelada.java:
  record MatriculaCancelada(MatriculaId matriculaId, AlunoId alunoId, PeriodoLetivo periodoLetivo, java.time.LocalDateTime ocorridoEm)

Para cada Domain Event, adicionar Javadoc com: quem publica (UseCase correspondente), quem consome (FinanceiroEventListener e/ou AcademicoEventListener), e por que é um Domain Event e não um método de callback.
  </action>

  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && grep -rl "import org.springframework\|import org.mybatis\|import jakarta" src/main/java/br/com/escola/matricula/dominio/ 2>/dev/null; echo "---"; mvn compile -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
- grep -rl "import org.springframework\|import org.mybatis\|import jakarta" src/main/java/br/com/escola/matricula/dominio/ retorna VAZIO (zero arquivos com imports proibidos)
- grep "sealed interface StatusMatricula" src/main/java/br/com/escola/matricula/dominio/modelo/StatusMatricula.java retorna resultado
- grep "record PeriodoLetivo(int ano, int semestre)" src/main/java/br/com/escola/matricula/dominio/vo/PeriodoLetivo.java retorna resultado (não LocalDate!)
- grep "record AlunoMatriculado" src/main/java/br/com/escola/matricula/dominio/evento/AlunoMatriculado.java retorna resultado
- Todos os 13 arquivos desta tarefa existem em seus caminhos corretos
- mvn compile -q retorna BUILD SUCCESS
  </acceptance_criteria>

  <done>Value Objects, StatusMatricula, Entidades e Domain Events criados, compilam sem imports de framework, grep de Spring/MyBatis/Jakarta no pacote dominio/ retorna vazio</done>
</task>

<task type="auto">
  <name>Tarefa 2: Aggregate Root Matricula, Repositório, Domain Service e Exceções</name>
  <files>
    src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java
    src/main/java/br/com/escola/matricula/dominio/repositorio/MatriculaRepositorio.java
    src/main/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatricula.java
    src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java
    src/main/java/br/com/escola/matricula/dominio/excecao/DisciplinaJaMatriculadaException.java
    src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaCanceladaException.java
    src/main/java/br/com/escola/matricula/dominio/excecao/AlunoInativoException.java
    src/main/java/br/com/escola/matricula/dominio/excecao/PeriodoFechadoException.java
    src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaDuplicadaException.java
    src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaNaoEncontradaException.java
  </files>

  <read_first>
    - docs/02-design-tatico/agregados.md (invariantes do Aggregate Matricula, adicionarDisciplina() com 3 guards, cancelar(), LIMITE_DISCIPLINAS = 6)
    - docs/02-design-tatico/repositorios.md (interface MatriculaRepositorio — assinaturas exatas)
    - docs/02-design-tatico/domain-services.md (VerificadorElegibilidadeMatricula — o que verifica)
    - .planning/phases/03-implementacao/03-RESEARCH.md (seção 7 — Matricula.java, VerificadorElegibilidadeMatricula, excecoes; seção Pitfall 8 — sem @Service no Domain Service; seção Pitfall 9 — construtor de reconstituição deve inicializar eventos)
    - .planning/phases/03-implementacao/03-CONTEXT.md (D-09 — List<Object> eventos + coletarEventos(); D-10 — ordem de publicação; D-11 — Domain Events sem interface base)
    - contexto-matricula.md (regras de negócio — fonte de verdade para invariantes)
  </read_first>

  <action>
Criar Matricula (Aggregate Root), MatriculaRepositorio, VerificadorElegibilidadeMatricula e todas as exceções de domínio.

REGRA ABSOLUTA: zero imports de org.springframework.*, org.mybatis.*, jakarta.* em qualquer arquivo desta tarefa.

Matricula.java — pacote br.com.escola.matricula.dominio.modelo (per DOM-04, DOM-07, D-09):
  Classe Matricula (Aggregate Root) com:
  Constante: private static final int LIMITE_DISCIPLINAS = 6;
  Campos imutáveis: private final MatriculaId id; private final AlunoId alunoId; private final TurmaId turmaId; private final PeriodoLetivo periodoLetivo;
  Campos mutáveis: private StatusMatricula status; private final List<ItemMatricula> disciplinas; private final List<Object> eventos;

  Construtor estático criar(AlunoId alunoId, TurmaId turmaId, PeriodoLetivo periodoLetivo):
    - Gera MatriculaId novo com UUID.randomUUID()
    - Inicializa status = new StatusMatricula.Ativa()
    - Inicializa disciplinas = new ArrayList<>()
    - Inicializa eventos = new ArrayList<>()
    - Adiciona AlunoMatriculado ao eventos

  Construtor de reconstituição Matricula(MatriculaId id, AlunoId alunoId, TurmaId turmaId, PeriodoLetivo periodoLetivo, StatusMatricula status, List<ItemMatricula> disciplinas):
    - Inicializa TODOS os campos
    - CRÍTICO: this.eventos = new ArrayList<>() — sem isso, adicionarDisciplina() lança NPE ao tentar eventos.add() (Pitfall 9 do RESEARCH.md)
    - this.disciplinas = new ArrayList<>(disciplinas) — cópia defensiva

  Método void adicionarDisciplina(NomeDisciplina disciplina) (per DOM-04):
    - Guard 1: se status instanceof StatusMatricula.Cancelada → lança MatriculaCanceladaException(this.id)
    - Guard 2: se disciplinas.size() >= LIMITE_DISCIPLINAS → lança LimiteDisciplinasExcedidoException(LIMITE_DISCIPLINAS, this.id)
    - Guard 3: se disciplinas contém ItemMatricula com disciplina.equals(novaDisciplina) → lança DisciplinaJaMatriculadaException(disciplina, this.id)
    - Adiciona new ItemMatricula(disciplina) à lista
    - Adiciona new DisciplinaAdicionada(this.id, this.alunoId, disciplina, LocalDateTime.now()) ao eventos

  Método void cancelar():
    - Guard: se status instanceof StatusMatricula.Cancelada → lança MatriculaCanceladaException(this.id)
    - Atualiza status = new StatusMatricula.Cancelada(LocalDateTime.now())
    - Adiciona new MatriculaCancelada(this.id, this.alunoId, this.periodoLetivo, LocalDateTime.now()) ao eventos

  Método List<Object> coletarEventos() (per D-09):
    - List<Object> copia = List.copyOf(this.eventos)
    - this.eventos.clear()
    - return copia
    Javadoc explicando: "Coleta eventos para publicação via ApplicationEventPublisher no UseCase. Chamado APÓS repositorio.salvar() — per D-10."

  Getters para todos os campos (getId, getAlunoId, getTurmaId, getPeriodoLetivo, getStatus, getDisciplinas — retorna List.copyOf para imutabilidade).

  Javadoc na classe explicando: "Aggregate Root do BC Matrícula. Toda lógica de negócio de matrícula é encapsulada aqui. Nunca modificar estado exteriormente — usar apenas os métodos de domínio."

MatriculaRepositorio.java — pacote br.com.escola.matricula.dominio.repositorio (per DOM-09):
  Interface pura com exatamente:
  Optional<Matricula> buscarPorId(MatriculaId id);
  List<Matricula> buscarPorAluno(AlunoId alunoId);
  boolean existeMatriculaAtiva(AlunoId alunoId, PeriodoLetivo periodo);
  void salvar(Matricula matricula);
  Imports SOMENTE de java.util.Optional e java.util.List — ZERO framework imports.
  Javadoc na interface explicando: "Interface de Repositório declarada no DOMÍNIO (não na infraestrutura). Dependency Inversion: domínio define o contrato; infraestrutura implementa. Implementação: MatriculaRepositorioMyBatis."

VerificadorElegibilidadeMatricula.java — pacote br.com.escola.matricula.dominio.servico (per DOM-08):
  Classe Java pura — SEM @Service, SEM @Component (per RESEARCH.md Pitfall 8 e CONTEXT.md D-08).
  Campo: private final MatriculaRepositorio repositorio;
  Construtor com MatriculaRepositorio — injeção de dependência por construtor.
  Método void verificar(Aluno aluno, Turma turma, PeriodoLetivo periodo):
    - Se !aluno.estaAtivo() → lança AlunoInativoException(aluno.getId())
    - Se !turma.periodoEstaAberto() → lança PeriodoFechadoException(periodo)
    - Se repositorio.existeMatriculaAtiva(aluno.getId(), periodo) → lança MatriculaDuplicadaException(aluno.getId(), periodo)
  Javadoc: "Domain Service — lógica que cruza Aluno e Turma. Sem @Component: é Java puro, instanciado pelo Spring via @Bean em infraestrutura/config/. Não pertence a um único Aggregate porque envolve consulta ao repositório."

Exceções — pacote br.com.escola.matricula.dominio.excecao (per DOM-10):
  Todas estendem RuntimeException. Todas têm campos estruturados (não apenas mensagem String).

LimiteDisciplinasExcedidoException: campos private final int limite; private final int atual; Construtores que recebem (int limite, int atual, MatriculaId matriculaId). Getters getLimite() e getAtual(). Mensagem: "Limite de X disciplinas excedido. Atual: Y. Matrícula: Z".

DisciplinaJaMatriculadaException: campo private final NomeDisciplina disciplina; private final MatriculaId matriculaId; Mensagem: "Disciplina 'X' já está matriculada em Y".

MatriculaCanceladaException: campo private final MatriculaId matriculaId; Mensagem: "Matrícula X está cancelada e não aceita operações".

AlunoInativoException: campo private final AlunoId alunoId; Mensagem: "Aluno X está inativo e não pode ser matriculado".

PeriodoFechadoException: campo private final PeriodoLetivo periodo; Mensagem: "Período X/Y não está aberto para matrícula".

MatriculaDuplicadaException: campos private final AlunoId alunoId; private final PeriodoLetivo periodo; Mensagem: "Aluno X já possui matrícula ativa no período Y/Z".

MatriculaNaoEncontradaException: campo private final MatriculaId matriculaId; Mensagem: "Matrícula X não encontrada". (Usada pelos UseCases na Tarefa Wave 2.)
  </action>

  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && grep -r "import org.springframework\|import org.mybatis\|import jakarta" src/main/java/br/com/escola/matricula/dominio/ 2>/dev/null; echo "GREP RESULT ABOVE (should be empty)"; mvn compile -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
- grep "import org.springframework\|import org.mybatis\|import jakarta" src/main/java/br/com/escola/matricula/dominio/**/*.java retorna VAZIO (zero ocorrências)
- grep "private static final int LIMITE_DISCIPLINAS = 6" src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java retorna resultado
- grep "List.copyOf" src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java retorna resultado (coletarEventos)
- grep "this.eventos = new ArrayList" src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java retorna resultado (construtor de reconstituição)
- grep "Optional\|List" src/main/java/br/com/escola/matricula/dominio/repositorio/MatriculaRepositorio.java retorna resultado; grep "springframework\|mybatis\|jakarta" retorna VAZIO
- grep "@Service\|@Component" src/main/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatricula.java retorna VAZIO
- grep "int limite" src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java retorna resultado
- grep "int atual" src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java retorna resultado
- mvn compile -q retorna BUILD SUCCESS
  </acceptance_criteria>

  <done>Aggregate Root Matricula com invariantes encapsuladas, MatriculaRepositorio sem imports de framework, VerificadorElegibilidadeMatricula Java puro, 7 exceções tipadas com campos estruturados — tudo compila, domínio sem framework</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dados vindos da infraestrutura → Aggregate | MatriculaRowMapper constrói objetos de domínio com dados do banco |
| Input do UseCase → Aggregate | Commands carregam dados não validados de contextos externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | Matricula.java — estado mutable via getters | accept | getDisciplinas() retorna List.copyOf — cópia imutável. getStatus() retorna referência mas StatusMatricula é record imutável. Campos finais protegem contra reassignment. |
| T-03-03 | Elevation of Privilege | VerificadorElegibilidadeMatricula | accept | Serviço de domínio puro — não tem acesso a dados além do que o repositório provê. Sem Spring Security, sem sessão. Projeto didático sem autenticação (declarado fora de escopo). |
| T-03-SC | Tampering | mvn compile (sem dependências externas nesta tarefa) | accept | Camada de domínio usa apenas java.util.* e java.time.* — zero dependências externas, zero surface de ataque de supply chain nesta camada. |
</threat_model>

<verification>
Verificações críticas ao final do plano:

1. SUCESSO CRÍTICO — domínio sem framework:
   grep -r "import org.springframework" src/main/java/br/com/escola/matricula/dominio/
   grep -r "import org.mybatis" src/main/java/br/com/escola/matricula/dominio/
   Ambos devem retornar VAZIO.

2. mvn compile -q → BUILD SUCCESS

3. Invariante estrutural:
   grep "int limite" src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java → presente
   grep "int atual" src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java → presente

4. Sealed interface presente:
   grep "sealed interface StatusMatricula" src/main/java/br/com/escola/matricula/dominio/modelo/StatusMatricula.java → presente

5. PeriodoLetivo preservado como (ano, semestre):
   grep "record PeriodoLetivo(int ano, int semestre)" src/main/java/br/com/escola/matricula/dominio/vo/PeriodoLetivo.java → presente

6. Aggregate Root sem Spring:
   grep "@Service\|@Component\|@Repository" src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java → VAZIO
</verification>

<success_criteria>
- 23 arquivos Java criados no pacote dominio.* (vo, modelo, evento, repositorio, servico, excecao)
- Zero imports de org.springframework.*, org.mybatis.*, jakarta.* em qualquer arquivo do pacote dominio/
- PeriodoLetivo mantido como record(int ano, int semestre) — sem alteração do design tático
- Matricula.adicionarDisciplina() encapsula os 3 guards de invariante
- coletarEventos() retorna List.copyOf e limpa lista interna
- LimiteDisciplinasExcedidoException com campos int limite e int atual
- VerificadorElegibilidadeMatricula sem anotações Spring
- MatriculaRepositorio com 4 métodos, zero imports de framework
- mvn compile -q → BUILD SUCCESS
</success_criteria>

<output>
Criar `.planning/phases/03-implementacao/03-02-SUMMARY.md` ao finalizar
</output>
