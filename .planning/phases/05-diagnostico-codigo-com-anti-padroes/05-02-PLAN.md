---
phase: 05-diagnostico-codigo-com-anti-padroes
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - erp-matricula-camadas/pom.xml
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/ErpMatriculaCamadasApplication.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Aluno.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Turma.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/ItemMatricula.java
  - erp-matricula-camadas/src/main/resources/application.yml
autonomous: true
requirements:
  - DIAG-02
  - DIAG-06
  - DID-01

must_haves:
  truths:
    - "erp-matricula-camadas/pom.xml existe e herda do parent local erp-matricula-parent"
    - "Matricula.java no módulo camadas tem apenas campos private, getters e setters — sem adicionarDisciplina() nem cancelar()"
    - "Matricula.java usa String status (não sealed interface) e UUID alunoId (não AlunoId Value Object)"
    - "Todos os models têm comentário // ANTI-PADRAO: Entidade Anêmica (DIAG-02) no cabeçalho"
    - "mvn -pl erp-matricula-camadas compile -q termina com exit 0"
  artifacts:
    - path: "erp-matricula-camadas/pom.xml"
      provides: "POM do módulo camadas herdando do parent local"
      contains: "<artifactId>erp-matricula-parent</artifactId>"
    - path: "erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java"
      provides: "Entidade anêmica — anti-padrão DIAG-02"
      contains: "ANTI-PADRAO: Entidade Anêmica"
    - path: "erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Aluno.java"
      provides: "Entidade anêmica de aluno"
      contains: "isAtivo()"
    - path: "erp-matricula-camadas/src/main/resources/application.yml"
      provides: "Configuração apontando para banco erp_matricula_camadas"
      contains: "erp_matricula_camadas"
  key_links:
    - from: "ErpMatriculaCamadasApplication.java"
      to: "br.com.escola.matricula.repository"
      via: "@MapperScan"
      pattern: "MapperScan.*repository"
---

<objective>
Criar o esqueleto do módulo erp-matricula-camadas: pom.xml, classe main, quatro model classes anêmicas e application.yml. As model classes são o "contrato de dados" que o módulo camadas usa — intencionalmente sem comportamento para demonstrar os anti-padrões DIAG-02 (Entidade Anêmica) e DIAG-06 (Acoplamento ao Banco).

Purpose: Estabelecer os tipos que as camadas superiores (service, repository, controller) vão usar. Comentários inline explicam a ausência de comportamento — o desenvolvedor lê Matricula.java e vê exatamente o que falta em comparação com o módulo DDD.

Output: pom.xml do módulo camadas, ErpMatriculaCamadasApplication.java, Matricula.java, Aluno.java, Turma.java, ItemMatricula.java, application.yml.
</objective>

<execution_context>
@/home/marcelino/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcelino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-CONTEXT.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-01-SUMMARY.md

<interfaces>
<!-- Contraste intencional: modelos DDD vs. modelos anêmicos -->

Do erp-matricula-ddd (Aggregate rico — o oposto do que criar aqui):
  Matricula.java — tem adicionarDisciplina(NomeDisciplina), cancelar(), coletarEventos()
  Matricula.java — usa MatriculaId (VO), AlunoId (VO), StatusMatricula (sealed interface)
  Aluno.java — tem estaAtivo() (semântica de negócio), desativar()
  Turma.java — tem periodoEstaAberto()
  ItemMatricula.java — record imutável com NomeDisciplina (VO)

Módulo camadas deve fazer o OPOSTO:
  Matricula — campos private UUID/String, apenas getters/setters, sem comportamento
  Aluno — boolean ativo com isAtivo()/setAtivo(), sem desativar()
  Turma — campos simples, sem periodoEstaAberto()
  ItemMatricula — classe mutável com String disciplina (não NomeDisciplina VO)

Package base do módulo camadas: br.com.escola.matricula (per D-05)
Package de models: br.com.escola.matricula.model
Package da main class: br.com.escola.matricula
@MapperScan target: br.com.escola.matricula.repository
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar pom.xml e ErpMatriculaCamadasApplication.java do módulo camadas</name>
  <files>
    erp-matricula-camadas/pom.xml (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/ErpMatriculaCamadasApplication.java (NOVO),
    erp-matricula-camadas/src/main/resources/application.yml (NOVO)
  </files>
  <read_first>
    - erp-matricula-ddd/pom.xml — dependências a copiar (spring-boot-starter, web, validation, mybatis 3.0.5, postgresql, flyway-core, flyway-database-postgresql, spring-boot-starter-test)
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/ErpMatriculaApplication.java — padrão da main class a replicar
    - erp-matricula-ddd/src/main/resources/application.yml — configuração a adaptar (mudar datasource URL e type-aliases-package)
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seções "erp-matricula-camadas/pom.xml", "application.yml", "ErpMatriculaCamadasApplication.java"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-01-SUMMARY.md — confirmar que parent POM existe antes de criar o módulo
  </read_first>
  <action>
    Criar estrutura de diretórios:
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/controller/
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/
    erp-matricula-camadas/src/main/resources/db/migration/
    erp-matricula-camadas/src/main/resources/mapper/
    erp-matricula-camadas/src/test/java/br/com/escola/matricula/

    Criar erp-matricula-camadas/pom.xml:
    Parent: groupId=br.com.escola, artifactId=erp-matricula-parent, version=0.1.0-SNAPSHOT, relativePath=../pom.xml
    Coordenadas: groupId=br.com.escola, artifactId=erp-matricula-camadas, version=0.1.0-SNAPSHOT, packaging=jar
    Nome: "ERP Matrícula — Módulo Camadas (arquitetura tradicional — o 'antes' didático)"
    Dependências idênticas ao erp-matricula-ddd/pom.xml: spring-boot-starter, spring-boot-starter-web, spring-boot-starter-validation, mybatis-spring-boot-starter 3.0.5 (versão explícita), postgresql, flyway-core, flyway-database-postgresql, spring-boot-starter-test scope test.
    Plugin: spring-boot-maven-plugin sem skip (este módulo é JAR executável).
    Comentário XML de cabeçalho: "Módulo erp-matricula-camadas — arquitetura tradicional Controller->Service->Repository. Os 6 anti-padrões coexistem neste módulo de forma integrada e realista. Este é o 'antes' — o módulo erp-matricula-ddd é o 'depois'."

    Criar ErpMatriculaCamadasApplication.java em br.com.escola.matricula:
    Anotações: @SpringBootApplication, @MapperScan("br.com.escola.matricula.repository")
    Classe: ErpMatriculaCamadasApplication com main(String[] args) chamando SpringApplication.run(ErpMatriculaCamadasApplication.class, args).
    Comentário Javadoc: "Módulo erp-matricula-camadas — arquitetura em camadas (Controller->Service->Repository). Porta: 8081 (via docker-compose). Banco: erp_matricula_camadas."

    Criar erp-matricula-camadas/src/main/resources/application.yml:
    datasource.url: jdbc:postgresql://localhost:5432/erp_matricula_camadas
    datasource.username: matricula / password: matricula / driver-class-name: org.postgresql.Driver
    flyway.enabled: true / locations: classpath:db/migration
    mybatis.mapper-locations: classpath:mapper/**/*.xml
    mybatis.type-aliases-package: br.com.escola.matricula.repository (não infraestrutura.persistencia — no módulo camadas não há separação)
    mybatis.configuration.map-underscore-to-camel-case: false (igual ao módulo DDD — per padrão estabelecido)
    mybatis.configuration.lazy-loading-enabled: false
    logging.level.br.com.escola.matricula: DEBUG / logging.level.org.mybatis: DEBUG
    server.port: 8080 (porta interna; docker-compose mapeia 8081 no host)
    Comentário no topo: "Módulo camadas — banco separado erp_matricula_camadas evita conflito com Flyway do módulo DDD (ver RESEARCH.md Pitfall 1)"
  </action>
  <verify>
    <automated>test -f /home/marcelino/Área\ de\ trabalho/erp-matricula/erp-matricula-camadas/pom.xml && grep -c "erp-matricula-parent" /home/marcelino/Área\ de\ trabalho/erp-matricula/erp-matricula-camadas/pom.xml && echo "pom.xml OK"</automated>
  </verify>
  <acceptance_criteria>
    - erp-matricula-camadas/pom.xml existe e contém: &lt;artifactId&gt;erp-matricula-parent&lt;/artifactId&gt; no bloco parent
    - erp-matricula-camadas/pom.xml contém: &lt;artifactId&gt;erp-matricula-camadas&lt;/artifactId&gt;
    - erp-matricula-camadas/pom.xml contém: mybatis-spring-boot-starter com version 3.0.5 (versão explícita)
    - ErpMatriculaCamadasApplication.java contém: @MapperScan("br.com.escola.matricula.repository")
    - ErpMatriculaCamadasApplication.java está no package br.com.escola.matricula
    - application.yml contém: erp_matricula_camadas na URL do datasource
    - application.yml contém: map-underscore-to-camel-case: false
  </acceptance_criteria>
  <done>Módulo camadas tem pom.xml, main class e application.yml — estrutura pronta para receber as classes Java.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar as quatro model classes anêmicas (DIAG-02 + DIAG-06)</name>
  <files>
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Aluno.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Turma.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/ItemMatricula.java (NOVO)
  </files>
  <read_first>
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Matricula.java — o oposto: lê todos os métodos de comportamento que a versão camadas NÃO deve ter
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Aluno.java — lê estaAtivo() e desativar() para registrar o contraste no comentário
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Turma.java — lê periodoEstaAberto() para registrar o contraste
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seções "model/Matricula.java", "model/Aluno.java", "model/ItemMatricula.java", "model/Turma.java"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md — seção "DIAG-02: Entidade Anêmica" e "DIAG-06: Acoplamento ao Banco"
  </read_first>
  <action>
    Criar model/Matricula.java no package br.com.escola.matricula.model:
    Campos: private UUID id; private UUID alunoId; private UUID turmaId; private String periodoInicio; private String periodoFim; private String status;
    Tipos intencionais: UUID (não MatriculaId VO), String status (não StatusMatricula sealed interface), String periodo (não PeriodoLetivo VO).
    Métodos: apenas getters públicos (getId, getAlunoId, getTurmaId, getPeriodoInicio, getPeriodoFim, getStatus) e setters públicos correspondentes. setStatus(String status) sem validação de transição.
    Sem construtor com regras, sem adicionarDisciplina(), sem cancelar(), sem coletarEventos().
    Comentário de cabeçalho (3 linhas acima da declaração de classe): "ANTI-PADRAO: Entidade Anêmica (DIAG-02) / Esta classe não tem comportamento. Ela é um container de dados — um mapeamento 1:1 com a tabela matriculas. / Quem protege a invariante 'máximo 6 disciplinas'? O MatriculaServiceImpl. / Quem protege 'matrícula cancelada não aceita disciplinas'? O MatriculaServiceImpl. / Contraste: erp-matricula-ddd/.../dominio/modelo/Matricula.java tem adicionarDisciplina() e cancelar()."
    Comentário inline nos campos UUID/String: "// igual à coluna aluno_id — ANTI-PADRAO: Acoplamento ao Banco (DIAG-06)" e "// String livre: 'ATIVA', 'CANCELADA' — sem tipo seguro; no DDD é StatusMatricula (sealed interface)".

    Criar model/Aluno.java no package br.com.escola.matricula.model:
    Campos: private UUID id; private String cpf; private String nome; private boolean ativo;
    Métodos: getters e setters para todos os campos. isAtivo() retorna ativo; setAtivo(boolean ativo).
    Sem estaAtivo() de semântica de negócio, sem desativar().
    Comentário: "ANTI-PADRAO: Entidade Anêmica (DIAG-02) / Aluno como data class sem comportamento. / Contraste: erp-matricula-ddd/.../dominio/modelo/Aluno.java tem estaAtivo() e desativar() com semântica de domínio. / cpf é String simples — no módulo DDD é Cpf (Value Object com validação de formato)."

    Criar model/Turma.java no package br.com.escola.matricula.model:
    Campos: private UUID id; private String codigo; private String periodoInicio; private String periodoFim;
    Métodos: apenas getters e setters.
    Sem periodoEstaAberto().
    Comentário: "ANTI-PADRAO: Entidade Anêmica (DIAG-02) / Sem periodoEstaAberto() — quem verifica se o período está aberto? O MatriculaServiceImpl. / Contraste: erp-matricula-ddd/.../dominio/modelo/Turma.java tem periodoEstaAberto()."

    Criar model/ItemMatricula.java no package br.com.escola.matricula.model:
    Campos: private UUID id; private UUID matriculaId; private String disciplina; private java.time.LocalDateTime adicionadaEm;
    Métodos: apenas getters e setters.
    Comentários: "ANTI-PADRAO: Entidade Anêmica (DIAG-02) + Acoplamento ao Banco (DIAG-06) / disciplina é String simples — no módulo DDD é NomeDisciplina (Value Object com validação de comprimento mínimo). / adicionadaEm espelha a coluna adicionada_em da tabela itens_matricula."

    Compilar o módulo após criar as classes:
    Execute: mvn -pl erp-matricula-camadas compile -q
  </action>
  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && mvn -pl erp-matricula-camadas compile -q 2>&1 | tail -5 && echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - mvn -pl erp-matricula-camadas compile -q termina com exit code 0
    - Matricula.java contém: private String status; (não enum nem sealed interface)
    - Matricula.java contém: ANTI-PADRAO: Entidade Anêmica (DIAG-02)
    - Matricula.java contém: ANTI-PADRAO: Acoplamento ao Banco (DIAG-06) em comentário inline
    - Matricula.java NÃO contém: adicionarDisciplina ou cancelar ou coletarEventos
    - Aluno.java contém: isAtivo() (não estaAtivo())
    - Aluno.java contém: ANTI-PADRAO: Entidade Anêmica (DIAG-02)
    - Turma.java contém: ANTI-PADRAO: Entidade Anêmica (DIAG-02)
    - Turma.java NÃO contém: periodoEstaAberto
    - ItemMatricula.java contém: private String disciplina; (não NomeDisciplina)
    - Todos os 4 arquivos estão no package br.com.escola.matricula.model
  </acceptance_criteria>
  <done>Quatro model classes anêmicas criadas e compilando — DIAG-02 e DIAG-06 evidentes no código com comentários explicativos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova trust boundary neste plano | Código de modelo puro; sem I/O externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | Campos String mutáveis em model classes | accept | Módulo didático — setters abertos são o ponto pedagógico. Em produção, usar Value Objects (demonstrado no módulo DDD). |
| T-05-SC | Tampering | Sem novos pacotes neste plano | accept | Todos os pacotes herdados do parent POM já auditados no RESEARCH.md |
</threat_model>

<verification>
Compilação do módulo completo:
  mvn -pl erp-matricula-camadas compile -q deve sair com exit 0

Verificação de conteúdo dos anti-padrões:
  grep -l "ANTI-PADRAO: Entidade Anêmica" erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/*.java deve listar 4 arquivos
</verification>

<success_criteria>
- erp-matricula-camadas/pom.xml herda de erp-matricula-parent, tem mybatis 3.0.5 explícito
- ErpMatriculaCamadasApplication.java tem @MapperScan("br.com.escola.matricula.repository")
- application.yml aponta para erp_matricula_camadas
- Quatro model classes no package br.com.escola.matricula.model compilando sem erros
- Matricula.java tem String status, UUID alunoId, sem comportamento — DIAG-02 + DIAG-06 comentados
- mvn -pl erp-matricula-camadas compile -q sai com 0
</success_criteria>

<output>
Criar `.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-02-SUMMARY.md` quando concluído.
</output>
