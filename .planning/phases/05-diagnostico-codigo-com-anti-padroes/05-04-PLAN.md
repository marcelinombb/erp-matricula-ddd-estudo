---
phase: 05-diagnostico-codigo-com-anti-padroes
plan: 04
type: execute
wave: 4
depends_on:
  - 05-02
  - 05-03
files_modified:
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaService.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/DisciplinaServiceImpl.java
autonomous: true
requirements:
  - DIAG-01
  - DIAG-03
  - DIAG-04
  - DIAG-06
  - DID-01

must_haves:
  truths:
    - "MatriculaServiceImpl.java tem 200+ linhas com todos os métodos de matrícula, adicionarDisciplina, cancelar, buscarPorAluno e listarTodos no mesmo arquivo"
    - "O método matricular() contém a validação aluno.isAtivo() com comentário ANTI-PADRAO: Service Anêmico (DIAG-01)"
    - "DisciplinaServiceImpl.java contém a mesma validação aluno.isAtivo() com comentário ANTI-PADRAO: Duplicação de Regras (DIAG-04)"
    - "adicionarDisciplina() chama itemMatriculaRepository.countByMatriculaId() com comentário ANTI-PADRAO: Acoplamento ao Banco (DIAG-06)"
    - "mvn -pl erp-matricula-camadas compile -q termina com exit 0"
  artifacts:
    - path: "erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java"
      provides: "Service Anêmico (DIAG-01) + Service Deus (DIAG-03) + parte de DIAG-04 + DIAG-06"
      min_lines: 200
      contains: "ANTI-PADRAO: Service Anêmico"
    - path: "erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/DisciplinaServiceImpl.java"
      provides: "Duplicação de Regras (DIAG-04)"
      contains: "ANTI-PADRAO: Duplicação de Regras"
    - path: "erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaService.java"
      provides: "Interface de serviço (contraste com módulo DDD que não tem interface de UseCase)"
      contains: "interface MatriculaService"
  key_links:
    - from: "MatriculaServiceImpl.java"
      to: "AlunoRepository.java"
      via: "findById — regra de negócio no Service (DIAG-01)"
      pattern: "alunoRepository.findById"
    - from: "DisciplinaServiceImpl.java"
      to: "AlunoRepository.java"
      via: "findById — duplicação da mesma validação (DIAG-04)"
      pattern: "alunoRepository.findById"
    - from: "MatriculaServiceImpl.adicionarDisciplina"
      to: "ItemMatriculaRepository.countByMatriculaId"
      via: "COUNT(*) em vez de invariante no objeto (DIAG-06)"
      pattern: "countByMatriculaId"
---

<objective>
Criar a camada de serviço do módulo erp-matricula-camadas com três anti-padrões integrados: MatriculaServiceImpl demonstra Service Anêmico (DIAG-01) + Service Deus (DIAG-03); DisciplinaServiceImpl demonstra Duplicação de Regras (DIAG-04); ambos demonstram Acoplamento ao Banco (DIAG-06) via countByMatriculaId.

Purpose: O coração do módulo "antes". O desenvolvedor lê MatriculaServiceImpl e sente o problema: 200+ linhas, regras de negócio em um único lugar, o mesmo if (!aluno.isAtivo()) repetido em dois services. Os comentários ANTI-PADRAO: guiam a leitura sem soar prescritivos.

Output: MatriculaService interface, MatriculaServiceImpl (200+ linhas, 5-6 métodos, quatro anti-padrões visíveis), DisciplinaServiceImpl (duplica a validação de aluno ativo).
</objective>

<execution_context>
@/home/marcelino/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcelino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-CONTEXT.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-02-SUMMARY.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-03-SUMMARY.md

<interfaces>
<!-- Tipos disponíveis dos planos anteriores (wave 2) -->

Do Plano 02 (model classes):
  br.com.escola.matricula.model.Matricula — campos: UUID id, UUID alunoId, UUID turmaId, String periodoInicio, String periodoFim, String status
  br.com.escola.matricula.model.Aluno — campos: UUID id, String cpf, String nome, boolean ativo; método: boolean isAtivo()
  br.com.escola.matricula.model.Turma — campos: UUID id, String codigo, String periodoInicio, String periodoFim
  br.com.escola.matricula.model.ItemMatricula — campos: UUID id, UUID matriculaId, String disciplina, LocalDateTime adicionadaEm

Do Plano 03 (repositórios):
  br.com.escola.matricula.repository.MatriculaRepository — findById(UUID), findByAlunoId(UUID), insert(Matricula), updateStatus(UUID, String), countDisciplinas(UUID)
  br.com.escola.matricula.repository.AlunoRepository — findById(UUID)
  br.com.escola.matricula.repository.TurmaRepository — findById(UUID)
  br.com.escola.matricula.repository.ItemMatriculaRepository — findByMatriculaId(UUID), insert(ItemMatricula), countByMatriculaId(UUID)

Módulo DDD (contraste — o oposto do que criar aqui):
  MatricularAlunoUseCase.java — 109 linhas, delega ao domínio, não contém regras de negócio
  Matricula.java (DDD) — adicionarDisciplina() protege a invariante internamente
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar MatriculaService interface e MatriculaServiceImpl (Service Anêmico + Service Deus)</name>
  <files>
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaService.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java (NOVO)
  </files>
  <read_first>
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/aplicacao/MatricularAlunoUseCase.java — leia integralmente; é o oposto do que criar (UseCase delega, Service anêmico acumula)
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java — campos disponíveis para criar objeto
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java — assinaturas disponíveis
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/ItemMatriculaRepository.java — countByMatriculaId disponível
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md — seções "DIAG-01: Service Anêmico", "DIAG-03: Service Deus", "DIAG-06: Acoplamento ao Banco"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seções "service/MatriculaService.java" e "service/MatriculaServiceImpl.java"
  </read_first>
  <action>
    Criar MatriculaService.java em br.com.escola.matricula.service (interface):
    Métodos: UUID matricular(UUID alunoId, UUID turmaId, String periodoInicio, String periodoFim); void adicionarDisciplina(UUID matriculaId, String nomeDisciplina); void cancelar(UUID matriculaId); List buscarPorAluno(UUID alunoId);
    Comentário: "Interface de serviço — no módulo DDD os UseCases não têm interfaces separadas (MatricularAlunoUseCase é diretamente @Service). No módulo camadas, a interface MatriculaService é convencional no padrão Controller->Service->Repository."

    Criar MatriculaServiceImpl.java em br.com.escola.matricula.service:
    Anotações: @Service, @Transactional
    Implementa: MatriculaService
    Campos injetados via construtor: MatriculaRepository matriculaRepository, AlunoRepository alunoRepository, TurmaRepository turmaRepository, ItemMatriculaRepository itemMatriculaRepository.

    Comentário de cabeçalho acima da declaração de classe (bloco multilinha):
    "ANTI-PADRAO: Service Deus (DIAG-03) — Esta classe tem 200+ linhas e cresce sem parar. Toda nova regra de matrícula vem parar aqui. Um Service Deus viola o Princípio da Responsabilidade Única — ele sabe de tudo, faz de tudo, muda por qualquer razão. Relação com DIAG-01: o service está inchado exatamente porque o modelo não tem comportamento — como Matricula.java é anêmica, toda lógica migrou para cá."

    Método matricular(UUID alunoId, UUID turmaId, String periodoInicio, String periodoFim) retorna UUID:
    1. Buscar aluno via alunoRepository.findById(alunoId).orElseThrow(() -> new RuntimeException("Aluno não encontrado: " + alunoId))
    2. Comentário: "ANTI-PADRAO: Service Anêmico (DIAG-01) — Regra de negócio (aluno inativo não matricula) vivendo no Service, não na entidade Aluno. No módulo DDD: aluno.estaAtivo() encapsula a mesma verificação no próprio objeto."
    3. if (!aluno.isAtivo()) throw new RuntimeException("Aluno inativo não pode ser matriculado")
    4. Buscar turma via turmaRepository.findById(turmaId).orElseThrow(...)
    5. Comentário: "ANTI-PADRAO: Service Anêmico (DIAG-01) — Verificação de período aberto no Service. No módulo DDD: turma.periodoEstaAberto() encapsula essa regra."
    6. Verificar se período está no futuro: if (periodoInicio == null || periodoInicio.isBlank()) throw new RuntimeException("Período de início é obrigatório")
    7. Criar Matricula, setar id=UUID.randomUUID(), alunoId, turmaId, periodoInicio, periodoFim, status="ATIVA"
    8. matriculaRepository.insert(matricula)
    9. return matricula.getId()
    Linhas aproximadas do método: 30-40 linhas com comentários.

    Método adicionarDisciplina(UUID matriculaId, String nomeDisciplina):
    1. Buscar matricula via findById.orElseThrow(...)
    2. Verificar status: if (!"ATIVA".equals(matricula.getStatus())) throw new RuntimeException("Matrícula não está ativa")
    3. Comentário: "ANTI-PADRAO: Service Anêmico (DIAG-01) — verificação de status no Service. No módulo DDD: a invariante 'cancelada não aceita disciplinas' é protegida dentro de Matricula.adicionarDisciplina()."
    4. Buscar aluno via alunoRepository.findById(matricula.getAlunoId()).orElseThrow(...)
    5. Comentário: "ANTI-PADRAO: Duplicação de Regras (DIAG-04) — Esta validação aluno.isAtivo() já existe em matricular() acima. A mesma validação também existe em DisciplinaServiceImpl.adicionarDisciplina(). Com o tempo, as implementações divergem."
    6. if (!aluno.isAtivo()) throw new RuntimeException("Aluno inativo não pode adicionar disciplinas")
    7. Comentário: "ANTI-PADRAO: Acoplamento ao Banco (DIAG-06) — A regra 'máximo 6 disciplinas' não existe no modelo de domínio. Ela existe como uma query SQL. Se você ler Matricula.java, não encontrará essa regra — para descobri-la, precisa ler este Service e rastrear até o repositório."
    8. int qtd = itemMatriculaRepository.countByMatriculaId(matriculaId); if (qtd >= 6) throw new RuntimeException("Limite de 6 disciplinas atingido")
    9. Criar ItemMatricula, setar id=UUID.randomUUID(), matriculaId, disciplina=nomeDisciplina, adicionadaEm=LocalDateTime.now()
    10. itemMatriculaRepository.insert(item)
    Linhas aproximadas: 40-50 linhas com comentários.

    Método cancelar(UUID matriculaId):
    1. Buscar matricula. Verificar se está ATIVA. Comentário DIAG-01 na verificação.
    2. matriculaRepository.updateStatus(matriculaId, "CANCELADA")
    Linhas: 20-25 linhas.

    Método buscarPorAluno(UUID alunoId) retorna List:
    1. return matriculaRepository.findByAlunoId(alunoId)
    Linhas: 10 linhas.

    Método verificarElegibilidade(UUID alunoId) — método private (usado por matricular e adicionarDisciplina para mostrar que PODERIA ser extraído mas não é):
    Comentário: "ANTI-PADRAO: Service Deus (DIAG-03) — Este método existe mas está duplicado na prática: tanto matricular() quanto adicionarDisciplina() revalidam as mesmas condições sem usar este método consistentemente. Sem um modelo de domínio rico, a disciplina de delegar para métodos privados é frágil."
    1. Buscar aluno, verificar ativo. Retorna void, lança RuntimeException.
    Linhas: 15 linhas.

    Alvo total da classe: 200-230 linhas incluindo comentários e imports.
  </action>
  <verify>
    <automated>wc -l /home/marcelino/Área\ de\ trabalho/erp-matricula/erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java && echo "Linhas acima (deve ser 200+)"</automated>
  </verify>
  <acceptance_criteria>
    - MatriculaServiceImpl.java tem 200+ linhas (wc -l retorna >= 200)
    - MatriculaServiceImpl.java contém: ANTI-PADRAO: Service Anêmico (DIAG-01)
    - MatriculaServiceImpl.java contém: ANTI-PADRAO: Service Deus (DIAG-03)
    - MatriculaServiceImpl.java contém: ANTI-PADRAO: Duplicação de Regras (DIAG-04)
    - MatriculaServiceImpl.java contém: ANTI-PADRAO: Acoplamento ao Banco (DIAG-06)
    - MatriculaServiceImpl.java contém: itemMatriculaRepository.countByMatriculaId
    - MatriculaServiceImpl.java contém: if (!aluno.isAtivo())
    - MatriculaService.java é interface (não classe): contém "interface MatriculaService"
    - mvn -pl erp-matricula-camadas compile -q termina com exit code 0
  </acceptance_criteria>
  <done>MatriculaServiceImpl criada com 200+ linhas demonstrando quatro anti-padrões com comentários descritivos.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar DisciplinaServiceImpl demonstrando Duplicação de Regras (DIAG-04)</name>
  <files>
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/DisciplinaServiceImpl.java (NOVO)
  </files>
  <read_first>
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java — leia o método adicionarDisciplina() completo; DisciplinaServiceImpl deve duplicar a validação aluno.isAtivo() que está lá
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/aplicacao/AdicionarDisciplinaUseCase.java — o oposto: UseCase delega ao aggregate, não revalida aluno
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/AlunoRepository.java — findById disponível
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java — findById disponível
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seção "service/DisciplinaServiceImpl.java"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md — seção "DIAG-04: Duplicação de Regras"
  </read_first>
  <action>
    Criar DisciplinaServiceImpl.java em br.com.escola.matricula.service:
    Anotações: @Service, @Transactional.
    Campos injetados via construtor: MatriculaRepository matriculaRepository, AlunoRepository alunoRepository, ItemMatriculaRepository itemMatriculaRepository.

    Comentário de cabeçalho acima da classe (multilinha):
    "ANTI-PADRAO: Duplicação de Regras (DIAG-04) — Esta classe existe separada de MatriculaServiceImpl mas duplica as mesmas validações. A validação 'aluno inativo não pode adicionar disciplinas' já existe em MatriculaServiceImpl.adicionarDisciplina(). Daqui a 6 meses, alguém adiciona uma exceção em um lugar e esquece do outro — as regras divergem silenciosamente. Causa raiz: sem um modelo de domínio rico (Aluno.podeMatricular(), Matricula.adicionarDisciplina()), as regras vivem no Service e são copiadas quando novos Services surgem."

    Método adicionarDisciplinaExtra(UUID matriculaId, String nomeDisciplina) retorna void:
    1. Buscar matricula via matriculaRepository.findById.orElseThrow(...)
    2. Buscar aluno via alunoRepository.findById(matricula.getAlunoId()).orElseThrow(...)
    3. Comentário inline: "ANTI-PADRAO: Duplicação de Regras (DIAG-04) — Cópia exata da validação em MatriculaServiceImpl.adicionarDisciplina() linha N. Em projetos reais, esta cópia foi feita quando DisciplinaServiceImpl foi criada para uma feature nova. A duplicação parecia razoável. Seis meses depois, MatriculaServiceImpl adicionou exceção para 'alunos em período de teste' — DisciplinaServiceImpl não foi atualizado."
    4. if (!aluno.isAtivo()) throw new RuntimeException("Aluno inativo")
    5. int qtd = itemMatriculaRepository.countByMatriculaId(matriculaId); if (qtd >= 6) throw new RuntimeException("Limite atingido")
    6. Criar ItemMatricula e inserir.

    Método verificarStatusMatricula(UUID matriculaId) retorna boolean:
    Comentário: "ANTI-PADRAO: Duplicação de Regras (DIAG-04) — Verificação duplicada. MatriculaServiceImpl.cancelar() faz a mesma verificação inline. Este método foi criado aqui para 'reutilização', mas MatriculaServiceImpl não o usa — ele está duplicado de outra forma."
    Implementação: return "ATIVA".equals(matriculaRepository.findById(matriculaId).map(m -> m.getStatus()).orElse(""))

    Alvo: 60-80 linhas incluindo comentários.
  </action>
  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && mvn -pl erp-matricula-camadas compile -q 2>&1 | tail -5 && echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - DisciplinaServiceImpl.java contém: ANTI-PADRAO: Duplicação de Regras (DIAG-04)
    - DisciplinaServiceImpl.java contém: if (!aluno.isAtivo()) — mesma validação de MatriculaServiceImpl
    - DisciplinaServiceImpl.java contém: itemMatriculaRepository.countByMatriculaId
    - DisciplinaServiceImpl.java tem @Service e @Transactional
    - mvn -pl erp-matricula-camadas compile -q termina com exit code 0
    - Três services existem: MatriculaService.java (interface), MatriculaServiceImpl.java, DisciplinaServiceImpl.java
  </acceptance_criteria>
  <done>DisciplinaServiceImpl criada com duplicação explícita de DIAG-04 comentada, módulo camadas compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Repository | RuntimeException propagada sem tipagem — padrão anêmico intencional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | RuntimeException com mensagens verbosas ("Aluno não encontrado: " + id) | accept | Módulo didático. O ponto pedagógico é mostrar erros genéricos vs. exceções tipadas do módulo DDD. Em produção, exceções tipadas são preferíveis. |
| T-05-SC | Tampering | Sem novos pacotes neste plano | accept | Todos os pacotes herdados do parent POM já auditados |
</threat_model>

<verification>
Verificação de completude dos services:
  ls erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/*.java deve listar 3 arquivos

Verificação dos quatro anti-padrões no Service principal:
  grep -c "ANTI-PADRAO:" erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java deve retornar >= 4

Verificação da duplicação:
  grep -c "isAtivo()" erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java deve retornar >= 2
  grep -c "isAtivo()" erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/DisciplinaServiceImpl.java deve retornar >= 1
</verification>

<success_criteria>
- MatriculaServiceImpl.java tem 200+ linhas com comentários DIAG-01, DIAG-03, DIAG-04, DIAG-06
- DisciplinaServiceImpl.java duplica validação aluno.isAtivo() com comentário DIAG-04 explicando o problema
- MatriculaService.java é interface com 4 métodos declarados
- mvn -pl erp-matricula-camadas compile -q sai com 0
</success_criteria>

<output>
Criar `.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-04-SUMMARY.md` quando concluído.
</output>
