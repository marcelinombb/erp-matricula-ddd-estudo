---
phase: 05-diagnostico-codigo-com-anti-padroes
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/AlunoRepository.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/TurmaRepository.java
  - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/ItemMatriculaRepository.java
  - erp-matricula-camadas/src/main/resources/mapper/MatriculaMapper.xml
  - erp-matricula-camadas/src/main/resources/mapper/AlunoMapper.xml
  - erp-matricula-camadas/src/main/resources/mapper/ItemMatriculaMapper.xml
  - erp-matricula-camadas/src/main/resources/db/migration/V1__schema.sql
  - erp-matricula-camadas/src/main/resources/db/migration/V2__seeds.sql
  - erp-matricula-camadas/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql
autonomous: true
requirements:
  - DIAG-06
  - DID-01

must_haves:
  truths:
    - "MatriculaRepository.java tem método countDisciplinas(UUID matriculaId) — expõe o Acoplamento ao Banco (DIAG-06)"
    - "MatriculaMapper.xml existe com resultMap mapeando colunas diretamente (aluno_id, turma_id, periodo_inicio, periodo_fim, status)"
    - "As três migrations V1-V3 são cópias idênticas das do módulo DDD — mesmo schema, banco separado"
    - "Todos os @Mapper são diretos (sem separação domain interface vs. infrastructure — contraste com DDD)"
  artifacts:
    - path: "erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java"
      provides: "Mapper MyBatis para Matricula com countDisciplinas"
      contains: "countDisciplinas"
    - path: "erp-matricula-camadas/src/main/resources/mapper/MatriculaMapper.xml"
      provides: "XML com resultMap e queries SQL"
      contains: "resultMap"
    - path: "erp-matricula-camadas/src/main/resources/db/migration/V1__schema.sql"
      provides: "Schema idêntico ao módulo DDD"
      contains: "CREATE TABLE matriculas"
  key_links:
    - from: "MatriculaRepository.java"
      to: "MatriculaMapper.xml"
      via: "@Mapper + namespace"
      pattern: "br.com.escola.matricula.repository.MatriculaRepository"
    - from: "MatriculaServiceImpl.java (Plano 04)"
      to: "MatriculaRepository.java"
      via: "countDisciplinas — DIAG-06"
      pattern: "countDisciplinas"
---

<objective>
Criar a camada de repositório do módulo erp-matricula-camadas: quatro interfaces @Mapper MyBatis com XMLs de mapeamento e cópias das três migrations Flyway. A diferença pedagógica chave em relação ao módulo DDD: no módulo camadas o @Mapper IS o repositório (sem separação de domain interface vs. infrastructure implementation). O método countDisciplinas em MatriculaRepository expõe explicitamente o anti-padrão DIAG-06 (Acoplamento ao Banco).

Purpose: Estabelecer a persistência que o Service (Plano 04) vai usar. O repositório precisa existir antes do Service compilar. A cópia idêntica das migrations reforça o ponto pedagógico: o schema de banco é o mesmo — o que muda é o código Java que o acessa.

Output: Quatro @Mapper interfaces, três XMLs de mapeamento, três arquivos de migration Flyway copiados.
</objective>

<execution_context>
@/home/marcelino/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcelino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-CONTEXT.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-01-SUMMARY.md

<interfaces>
<!-- Estrutura de persistência do módulo DDD para contrastar -->

erp-matricula-ddd: dominio/repositorio/MatriculaRepositorio.java — interface pura sem @Mapper
erp-matricula-ddd: infraestrutura/persistencia/MatriculaMapper.java — @Mapper separado da interface de domínio
erp-matricula-ddd: infraestrutura/persistencia/MatriculaRepositorioMyBatis.java — implementa MatriculaRepositorio usando MatriculaMapper

Módulo camadas (sem separação):
  MatriculaRepository.java — @Mapper diretamente (sem interface de domínio separada)
  AlunoRepository.java — @Mapper direto
  TurmaRepository.java — @Mapper direto
  ItemMatriculaRepository.java — @Mapper direto com countByMatriculaId

Types importados (definidos no Plano 02, wave 2 — paralelo, disponíveis quando Plano 03 termina):
  br.com.escola.matricula.model.Matricula
  br.com.escola.matricula.model.Aluno
  br.com.escola.matricula.model.Turma
  br.com.escola.matricula.model.ItemMatricula

Nota: Planos 02 e 03 correm em wave 2 em paralelo. Plano 03 não depende de Plano 02 compilar —
apenas precisa que os arquivos model/*.java existam (criados pelo executor de Plano 02).
Se os arquivos model ainda não existem ao iniciar Plano 03, criar stubs de model suficientes
para compilação e aguardar Plano 02 finalizar antes de commit final.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar as quatro interfaces @Mapper e copiar as migrations Flyway</name>
  <files>
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/AlunoRepository.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/TurmaRepository.java (NOVO),
    erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/ItemMatriculaRepository.java (NOVO),
    erp-matricula-camadas/src/main/resources/db/migration/V1__schema.sql (NOVO — cópia),
    erp-matricula-camadas/src/main/resources/db/migration/V2__seeds.sql (NOVO — cópia),
    erp-matricula-camadas/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql (NOVO — cópia)
  </files>
  <read_first>
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaMapper.java — assinaturas de método a adaptar para a interface @Mapper direta do módulo camadas
    - erp-matricula-ddd/src/main/resources/db/migration/V1__schema.sql — copiar integralmente
    - erp-matricula-ddd/src/main/resources/db/migration/V2__seeds.sql — copiar integralmente
    - erp-matricula-ddd/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql — copiar integralmente
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seção "repository/MatriculaRepository.java"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md — seção "DIAG-06: Acoplamento ao Banco"
  </read_first>
  <action>
    Criar MatriculaRepository.java em br.com.escola.matricula.repository:
    Anotação: @Mapper
    Import: org.apache.ibatis.annotations.Mapper, org.apache.ibatis.annotations.Param, java.util.List, java.util.Optional, java.util.UUID, br.com.escola.matricula.model.Matricula.
    Métodos:
      Optional findById(@Param("id") UUID id);
      List findByAlunoId(@Param("alunoId") UUID alunoId);
      void insert(Matricula matricula);
      void updateStatus(@Param("id") UUID id, @Param("status") String status);
      int countDisciplinas(@Param("matriculaId") UUID matriculaId); — com comentário: "ANTI-PADRAO: Acoplamento ao Banco (DIAG-06) — a regra 'máximo 6 disciplinas' não existe no modelo de domínio. Ela existe como uma query SQL. Se você ler Matricula.java, não encontra essa regra."
    Comentário de cabeçalho: "Repositório MyBatis direto — no módulo DDD esta interface seria o MatriculaRepositorio (domínio puro) separado do MatriculaMapper (infraestrutura). Aqui são um só — sem separação camadas de domínio / infraestrutura."

    Criar AlunoRepository.java em br.com.escola.matricula.repository:
    Anotação: @Mapper
    Métodos: Optional findById(@Param("id") UUID id); List findAll();

    Criar TurmaRepository.java em br.com.escola.matricula.repository:
    Anotação: @Mapper
    Métodos: Optional findById(@Param("id") UUID id);

    Criar ItemMatriculaRepository.java em br.com.escola.matricula.repository:
    Anotação: @Mapper
    Métodos:
      List findByMatriculaId(@Param("matriculaId") UUID matriculaId);
      void insert(ItemMatricula item);
      int countByMatriculaId(@Param("matriculaId") UUID matriculaId); — com comentário: "ANTI-PADRAO: Acoplamento ao Banco (DIAG-06) — contagem de disciplinas via SQL no repositório, não via invariante no objeto Matricula."

    Copiar migrations Flyway (schema idêntico, banco separado):
    Copiar erp-matricula-ddd/src/main/resources/db/migration/V1__schema.sql para erp-matricula-camadas/src/main/resources/db/migration/V1__schema.sql sem modificação.
    Copiar V2__seeds.sql sem modificação.
    Copiar V3__adicionar_adicionada_em.sql sem modificação.
    Adicionar comentário no topo de cada arquivo copiado: "-- Migrations idênticas ao módulo erp-matricula-ddd aplicadas no banco erp_matricula_camadas. -- O schema de banco é o mesmo — o que muda é o código Java que o acessa."
  </action>
  <verify>
    <automated>ls /home/marcelino/Área\ de\ trabalho/erp-matricula/erp-matricula-camadas/src/main/resources/db/migration/ && grep -c "countDisciplinas" /home/marcelino/Área\ de\ trabalho/erp-matricula/erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - MatriculaRepository.java contém: @Mapper
    - MatriculaRepository.java contém: countDisciplinas(@Param("matriculaId") UUID matriculaId)
    - MatriculaRepository.java contém: ANTI-PADRAO: Acoplamento ao Banco (DIAG-06)
    - ItemMatriculaRepository.java contém: countByMatriculaId
    - Todos os quatro @Mapper estão no package br.com.escola.matricula.repository
    - V1__schema.sql existe em erp-matricula-camadas/src/main/resources/db/migration/
    - V2__seeds.sql existe em erp-matricula-camadas/src/main/resources/db/migration/
    - V3__adicionar_adicionada_em.sql existe em erp-matricula-camadas/src/main/resources/db/migration/
    - grep "CREATE TABLE matriculas" erp-matricula-camadas/src/main/resources/db/migration/V1__schema.sql retorna match (schema idêntico)
  </acceptance_criteria>
  <done>Quatro @Mapper interfaces criadas com countDisciplinas expondo DIAG-06, migrations copiadas.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar os XMLs de mapeamento MyBatis</name>
  <files>
    erp-matricula-camadas/src/main/resources/mapper/MatriculaMapper.xml (NOVO),
    erp-matricula-camadas/src/main/resources/mapper/AlunoMapper.xml (NOVO),
    erp-matricula-camadas/src/main/resources/mapper/ItemMatriculaMapper.xml (NOVO)
  </files>
  <read_first>
    - erp-matricula-ddd/src/main/resources/mapper/MatriculaMapper.xml — estrutura de resultMap e queries a adaptar
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/MatriculaRepository.java — assinaturas dos métodos que o XML precisa implementar
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java — campos que o resultMap precisa mapear
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seção "Estilo MyBatis: @Mapper + XML"
  </read_first>
  <action>
    Criar MatriculaMapper.xml em erp-matricula-camadas/src/main/resources/mapper/:
    namespace: br.com.escola.matricula.repository.MatriculaRepository
    resultMap id="MatriculaResultMap" type="br.com.escola.matricula.model.Matricula":
      id property="id" column="id" javaType="java.util.UUID"
      result property="alunoId" column="aluno_id" javaType="java.util.UUID"
      result property="turmaId" column="turma_id" javaType="java.util.UUID"
      result property="periodoInicio" column="periodo_inicio"
      result property="periodoFim" column="periodo_fim"
      result property="status" column="status"
    Comentário XML no resultMap: "Mapeamento direto coluna->campo — ANTI-PADRAO: Acoplamento ao Banco (DIAG-06). No módulo DDD, o MatriculaMapper usa MatriculaRow (objeto de persistência separado do modelo de domínio). Aqui Matricula.java É diretamente o objeto de persistência."
    select id="findById" resultMap="MatriculaResultMap": SELECT id, aluno_id, turma_id, periodo_inicio, periodo_fim, status FROM matriculas WHERE id = #{id}
    select id="findByAlunoId" resultMap="MatriculaResultMap": SELECT ... FROM matriculas WHERE aluno_id = #{alunoId}
    insert id="insert" parameterType="br.com.escola.matricula.model.Matricula": INSERT INTO matriculas (id, aluno_id, turma_id, periodo_inicio, periodo_fim, status) VALUES (#{id}, #{alunoId}, #{turmaId}, #{periodoInicio}, #{periodoFim}, #{status})
    update id="updateStatus": UPDATE matriculas SET status = #{status} WHERE id = #{id}
    select id="countDisciplinas" resultType="int": SELECT COUNT(*) FROM itens_matricula WHERE matricula_id = #{matriculaId}

    Criar AlunoMapper.xml em erp-matricula-camadas/src/main/resources/mapper/:
    namespace: br.com.escola.matricula.repository.AlunoRepository
    resultMap "AlunoResultMap" type="br.com.escola.matricula.model.Aluno": id column=id, result property=cpf column=cpf, result property=nome column=nome, result property=ativo column=ativo javaType=boolean
    select findById, findAll com SELECT id, cpf, nome, ativo FROM alunos

    Criar ItemMatriculaMapper.xml em erp-matricula-camadas/src/main/resources/mapper/:
    namespace: br.com.escola.matricula.repository.ItemMatriculaRepository
    resultMap "ItemMatriculaResultMap" type="br.com.escola.matricula.model.ItemMatricula": id, matricula_id, disciplina, adicionada_em
    select findByMatriculaId, insert, select countByMatriculaId resultType="int" (SELECT COUNT(*) FROM itens_matricula WHERE matricula_id = #{matriculaId})

    Verificar compilação após criar XMLs:
    Execute: mvn -pl erp-matricula-camadas compile -q
    Nota: a compilação apenas valida Java — XMLs são validados em runtime. O objetivo aqui é confirmar que a estrutura do módulo compila sem erros de tipo (os @Mapper são interfaces, não implementações).
  </action>
  <verify>
    <automated>grep -c "countDisciplinas" /home/marcelino/Área\ de\ trabalho/erp-matricula/erp-matricula-camadas/src/main/resources/mapper/MatriculaMapper.xml && echo "countDisciplinas no XML OK"</automated>
  </verify>
  <acceptance_criteria>
    - MatriculaMapper.xml existe e namespace é br.com.escola.matricula.repository.MatriculaRepository
    - MatriculaMapper.xml contém: countDisciplinas (query SELECT COUNT(*) FROM itens_matricula)
    - MatriculaMapper.xml contém: ANTI-PADRAO: Acoplamento ao Banco (DIAG-06) em comentário XML
    - AlunoMapper.xml existe e namespace é br.com.escola.matricula.repository.AlunoRepository
    - ItemMatriculaMapper.xml existe com countByMatriculaId
    - mvn -pl erp-matricula-camadas compile -q termina com exit code 0
    - grep -l "resultMap" erp-matricula-camadas/src/main/resources/mapper/*.xml lista pelo menos MatriculaMapper.xml e AlunoMapper.xml
  </acceptance_criteria>
  <done>XMLs MyBatis criados com countDisciplinas demonstrando DIAG-06, módulo camadas compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL parameters → PostgreSQL | Parâmetros de consulta passados via MyBatis #{} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | SQL injection via MyBatis | mitigate | Usar #{param} (prepared statement) em TODOS os lugares dos XMLs. Nunca ${param} para parâmetros de usuário. |
| T-05-SC | Tampering | Sem novos pacotes neste plano | accept | Todos os pacotes herdados do parent POM já auditados |
</threat_model>

<verification>
Verificação de completude dos repositórios:
  ls erp-matricula-camadas/src/main/java/br/com/escola/matricula/repository/*.java deve listar 4 arquivos
  ls erp-matricula-camadas/src/main/resources/mapper/*.xml deve listar 3 arquivos
  ls erp-matricula-camadas/src/main/resources/db/migration/ deve listar V1, V2, V3

Verificação do anti-padrão DIAG-06:
  grep -r "ANTI-PADRAO: Acoplamento ao Banco" erp-matricula-camadas/src/main/java/ erp-matricula-camadas/src/main/resources/mapper/ deve retornar matches
</verification>

<success_criteria>
- Quatro @Mapper interfaces no package br.com.escola.matricula.repository
- MatriculaRepository.java tem countDisciplinas com comentário DIAG-06
- Três XMLs de mapeamento com namespaces corretos e queries SQL
- Três migrations V1-V3 copiadas identicamente na pasta db/migration do módulo camadas
- mvn -pl erp-matricula-camadas compile -q sai com 0
</success_criteria>

<output>
Criar `.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-03-SUMMARY.md` quando concluído.
</output>
