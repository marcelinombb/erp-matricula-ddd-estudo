---
phase: 05-diagnostico-codigo-com-anti-padroes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - erp-matricula-ddd/pom.xml
  - Dockerfile
  - Dockerfile.camadas
  - docker-compose.yml
  - init-db.sql
  - README.md
  - docs/04-material-didatico/estrutura-pastas.md
  - docs/04-material-didatico/ddd-vs-camadas.md
  - docs/04-material-didatico/guia-consulta.md
autonomous: true
requirements:
  - DID-01

must_haves:
  truths:
    - "O projeto tem um parent POM na raiz com dois módulos declarados: erp-matricula-ddd e erp-matricula-camadas"
    - "O diretório erp-matricula-app não existe mais — foi renomeado para erp-matricula-ddd via git mv"
    - "mvn -pl erp-matricula-ddd compile compila o módulo DDD sem erros"
    - "docker compose up sobe postgres + app-ddd (8080) + app-camadas (8081)"
    - "Os dois bancos erp_matricula e erp_matricula_camadas são criados na inicialização do postgres"
  artifacts:
    - path: "pom.xml"
      provides: "Parent POM multi-module com dois módulos"
      contains: "<module>erp-matricula-ddd</module>"
    - path: "erp-matricula-ddd/pom.xml"
      provides: "POM do módulo DDD atualizado para herdar do parent local"
      contains: "<artifactId>erp-matricula-parent</artifactId>"
    - path: "Dockerfile.camadas"
      provides: "Dockerfile para o módulo erp-matricula-camadas"
      contains: "erp-matricula-camadas"
    - path: "init-db.sql"
      provides: "Script SQL para criar banco erp_matricula_camadas no init do postgres"
      contains: "CREATE DATABASE erp_matricula_camadas"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "app-ddd service build"
      pattern: "dockerfile: Dockerfile"
    - from: "docker-compose.yml"
      to: "Dockerfile.camadas"
      via: "app-camadas service build"
      pattern: "dockerfile: Dockerfile.camadas"
    - from: "pom.xml"
      to: "erp-matricula-ddd/pom.xml"
      via: "Maven module hierarchy"
      pattern: "<module>erp-matricula-ddd</module>"
---

<objective>
Estabelecer a estrutura Maven multi-module e a infraestrutura Docker para a fase 5. Renomear o módulo existente de erp-matricula-app para erp-matricula-ddd (simetria de nomenclatura com erp-matricula-camadas), criar o parent POM na raiz, atualizar Dockerfile e docker-compose.yml para subir os dois módulos em paralelo.

Purpose: Esta estrutura de dois módulos físicos É o ponto pedagógico central do milestone v1.1. O desenvolvedor enxerga duas aplicações Spring Boot completas com o mesmo domínio — uma com arquitetura em camadas ("antes"), outra com DDD ("depois"). A separação física torna a comparação inegável.

Output: Parent POM, erp-matricula-ddd/ (renomeado), Dockerfile.camadas, docker-compose.yml atualizado com dois serviços Spring Boot, init-db.sql para criar os dois bancos PostgreSQL, referências em docs/04-* atualizadas.
</objective>

<execution_context>
@/home/marcelino/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcelino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-CONTEXT.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md
@.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md

<interfaces>
<!-- Estrutura atual que será modificada -->

Do pom.xml atual (erp-matricula-app/pom.xml), bloco parent existente (linhas 19-24):
  groupId: org.springframework.boot / artifactId: spring-boot-starter-parent / version: 3.5.3

Do Dockerfile atual (linhas 26-37):
  RUN mvn -q package -DskipTests -f erp-matricula-app/pom.xml
  COPY --from=builder /build/erp-matricula-app/target/erp-matricula-*.jar app.jar

Do docker-compose.yml atual (linhas 37-52):
  service name: app / build: . / ports: "8080:8080"
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/erp_matricula
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Renomear erp-matricula-app para erp-matricula-ddd e criar parent POM</name>
  <files>
    pom.xml (NOVO — parent da raiz),
    erp-matricula-ddd/pom.xml (MODIFICADO — era erp-matricula-app/pom.xml)
  </files>
  <read_first>
    - erp-matricula-app/pom.xml — arquivo completo; herança e dependências a preservar
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md — seções "Standard Stack", "Architecture Patterns Pattern 1", "Common Pitfalls (Pitfall 2)"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seções "Parent POM" e "erp-matricula-ddd/pom.xml"
  </read_first>
  <action>
    Passo 1 — Renomear diretório preservando histórico git:
    Execute: git mv erp-matricula-app erp-matricula-ddd
    Isso preserva o histórico de commits de todos os arquivos Java do módulo DDD.

    Passo 2 — Criar pom.xml na raiz do projeto (parent POM multi-module):
    Coordenadas: groupId=br.com.escola, artifactId=erp-matricula-parent, version=0.1.0-SNAPSHOT, packaging=pom.
    Herança: spring-boot-starter-parent 3.5.3 com relativePath vazio.
    Módulos declarados: erp-matricula-ddd e erp-matricula-camadas (nesta ordem).
    Propriedades: java.version=21, project.build.sourceEncoding=UTF-8, project.reporting.outputEncoding=UTF-8.
    Plugin spring-boot-maven-plugin com configuration/skip=true (per Pitfall 2 RESEARCH.md — parent não é JAR executável; sem o skip, o build do parent falha durante repackage).
    Adicionar comentário XML de cabeçalho explicando a exceção D-03 ao CLAUDE.md sobre multi-module Maven (conforme padrão de documentação em pom.xml linhas 1-9 do analog).

    Passo 3 — Atualizar erp-matricula-ddd/pom.xml:
    Substituir o bloco parent (que atualmente herda de spring-boot-starter-parent) para herdar do parent local:
      groupId=br.com.escola, artifactId=erp-matricula-parent, version=0.1.0-SNAPSHOT, relativePath=../pom.xml
    Manter artifactId=erp-matricula (não precisa mudar com o diretório — convenção Maven).
    Manter todas as dependências e plugins idênticos ao original.

    Passo 4 — Verificar build:
    Execute: mvn -pl erp-matricula-ddd compile -q
    O módulo DDD deve compilar sem erros após a mudança de parent.
  </action>
  <verify>
    <automated>cd /home/marcelino/Área\ de\ trabalho/erp-matricula && mvn -pl erp-matricula-ddd compile -q 2>&1 | tail -5 && echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - Diretório erp-matricula-app não existe: ls erp-matricula-app deve retornar "No such file or directory"
    - Diretório erp-matricula-ddd existe com todos os arquivos Java intactos
    - pom.xml raiz contém: &lt;artifactId&gt;erp-matricula-parent&lt;/artifactId&gt;
    - pom.xml raiz contém: &lt;module&gt;erp-matricula-ddd&lt;/module&gt;
    - pom.xml raiz contém: &lt;module&gt;erp-matricula-camadas&lt;/module&gt;
    - pom.xml raiz contém: &lt;skip&gt;true&lt;/skip&gt; dentro do spring-boot-maven-plugin
    - erp-matricula-ddd/pom.xml contém: &lt;artifactId&gt;erp-matricula-parent&lt;/artifactId&gt; no bloco parent
    - mvn -pl erp-matricula-ddd compile -q termina com exit code 0
    - git log --follow erp-matricula-ddd/src/main/java/br/com/escola/matricula/ErpMatriculaApplication.java mostra commits anteriores (histórico preservado)
  </acceptance_criteria>
  <done>Parent POM criado, módulo DDD renomeado e compilando, histórico git preservado.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar Dockerfile, criar Dockerfile.camadas, atualizar docker-compose.yml e init-db.sql</name>
  <files>
    Dockerfile (MODIFICADO),
    Dockerfile.camadas (NOVO),
    docker-compose.yml (MODIFICADO),
    init-db.sql (NOVO),
    README.md (MODIFICADO),
    docs/04-material-didatico/estrutura-pastas.md (MODIFICADO),
    docs/04-material-didatico/ddd-vs-camadas.md (MODIFICADO),
    docs/04-material-didatico/guia-consulta.md (MODIFICADO)
  </files>
  <read_first>
    - Dockerfile — arquivo completo; linhas 26 e 37 têm paths erp-matricula-app a atualizar
    - docker-compose.yml — arquivo completo; service app será renomeado e expandido
    - docs/04-material-didatico/estrutura-pastas.md — procurar ocorrências de "erp-matricula-app"
    - docs/04-material-didatico/ddd-vs-camadas.md — procurar ocorrências de "erp-matricula-app"
    - docs/04-material-didatico/guia-consulta.md — procurar ocorrências de "erp-matricula-app"
    - README.md — procurar ocorrências de "erp-matricula-app"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-PATTERNS.md — seções "Dockerfile", "Dockerfile.camadas", "docker-compose.yml"
    - .planning/phases/05-diagnostico-codigo-com-anti-padroes/05-RESEARCH.md — seções "Common Pitfalls Pitfall 1" (conflito Flyway) e "Pitfall 3" (Dockerfile)
  </read_first>
  <action>
    Passo 1 — Atualizar Dockerfile (módulo DDD):
    Linha com RUN mvn: mudar de "-f erp-matricula-app/pom.xml" para "-pl erp-matricula-ddd"
    (com parent pom na raiz, -pl seleciona o módulo dentro do multi-module).
    Linha com COPY: mudar de "/build/erp-matricula-app/target/erp-matricula-*.jar" para "/build/erp-matricula-ddd/target/erp-matricula-*.jar".
    Manter todos os comentários explicativos existentes; atualizar apenas os paths.

    Passo 2 — Criar Dockerfile.camadas (cópia do Dockerfile com módulo camadas):
    Copiar estrutura idêntica do Dockerfile, substituindo:
      "-pl erp-matricula-ddd" por "-pl erp-matricula-camadas"
      "/build/erp-matricula-ddd/target/erp-matricula-*.jar" por "/build/erp-matricula-camadas/target/erp-matricula-camadas-*.jar"
    Adicionar cabeçalho comentando: "Dockerfile do módulo erp-matricula-camadas (arquitetura em camadas — o 'antes' didático)".

    Passo 3 — Criar init-db.sql na raiz do projeto:
    Conteúdo: CREATE DATABASE erp_matricula_camadas; (com comentário explicando que o banco separado evita conflito de Flyway migrations entre os dois módulos — per Pitfall 1 RESEARCH.md).
    Nota: o banco erp_matricula já é criado pela variável POSTGRES_DB no docker-compose; init-db.sql cria o segundo banco.

    Passo 4 — Atualizar docker-compose.yml:
    Serviço postgres: adicionar volume mount de ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql para criar o banco camadas na inicialização. Manter POSTGRES_DB=erp_matricula, healthcheck e volumes existentes.
    Serviço app (renomear para app-ddd): manter ports "8080:8080", SPRING_DATASOURCE_URL apontando para erp_matricula, depends_on postgres. Mudar build de "." para "build: { context: ., dockerfile: Dockerfile }".
    Serviço app-camadas (NOVO): build usando Dockerfile.camadas, ports "8081:8080", SPRING_DATASOURCE_URL para erp_matricula_camadas, depends_on postgres com condition: service_healthy.
    Adicionar comentário no topo explicando os dois módulos e suas portas (8080=DDD, 8081=camadas).

    Passo 5 — Atualizar referências em docs e README:
    Em docs/04-material-didatico/estrutura-pastas.md: substituir todas as ocorrências de "erp-matricula-app" por "erp-matricula-ddd".
    Em docs/04-material-didatico/ddd-vs-camadas.md: substituir paths de arquivo inline (comentários com caminhos para o código-fonte).
    Em docs/04-material-didatico/guia-consulta.md: atualizar links para código-fonte.
    Em README.md: atualizar seção de estrutura do projeto mencionando os dois módulos e suas portas.
    NÃO atualizar nenhum arquivo em .planning/milestones/ — são registros históricos.
  </action>
  <verify>
    <automated>grep -r "erp-matricula-app" /home/marcelino/Área\ de\ trabalho/erp-matricula/Dockerfile /home/marcelino/Área\ de\ trabalho/erp-matricula/docker-compose.yml /home/marcelino/Área\ de\ trabalho/erp-matricula/docs/04-material-didatico/ /home/marcelino/Área\ de\ trabalho/erp-matricula/README.md 2>/dev/null | grep -v "Binary" | wc -l && echo "Ocorrências de erp-matricula-app restantes (deve ser 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile linha RUN mvn contém: -pl erp-matricula-ddd (não -f erp-matricula-app/pom.xml)
    - Dockerfile linha COPY contém: /build/erp-matricula-ddd/target/
    - Dockerfile.camadas existe e contém: -pl erp-matricula-camadas
    - Dockerfile.camadas contém: erp-matricula-camadas-*.jar
    - init-db.sql existe e contém: CREATE DATABASE erp_matricula_camadas
    - docker-compose.yml contém service app-ddd com ports "8080:8080"
    - docker-compose.yml contém service app-camadas com ports "8081:8080"
    - docker-compose.yml contém: ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    - grep -r "erp-matricula-app" Dockerfile docker-compose.yml docs/04-material-didatico/ README.md retorna 0 linhas (nenhuma referência antiga restante nos arquivos ativos)
    - .planning/milestones/ não foi tocado (git status não mostra mudanças nessa pasta)
  </acceptance_criteria>
  <done>Docker e docs atualizados: dois serviços no compose, dois Dockerfiles, dois bancos separados, referências nos docs migradas para erp-matricula-ddd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose env vars → containers | Credenciais DB passadas como env; dev-only (não produção) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | application.yml — credenciais hardcoded | accept | Credenciais de dev apenas (matricula/matricula); docker-compose usa variáveis ${DB_USER:-matricula}. Projeto didático, não deployment. |
| T-05-02 | Tampering | MyBatis SQL — interpolação #{} vs ${} | mitigate | Usar #{param} em todos os mappers XML (prepared statement); nunca ${} para parâmetros de usuário |
| T-05-SC | Tampering | Nenhum novo pacote instalado neste plano | accept | Package Legitimacy Audit confirmado no RESEARCH.md: todos os pacotes já verificados em fases anteriores |
</threat_model>

<verification>
Verificação global da Tarefa 1:
  mvn -pl erp-matricula-ddd compile -q deve terminar com exit 0

Verificação global da Tarefa 2:
  grep -c "erp-matricula-app" Dockerfile docker-compose.yml deve retornar 0 para ambos
  grep "app-ddd\|app-camadas" docker-compose.yml deve retornar 2 linhas
  cat init-db.sql deve conter CREATE DATABASE erp_matricula_camadas
</verification>

<success_criteria>
- erp-matricula-ddd/ existe com histórico git preservado (git log --follow funciona nos arquivos Java)
- pom.xml raiz compila o módulo DDD: mvn -pl erp-matricula-ddd compile -q sai com 0
- docker-compose.yml tem três serviços: postgres, app-ddd (8080), app-camadas (8081)
- Nenhuma referência a erp-matricula-app nos arquivos ativos (Dockerfile, docker-compose.yml, docs/04-*, README.md)
- init-db.sql cria o banco erp_matricula_camadas no init do postgres
</success_criteria>

<output>
Criar `.planning/phases/05-diagnostico-codigo-com-anti-padroes/05-01-SUMMARY.md` quando concluído.
</output>
