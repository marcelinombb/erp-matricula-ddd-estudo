---
phase: 08-testes-unit-rios-do-dom-nio-ddd
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/MatriculaRepositorioEmMemoria.java
  - erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java
autonomous: true
requirements:
  - TDDD-03

must_haves:
  truths:
    - "Desenvolvedor lê MatriculaRepositorioEmMemoria.java e vê que é uma implementação de MatriculaRepositorio sem Mockito — zero imports de org.mockito.* ou org.springframework.*"
    - "Desenvolvedor lê MatriculaRepositorioEmMemoria.java e vê API fluente de configuração: stub.comMatriculaExistente() e stub.semMatriculaExistente() controlam o retorno de existeMatriculaAtiva()"
    - "Desenvolvedor executa mvn test e vê VerificadorElegibilidadeMatriculaTest com 4 testes passando: happy path, aluno inativo, período fechado, matrícula duplicada"
    - "Nenhum arquivo de teste contém import org.springframework.* ou org.mockito.* — verificável por grep"
  artifacts:
    - path: "erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/MatriculaRepositorioEmMemoria.java"
      provides: "Stub in-memory de MatriculaRepositorio sem Mockito (D-01, D-02)"
      contains: "class MatriculaRepositorioEmMemoria implements MatriculaRepositorio"
    - path: "erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java"
      provides: "Testes unitários do Domain Service VerificadorElegibilidadeMatricula — 4 métodos"
      contains: "class VerificadorElegibilidadeMatriculaTest"
  key_links:
    - from: "VerificadorElegibilidadeMatriculaTest.java"
      to: "MatriculaRepositorioEmMemoria.java (stub)"
      via: "new VerificadorElegibilidadeMatricula(stub) — construtor por injeção (D-01)"
      pattern: "new VerificadorElegibilidadeMatricula"
    - from: "MatriculaRepositorioEmMemoria.java"
      to: "MatriculaRepositorio.java (interface de domínio)"
      via: "implements MatriculaRepositorio — sem Mockito, sem Spring"
      pattern: "implements MatriculaRepositorio"
---

<objective>
Criar o stub in-memory MatriculaRepositorioEmMemoria e os testes do Domain Service VerificadorElegibilidadeMatricula, completando a demonstração de que o domínio DDD é testável sem nenhuma dependência de infraestrutura.

Purpose: O stub escrito à mão (decisão D-01) é o ponto pedagógico mais marcante do plano: o desenvolvedor vê que o domínio não depende de Mockito nem de Spring para ser testado. VerificadorElegibilidadeMatricula é o único artefato com dependência externa (MatriculaRepositorio), e a injeção por construtor torna o stub a solução natural. Os 4 testes cobrem cada regra de negócio do verificador separadamente (D-03 — um teste por regra).

Output: MatriculaRepositorioEmMemoria.java no mesmo pacote do teste (D-02), VerificadorElegibilidadeMatriculaTest.java com 4 testes usando o stub — sem Mockito, sem Spring.
</objective>

<execution_context>
@/home/marcelino/.claude/gsd-core/workflows/execute-plan.md
@/home/marcelino/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-CONTEXT.md
@.planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-PATTERNS.md
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/repositorio/MatriculaRepositorio.java
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatricula.java
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Aluno.java
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Turma.java
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/excecao/AlunoInativoException.java
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/excecao/PeriodoFechadoException.java
@erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaDuplicadaException.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar MatriculaRepositorioEmMemoria.java — stub in-memory com API fluente de configuração (D-01, D-02)</name>
  <files>erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/MatriculaRepositorioEmMemoria.java</files>

  <read_first>
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/repositorio/MatriculaRepositorio.java — ler TODOS os métodos da interface para implementar (com UnsupportedOperationException nos não-usados): existeMatriculaAtiva(AlunoId, PeriodoLetivo), buscarPorId(MatriculaId), buscarPorAluno(AlunoId), salvar(Matricula) — verificar assinaturas exatas e tipos de retorno
    - .planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-PATTERNS.md — seção `servico/MatriculaRepositorioEmMemoria.java`: implementação completa do stub com campo booleano configurável e métodos fluentes comMatriculaExistente() e semMatriculaExistente()
    - .planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-CONTEXT.md — D-01 (stub sem Mockito) e D-02 (pacote br.com.escola.matricula.dominio.servico em src/test/java)
  </read_first>

  <action>
    Criar o arquivo no pacote br.com.escola.matricula.dominio.servico em src/test/java (per D-02 — mesmo pacote do teste que o usa).

    Visibilidade: class package-private (sem modificador public) — acessível apenas ao VerificadorElegibilidadeMatriculaTest no mesmo pacote (per D-02).

    Imports necessários:
    - import br.com.escola.matricula.dominio.modelo.Matricula;
    - import br.com.escola.matricula.dominio.repositorio.MatriculaRepositorio;
    - import br.com.escola.matricula.dominio.vo.AlunoId;
    - import br.com.escola.matricula.dominio.vo.MatriculaId;
    - import br.com.escola.matricula.dominio.vo.PeriodoLetivo;
    - import java.util.List;
    - import java.util.Optional;

    Estrutura da classe:
    - Campo: private boolean existeMatriculaAtiva = false; (default: sem matrícula existente)
    - Método de configuração: MatriculaRepositorioEmMemoria comMatriculaExistente() { this.existeMatriculaAtiva = true; return this; }
    - Método de configuração: MatriculaRepositorioEmMemoria semMatriculaExistente() { this.existeMatriculaAtiva = false; return this; }
    - Override existeMatriculaAtiva(AlunoId, PeriodoLetivo): return this.existeMatriculaAtiva;
    - Override buscarPorId(MatriculaId): throw new UnsupportedOperationException("Não implementado no stub");
    - Override buscarPorAluno(AlunoId): throw new UnsupportedOperationException("Não implementado no stub");
    - Override salvar(Matricula): throw new UnsupportedOperationException("Não implementado no stub");

    Verificar a assinatura exata de existeMatriculaAtiva() em MatriculaRepositorio.java — os tipos dos parâmetros devem ser exatamente os mesmos (AlunoId, PeriodoLetivo — não UUID, não String).

    Zero imports de org.mockito.* ou org.springframework.*
  </action>

  <verify>
    <automated>cd /home/marcelino/Documentos/erp-matricula-ddd-estudo/erp-matricula-ddd && /home/marcelino/.maven/maven-3.9.14/bin/mvn compile test-compile -q 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo existe em erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/MatriculaRepositorioEmMemoria.java
    - Arquivo declara "class MatriculaRepositorioEmMemoria implements MatriculaRepositorio" (package-private — sem public)
    - mvn compile test-compile retorna BUILD SUCCESS (stub compila sem erros)
    - grep -c "org.springframework\|org.mockito" MatriculaRepositorioEmMemoria.java retorna 0
    - Arquivo contém os métodos: comMatriculaExistente, semMatriculaExistente, existeMatriculaAtiva, buscarPorId, buscarPorAluno, salvar
    - Métodos buscarPorId, buscarPorAluno, salvar lançam UnsupportedOperationException
  </acceptance_criteria>

  <done>MatriculaRepositorioEmMemoria.java compila, implementa MatriculaRepositorio sem Mockito/Spring, expõe API fluente comMatriculaExistente()/semMatriculaExistente() para configuração em testes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar VerificadorElegibilidadeMatriculaTest.java — 4 testes cobrindo happy path e 3 regras de negócio (D-03, D-09, D-10, D-11, D-12 — TDDD-03)</name>
  <files>erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java</files>

  <read_first>
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatricula.java — ler COMPLETO: construtor (recebe MatriculaRepositorio), assinatura do método verificar(Aluno, Turma, PeriodoLetivo), as 3 regras de negócio verificadas (aluno inativo → AlunoInativoException, período fechado → PeriodoFechadoException, matrícula duplicada → MatriculaDuplicadaException)
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Aluno.java — ler construtor: quais parâmetros aceita (AlunoId, Cpf, String nome, boolean ativo), verificar assinatura exata
    - erp-matricula-ddd/src/main/java/br/com/escola/matricula/dominio/modelo/Turma.java — ler construtor e método periodoEstaAberto() para entender a dependência de LocalDate.now() (Pitfall 2 do RESEARCH.md: semestre 1 vai de fev a jul, semestre 2 vai de ago a dez — PeriodoLetivo(2020,1) está sempre no passado)
    - .planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-PATTERNS.md — seção `servico/VerificadorElegibilidadeMatriculaTest.java`: padrões de fixture (criarAlunoAtivo, criarAlunoInativo, criarTurmaComPeriodoFechado), padrão de teste para cada caso
    - .planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-RESEARCH.md — Pitfall 2 (periodoEstaAberto depende de LocalDate.now()), Pitfall 3 (CPF 111.111.111-11 inválido — usar 529.982.247-25)
    - erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/MatriculaRepositorioEmMemoria.java — confirmar que foi criado na Task 1
  </read_first>

  <behavior>
    - Teste 1: `devePermitirMatriculaQuandoAlunoAtivoEPeriodoAbertoeElegivelSemDuplicidade()` — stub.semMatriculaExistente(); alunoAtivo; turma com período que periodoEstaAberto() retorna true — assertThatCode(() -> verificador.verificar(...)).doesNotThrowAnyException()
    - Teste 2: `deveLancarExcecaoQuandoAlunoInativo()` — stub.semMatriculaExistente(); alunoInativo; assertThatThrownBy(...).isInstanceOf(AlunoInativoException.class)
    - Teste 3: `deveLancarExcecaoQuandoPeriodoFechado()` — stub.semMatriculaExistente(); alunoAtivo; turma com PeriodoLetivo(2020, 1) — sempre no passado; assertThatThrownBy(...).isInstanceOf(PeriodoFechadoException.class)
    - Teste 4: `deveLancarExcecaoQuandoMatriculaDuplicada()` — stub.comMatriculaExistente(); alunoAtivo; turma com período que periodoEstaAberto() retorna true; assertThatThrownBy(...).isInstanceOf(MatriculaDuplicadaException.class)
  </behavior>

  <action>
    Criar o arquivo no pacote br.com.escola.matricula.dominio.servico em src/test/java.

    Bloco de imports (per D-12 — zero org.springframework.* e zero org.mockito.*):
    - import br.com.escola.matricula.dominio.excecao.AlunoInativoException;
    - import br.com.escola.matricula.dominio.excecao.MatriculaDuplicadaException;
    - import br.com.escola.matricula.dominio.excecao.PeriodoFechadoException;
    - import br.com.escola.matricula.dominio.modelo.Aluno;
    - import br.com.escola.matricula.dominio.modelo.Turma;
    - import br.com.escola.matricula.dominio.vo.AlunoId;
    - import br.com.escola.matricula.dominio.vo.Cpf;
    - import br.com.escola.matricula.dominio.vo.PeriodoLetivo;
    - import br.com.escola.matricula.dominio.vo.TurmaId;
    - import org.junit.jupiter.api.DisplayName;
    - import org.junit.jupiter.api.Test;
    - import java.util.UUID;
    - import static org.assertj.core.api.Assertions.assertThatCode;
    - import static org.assertj.core.api.Assertions.assertThatThrownBy;

    Constante de classe: private static final Cpf CPF_TESTE = new Cpf("529.982.247-25"); — CPF válido fixo para testes (evita repetição; evita Pitfall 3 do RESEARCH.md)

    Métodos auxiliares privados:
    - criarAlunoAtivo(): return new Aluno(new AlunoId(UUID.randomUUID()), CPF_TESTE, "João da Silva", true)
    - criarAlunoInativo(): return new Aluno(new AlunoId(UUID.randomUUID()), CPF_TESTE, "Maria Inativa", false)
    - criarTurmaComPeriodoFechado(): return new Turma(new TurmaId(UUID.randomUUID()), "Turma Passada", new PeriodoLetivo(2020, 1), 30) — PeriodoLetivo(2020, 1) sempre no passado: semestre 1 de 2020 vai de fev a jul de 2020, já encerrado em qualquer data de execução

    Estratégia para Teste 1 e 4 (happy path e duplicidade — precisam de período ABERTO):
    - Ler Turma.java para entender periodoEstaAberto() e qual período está aberto em junho de 2026
    - Abordagem robusta: criar Turma com PeriodoLetivo(LocalDate.now().getYear(), LocalDate.now().getMonthValue() >= 8 ? 2 : 1) para sempre usar o semestre corrente — OU documentar com comentário a dependência de data
    - Alternativa simples e pedagógica: usar PeriodoLetivo do semestre corrente em junho de 2026 = PeriodoLetivo(2026, 1) pois semestre 1 (fev-jul) está aberto em junho; documentar com comentário: "// NOTA: PeriodoLetivo(2026, 1) — semestre 1 está aberto de fev a jul; este teste pressupõe execução nesse período"
    - Para o Teste 4 (duplicidade), assegurar que periodoEstaAberto() retorne true antes de chegar na verificação de duplicidade — usar o mesmo período aberto do Teste 1

    Implementação dos 4 testes com D-09 (Given-When-Then), D-10 (nome em português), D-11 (AssertJ):

    Teste 1 (happy path): var stub = new MatriculaRepositorioEmMemoria().semMatriculaExistente(); var verificador = new VerificadorElegibilidadeMatricula(stub); var alunoAtivo = criarAlunoAtivo(); var periodo = new PeriodoLetivo(2026, 1); var turma = new Turma(new TurmaId(UUID.randomUUID()), "Turma A", periodo, 30); assertThatCode(() -> verificador.verificar(alunoAtivo, turma, periodo)).doesNotThrowAnyException()

    Teste 2 (inativo): var stub = new MatriculaRepositorioEmMemoria().semMatriculaExistente(); var verificador = new VerificadorElegibilidadeMatricula(stub); var alunoInativo = criarAlunoInativo(); var turma = criarTurmaComPeriodoFechado(); assertThatThrownBy(() -> verificador.verificar(alunoInativo, turma, new PeriodoLetivo(2020, 1))).isInstanceOf(AlunoInativoException.class)

    Teste 3 (período fechado): var stub = new MatriculaRepositorioEmMemoria().semMatriculaExistente(); var verificador = new VerificadorElegibilidadeMatricula(stub); var alunoAtivo = criarAlunoAtivo(); var turmaFechada = criarTurmaComPeriodoFechado(); assertThatThrownBy(() -> verificador.verificar(alunoAtivo, turmaFechada, new PeriodoLetivo(2020, 1))).isInstanceOf(PeriodoFechadoException.class)

    Teste 4 (duplicidade): var stub = new MatriculaRepositorioEmMemoria().comMatriculaExistente(); var verificador = new VerificadorElegibilidadeMatricula(stub); var alunoAtivo = criarAlunoAtivo(); var periodo = new PeriodoLetivo(2026, 1); var turma = new Turma(new TurmaId(UUID.randomUUID()), "Turma B", periodo, 30); assertThatThrownBy(() -> verificador.verificar(alunoAtivo, turma, periodo)).isInstanceOf(MatriculaDuplicadaException.class)

    Verificar o construtor exato de Aluno.java e Turma.java antes de usar — o número e ordem dos parâmetros devem ser exatamente os do código de produção.
  </action>

  <verify>
    <automated>cd /home/marcelino/Documentos/erp-matricula-ddd-estudo/erp-matricula-ddd && /home/marcelino/.maven/maven-3.9.14/bin/mvn test -Dtest=VerificadorElegibilidadeMatriculaTest -q 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo existe em erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java
    - mvn test -Dtest=VerificadorElegibilidadeMatriculaTest retorna BUILD SUCCESS com 4 testes executados, 0 falhas
    - grep -c "@Test" VerificadorElegibilidadeMatriculaTest.java retorna 4
    - grep -c "org.springframework\|org.mockito" VerificadorElegibilidadeMatriculaTest.java retorna 0
    - Arquivo usa new MatriculaRepositorioEmMemoria() — sem Mockito.mock()
    - Arquivo usa new VerificadorElegibilidadeMatricula(stub) — injeção por construtor
    - Arquivo contém a constante CPF_TESTE = new Cpf("529.982.247-25")
    - Arquivo contém métodos: deveLancarExcecaoQuandoAlunoInativo, deveLancarExcecaoQuandoPeriodoFechado, deveLancarExcecaoQuandoMatriculaDuplicada
  </acceptance_criteria>

  <done>4 testes de VerificadorElegibilidadeMatriculaTest passam: happy path, aluno inativo, período fechado, matrícula duplicada. Stub sem Mockito. Zero imports Spring ou Mockito.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `MatriculaRepositorioEmMemoria` | class (test support / stub) | erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/MatriculaRepositorioEmMemoria.java |
| `comMatriculaExistente` | method (fluent configurator) | MatriculaRepositorioEmMemoria.java |
| `semMatriculaExistente` | method (fluent configurator) | MatriculaRepositorioEmMemoria.java |
| `VerificadorElegibilidadeMatriculaTest` | class (test) | erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java |
| `CPF_TESTE` | constant | VerificadorElegibilidadeMatriculaTest.java |
| `criarAlunoAtivo` | private helper method | VerificadorElegibilidadeMatriculaTest.java |
| `criarAlunoInativo` | private helper method | VerificadorElegibilidadeMatriculaTest.java |
| `criarTurmaComPeriodoFechado` | private helper method | VerificadorElegibilidadeMatriculaTest.java |
| `devePermitirMatriculaQuandoAlunoAtivoEPeriodoAbertoElegivelSemDuplicidade` | test method | VerificadorElegibilidadeMatriculaTest.java |
| `deveLancarExcecaoQuandoAlunoInativo` | test method | VerificadorElegibilidadeMatriculaTest.java |
| `deveLancarExcecaoQuandoPeriodoFechado` | test method | VerificadorElegibilidadeMatriculaTest.java |
| `deveLancarExcecaoQuandoMatriculaDuplicada` | test method | VerificadorElegibilidadeMatriculaTest.java |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → domínio | Testes instanciam o Domain Service diretamente com stub injetado por construtor — sem Spring, sem proxy |
| stub → interface de domínio | MatriculaRepositorioEmMemoria implementa MatriculaRepositorio sem banco ou rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | MatriculaRepositorioEmMemoria / VerificadorElegibilidadeMatriculaTest | accept | Testes são arquivos somente de leitura em runtime; stub usa visibilidade package-private (D-02) — não acessível fora do pacote de teste |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Fase não instala novos pacotes — todas as dependências já presentes no pom.xml |
</threat_model>

<verification>
Após execução de ambas as tasks:

```bash
# Todos os testes do plano passam
cd /home/marcelino/Documentos/erp-matricula-ddd-estudo/erp-matricula-ddd
/home/marcelino/.maven/maven-3.9.14/bin/mvn test -Dtest=VerificadorElegibilidadeMatriculaTest 2>&1 | grep -E "(Tests run|BUILD|FAIL)"

# Zero imports Spring ou Mockito nos arquivos do servico
grep -r "org.springframework\|org.mockito" \
  erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/
# Resultado esperado: sem saída

# Verificação D-12 em todos os testes do domínio
grep -r "org.springframework" \
  erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/
# Resultado esperado: sem saída
```
</verification>

<success_criteria>
- mvn test -Dtest=VerificadorElegibilidadeMatriculaTest retorna BUILD SUCCESS com 4 testes executados, 0 falhas
- MatriculaRepositorioEmMemoria implementa MatriculaRepositorio com package-private visibility (D-02)
- Nenhum teste usa Mockito.mock() — stub é instanciado diretamente com new (D-01)
- grep de org.springframework e org.mockito em src/test/java/br/com/escola/matricula/dominio/ retorna vazio (D-12 satisfeita para todo o módulo de testes do domínio)
- Padrão fluente de configuração: stub.comMatriculaExistente() e stub.semMatriculaExistente() usados nos testes
</success_criteria>

<output>
Criar `.planning/phases/08-testes-unit-rios-do-dom-nio-ddd/08-04-SUMMARY.md` quando concluído.
</output>
