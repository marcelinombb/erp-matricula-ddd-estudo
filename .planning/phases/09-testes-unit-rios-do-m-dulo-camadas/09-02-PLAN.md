---
phase: 09-testes-unit-rios-do-m-dulo-camadas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/DisciplinaServiceImplTest.java
autonomous: true
requirements:
  - TCAM-02
must_haves:
  truths:
    - "Desenvolvedor vê que DisciplinaServiceImplTest usa 3 dos 4 mocks do MatriculaServiceImplTest — evidência de DIAG-04 (mesmas regras, quase mesmo setup)"
    - "Existe teste deveLancarExcecaoQuandoAlunoInativo() com comentário DIAG-04 explicitando que a mesma regra está em MatriculaServiceImplTest"
    - "O comentário nota a divergência de mensagem: 'Aluno inativo' aqui vs 'Aluno inativo não pode ser matriculado' no MatriculaServiceImpl — evidência do bug silencioso"
    - "Existe teste verificarStatusMatricula() mostrando a proliferação de métodos verificadores (DIAG-04)"
  artifacts:
    - path: "erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/DisciplinaServiceImplTest.java"
      provides: "Testes unitários do DisciplinaServiceImpl com Mockito puro — evidência TCAM-02"
      contains: "@ExtendWith(MockitoExtension.class)"
  key_links:
    - from: "DisciplinaServiceImplTest.deveLancarExcecaoQuandoAlunoInativo"
      to: "DIAG-04"
      via: "comentário inline referenciando MatriculaServiceImplTest"
      pattern: "DIAG-04: mesma regra testada em MatriculaServiceImplTest"
---

<objective>
Criar DisciplinaServiceImplTest.java no módulo erp-matricula-camadas evidenciando TCAM-02:
regras duplicadas entre DisciplinaServiceImpl e MatriculaServiceImpl se manifestando como
testes duplicados — o mesmo cenário de erro (aluno inativo, limite atingido) exige setup
idêntico em dois arquivos de teste distintos.

Purpose: Mostrar ao leitor que a duplicação de código de produção (DIAG-04) gera duplicação
de código de teste — o custo de manutenção se multiplica, e a divergência de mensagens
("Aluno inativo" vs "Aluno inativo não pode ser matriculado") é o bug silencioso que DIAG-04 prevê.

Output: erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/DisciplinaServiceImplTest.java
com 5 métodos de teste cobrindo adicionarDisciplinaExtra() (4 casos) e verificarStatusMatricula() (1 caso).
</objective>

<execution_context>
@/home/marcelino/.claude/gsd-core/workflows/execute-plan.md
@/home/marcelino/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/ROADMAP.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-CONTEXT.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-RESEARCH.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar DisciplinaServiceImplTest com 5 testes Mockito — evidência DIAG-04</name>
  <files>erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/DisciplinaServiceImplTest.java</files>

  <read_first>
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/DisciplinaServiceImpl.java — fonte da verdade: construtor 3-args (linhas 41-47), lógica de adicionarDisciplinaExtra() (linhas 61-102), verificarStatusMatricula() (linhas 120-130), mensagens de exceção exatas ("Aluno inativo", "Limite atingido", "Matrícula não encontrada: " + id, "Matrícula não está ativa. Status atual: " + status)
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Aluno.java — setters para construção no @BeforeEach
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java — setters para construção no @BeforeEach
    - erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java — padrão Given-When-Then e AssertJ (referência de contraste — zero mocks neste arquivo)
  </read_first>

  <action>
    Criar o arquivo no diretório src/test/java/br/com/escola/matricula/service/ (já existe se o plano 09-01 foi executado primeiro; se não, criar o diretório).

    Pacote: br.com.escola.matricula.service

    Anotações da classe: @ExtendWith(MockitoExtension.class) + @DisplayName("DisciplinaServiceImpl — Duplicação de Regras (DIAG-04)")

    Campos da classe (per D-01, D-05):
    - @Mock MatriculaRepository matriculaRepository
    - @Mock AlunoRepository alunoRepository
    - @Mock ItemMatriculaRepository itemMatriculaRepository
    - Comentário após os 3 @Mock: "// DIAG-04: 3 dos 4 mocks do MatriculaServiceImplTest — mesmas regras, quase mesmo setup"
    - @InjectMocks DisciplinaServiceImpl service — comentar: "// construtor 3-args (DisciplinaServiceImpl.java:41-47)"
    - campos privados: Aluno alunoAtivo, Matricula matriculaAtiva

    @BeforeEach configurarHappyPath() (per D-03):
    - UUID alunoId = UUID.randomUUID() local
    - Construir alunoAtivo com new Aluno() + setId(alunoId) + setAtivo(true)
    - Construir matriculaAtiva com new Matricula() + setId(UUID.randomUUID()) + setAlunoId(alunoId) + setStatus("ATIVA"); coordenar UUID (alunoId na matrícula = ID do aluno)
    - Stubs: when(matriculaRepository.findById(any(UUID.class))).thenReturn(Optional.of(matriculaAtiva)); when(alunoRepository.findById(any(UUID.class))).thenReturn(Optional.of(alunoAtivo)); when(matriculaRepository.countDisciplinas(any(UUID.class))).thenReturn(3)

    Cinco métodos @Test (per D-05, D-10, D-11, D-12, D-13):

    1. deveAdicionarDisciplinaExtraComSucesso() — @DisplayName("deve adicionar disciplina extra com sucesso")
       // given — happy path configurado no @BeforeEach
       // when: service.adicionarDisciplinaExtra(matriculaAtiva.getId(), "Matemática")
       // then: verify(itemMatriculaRepository).insert(any(ItemMatricula.class))

    2. deveLancarExcecaoQuandoMatriculaNaoEncontrada() — @DisplayName("deve lançar exceção quando matrícula não encontrada")
       // given: when(matriculaRepository.findById(any())).thenReturn(Optional.empty())
       // when/then: assertThatThrownBy(() -> service.adicionarDisciplinaExtra(...)).isInstanceOf(RuntimeException.class).hasMessageContaining("Matrícula não encontrada")

    3. deveLancarExcecaoQuandoMatriculaNaoEstaAtiva() — @DisplayName("deve lançar exceção quando matrícula não está ativa")
       // given: matriculaAtiva.setStatus("CANCELADA"); reconfigurar stub
       // when/then: assertThatThrownBy(...).hasMessageContaining("Matrícula não está ativa")

    4. deveLancarExcecaoQuandoAlunoInativo() — @DisplayName("deve lançar exceção quando aluno está inativo ao adicionar disciplina extra")
       Este é o teste central do DIAG-04. Adicionar comentários inline:
       "// DIAG-04: mesma regra testada em MatriculaServiceImplTest.deveLancarExcecaoQuandoAlunoInativo()"
       "// Nota: mensagem DIFERENTE — \"Aluno inativo\" aqui vs \"Aluno inativo não pode ser matriculado\""
       "// em MatriculaServiceImpl. Esta divergência é o bug silencioso que DIAG-04 prevê."
       // given: alunoAtivo.setAtivo(false); reconfigurar stub
       // when/then: assertThatThrownBy(...).isInstanceOf(RuntimeException.class).hasMessage("Aluno inativo") — mensagem EXATA (string curta sem UUID, per PATTERNS.md)

    5. deveVerificarSeMatriculaEstaAtiva() — @DisplayName("deve retornar true quando matrícula está ativa (método verificador — DIAG-04)")
       Adicionar comentário: "// DIAG-04: proliferação de métodos verificadores — verificarStatusMatricula()"
       "// existe aqui, mas MatriculaServiceImpl não o usa (faz a mesma verificação inline)."
       // given — happy path configurado no @BeforeEach
       // when: boolean resultado = service.verificarStatusMatricula(matriculaAtiva.getId())
       // then: assertThat(resultado).isTrue()

    NÃO usar @SpringBootTest. NÃO usar Mockito.mock() para Aluno ou Matricula. NÃO omitir comentários DIAG (são o mecanismo pedagógico, per D-09).
  </action>

  <verify>
    <automated>cd /home/marcelino/Documentos/erp-matricula-ddd-estudo/erp-matricula-camadas && mvn test -Dtest=DisciplinaServiceImplTest -pl . 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `mvn test -Dtest=DisciplinaServiceImplTest` no módulo erp-matricula-camadas termina com BUILD SUCCESS e "Tests run: 5, Failures: 0, Errors: 0"
    - grep -c "@Mock" DisciplinaServiceImplTest.java retorna 3
    - grep -c "DIAG-04" DisciplinaServiceImplTest.java retorna pelo menos 3 (cabeçalho + teste central + método verificador)
    - O arquivo contém a string exata "Aluno inativo" (sem sufixo) no assertThatThrownBy — mensagem exata de DisciplinaServiceImpl.java:83
    - O arquivo contém comentário sobre divergência de mensagem entre os dois services ("Esta divergência é o bug silencioso" ou equivalente)
    - grep -c "import org.springframework" DisciplinaServiceImplTest.java retorna 0
    - O arquivo contém "verificarStatusMatricula" como nome de método de teste
  </acceptance_criteria>

  <done>
    DisciplinaServiceImplTest.java existe em erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/,
    mvn test passa com 5 testes, 3 campos @Mock, comentários DIAG-04 nos pontos centrais incluindo
    a nota sobre divergência de mensagem entre os dois services, zero imports Spring.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Testes invocam DisciplinaServiceImpl diretamente via @InjectMocks; sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | DisciplinaServiceImplTest.java | accept | Arquivo de teste; não é executado em produção; sem dados sensíveis |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Fase sem novas dependências — todos os pacotes já declarados no pom.xml |
</threat_model>

<verification>
- mvn test -pl erp-matricula-camadas -Dtest=DisciplinaServiceImplTest retorna BUILD SUCCESS
- 5 testes passam
- O teste deveLancarExcecaoQuandoAlunoInativo usa .hasMessage("Aluno inativo") com string exata
- Comentário DIAG-04 explicita que MatriculaServiceImplTest testa a mesma regra
</verification>

<success_criteria>
DisciplinaServiceImplTest.java criado com 5 testes Mockito puros, 3 campos @Mock com comentário DIAG-04,
padrão Given-When-Then, AssertJ fluente, zero imports Spring. O teste de "aluno inativo" contém
comentário explícito sobre a divergência de mensagem como evidência do bug silencioso da duplicação.
</success_criteria>

<output>
Criar .planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-02-SUMMARY.md quando concluído.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Purpose |
|---|---|---|
| `erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/DisciplinaServiceImplTest.java` | new test class | Testes unitários do DisciplinaServiceImpl com Mockito puro — evidência TCAM-02 |
| `DisciplinaServiceImplTest` (class) | JUnit 5 test class | @ExtendWith(MockitoExtension.class), 3 @Mock fields, @InjectMocks DisciplinaServiceImpl |
| `configurarHappyPath()` (method) | @BeforeEach | Setup happy path com stubs dos 3 repositórios |
| `deveAdicionarDisciplinaExtraComSucesso()` | @Test | Happy path de adicionarDisciplinaExtra() |
| `deveLancarExcecaoQuandoMatriculaNaoEncontrada()` | @Test | Error path: matrícula ausente |
| `deveLancarExcecaoQuandoMatriculaNaoEstaAtiva()` | @Test | Error path: matrícula cancelada |
| `deveLancarExcecaoQuandoAlunoInativo()` | @Test | Error path central DIAG-04: regra duplicada com divergência de mensagem |
| `deveVerificarSeMatriculaEstaAtiva()` | @Test | Verifica verificarStatusMatricula() — evidência DIAG-04 proliferação |
</output>
