---
phase: 09-testes-unit-rios-do-m-dulo-camadas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/MatriculaServiceImplTest.java
autonomous: true
requirements:
  - TCAM-01
must_haves:
  truths:
    - "Desenvolvedor executa `mvn test` no módulo camadas e vê `@BeforeEach` com 4 mocks claramente comentados com DIAG-03"
    - "Cada método testado (matricular, adicionarDisciplina, cancelar) tem happy path + pelo menos 1 caso de erro"
    - "Testes de exceção usam assertThatThrownBy com mensagens exatas verificadas no código de produção"
    - "O arquivo de teste evidencia visualmente o custo de isolar o God Service: 4 campos @Mock antes de um único @InjectMocks"
  artifacts:
    - path: "erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/MatriculaServiceImplTest.java"
      provides: "Testes unitários do MatriculaServiceImpl com Mockito puro"
      contains: "@ExtendWith(MockitoExtension.class)"
  key_links:
    - from: "MatriculaServiceImplTest.java"
      to: "MatriculaServiceImpl.java"
      via: "@InjectMocks via construtor 4-args"
      pattern: "@InjectMocks MatriculaServiceImpl"
    - from: "MatriculaServiceImplTest.@BeforeEach"
      to: "DIAG-03"
      via: "comentário inline"
      pattern: "DIAG-03: 4 repositórios mockados"
---

<objective>
Criar MatriculaServiceImplTest.java no módulo erp-matricula-camadas evidenciando TCAM-01:
o custo de mocks necessários para isolar o God Service.

Purpose: Demonstrar ao leitor, através do próprio código de teste, que isolar
MatriculaServiceImpl exige 4 mocks simultâneos — contraste direto com
VerificadorElegibilidadeMatriculaTest (Phase 8) que usa zero mocks.

Output: erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/MatriculaServiceImplTest.java
com 7 métodos de teste cobrindo matricular(), adicionarDisciplina() e cancelar().
</objective>

<execution_context>
@/home/marcelino/.claude/gsd-core/workflows/execute-plan.md
@/home/marcelino/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/ROADMAP.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-CONTEXT.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-RESEARCH.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar MatriculaServiceImplTest com 7 testes Mockito — evidência DIAG-03</name>
  <files>erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/MatriculaServiceImplTest.java</files>

  <read_first>
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaServiceImpl.java — fonte da verdade: construtor 4-args (linhas 46-54), lógica de matricular() (linhas 64-106), adicionarDisciplina() (linhas 116-163), cancelar() (linhas 169-184), mensagens de exceção exatas
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Aluno.java — setters disponíveis: setId(UUID), setNome(String), setAtivo(boolean)
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Turma.java — setters disponíveis: setId(UUID), setCodigo(String)
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/model/Matricula.java — setters necessários para montar matriculaAtiva no @BeforeEach
    - erp-matricula-ddd/src/test/java/br/com/escola/matricula/dominio/servico/VerificadorElegibilidadeMatriculaTest.java — padrão Given-When-Then, AssertJ, nomes em português (contraste de referência: este arquivo usa zero mocks)
  </read_first>

  <action>
    Criar o arquivo criando o diretório src/test/java/br/com/escola/matricula/service/ se não existir.

    Pacote: br.com.escola.matricula.service

    Anotações da classe: @ExtendWith(MockitoExtension.class) + @DisplayName("MatriculaServiceImpl — God Service (DIAG-03)")

    Imports estáticos necessários (per D-13): assertThat e assertThatThrownBy de org.assertj.core.api.Assertions; verify, when, never de org.mockito.Mockito; any de org.mockito.ArgumentMatchers.

    Campos da classe (per D-01, D-02):
    - @Mock MatriculaRepository matriculaRepository
    - @Mock AlunoRepository alunoRepository
    - @Mock TurmaRepository turmaRepository
    - @Mock ItemMatriculaRepository itemMatriculaRepository
    - @InjectMocks MatriculaServiceImpl service
    - campos privados: Aluno alunoAtivo, Turma turma, Matricula matriculaAtiva

    O bloco de declaração dos 4 @Mock deve ter comentário de cabeçalho: "// DIAG-03: 4 repositórios mockados para isolar MatriculaServiceImpl" seguido de "// Contraste: VerificadorElegibilidadeMatriculaTest (Phase 8) usa 0 mocks"

    @BeforeEach configurarHappyPath() (per D-03):
    - Criar UUID alunoId = UUID.randomUUID() local
    - Construir alunoAtivo via new Aluno() + setId(alunoId) + setNome("João da Silva") + setAtivo(true); comentar com "// DIAG-02: Entidade anêmica — construção via setters (sem construtor com args)"
    - Construir turma via new Turma() + setId(UUID.randomUUID()) + setCodigo("TURMA-001")
    - Construir matriculaAtiva via new Matricula() + setId(UUID.randomUUID()) + setAlunoId(alunoId) + setStatus("ATIVA"); notar que alunoId é coordenado (adicionarDisciplina busca aluno por matricula.getAlunoId())
    - Configurar stubs com when(alunoRepository.findById(any(UUID.class))).thenReturn(Optional.of(alunoAtivo)); idem para turmaRepository, matriculaRepository, matriculaRepository.countDisciplinas retornando 3
    - Comentar countDisciplinas stub com "// DIAG-06: regra de limite no repositório, não no modelo"

    Sete métodos @Test (per D-04, D-10, D-11, D-12):

    1. deveMatricularAlunoComSucesso() — @DisplayName("deve matricular aluno com sucesso")
       // given — happy path configurado no @BeforeEach
       // when: UUID id = service.matricular(alunoAtivo.getId(), turma.getId(), "2026-02-01", "2026-06-30")
       // then: assertThat(id).isNotNull() + verify(matriculaRepository).insert(any(Matricula.class))

    2. deveLancarExcecaoQuandoAlunoNaoEncontrado() — @DisplayName("deve lançar exceção quando aluno não encontrado")
       // given: when(alunoRepository.findById(any())).thenReturn(Optional.empty())
       // when/then: assertThatThrownBy(() -> service.matricular(...)).isInstanceOf(RuntimeException.class).hasMessageContaining("Aluno não encontrado")

    3. deveLancarExcecaoQuandoAlunoInativo() — @DisplayName("deve lançar exceção quando aluno está inativo")
       // given: alunoAtivo.setAtivo(false) + reconfigurar stub
       // when/then: assertThatThrownBy(...).isInstanceOf(RuntimeException.class).hasMessage("Aluno inativo não pode ser matriculado")
       // Adicionar comentário: "// Contraste com módulo DDD: lançaria AlunoInativoException (tipo específico)"

    4. deveAdicionarDisciplinaComSucesso() — @DisplayName("deve adicionar disciplina com sucesso")
       // given — happy path configurado no @BeforeEach
       // when: service.adicionarDisciplina(matriculaAtiva.getId(), "Matemática")
       // then: verify(itemMatriculaRepository).insert(any(ItemMatricula.class))

    5. deveLancarExcecaoQuandoMatriculaNaoEstaAtiva() — @DisplayName("deve lançar exceção quando matrícula não está ativa ao adicionar disciplina")
       // given: matriculaAtiva.setStatus("CANCELADA") + reconfigurar stub matriculaRepository.findById
       // when/then: assertThatThrownBy(() -> service.adicionarDisciplina(...)).hasMessageContaining("Matrícula não está ativa")

    6. deveLancarExcecaoQuandoLimiteDisciplinasAtingido() — @DisplayName("deve lançar exceção quando limite de 6 disciplinas atingido")
       // given: when(matriculaRepository.countDisciplinas(any())).thenReturn(6); comentar com "// DIAG-06: limite verificado via banco — countDisciplinas() no repositório"
       // when/then: assertThatThrownBy(...).hasMessageContaining("Limite de 6 disciplinas")

    7. deveCancelarMatriculaComSucesso() — @DisplayName("deve cancelar matrícula com sucesso")
       // given — happy path configurado no @BeforeEach
       // when: service.cancelar(matriculaAtiva.getId())
       // then: verify(matriculaRepository).updateStatus(eq(matriculaAtiva.getId()), eq("CANCELADA"))
       Adicionar import estático org.mockito.ArgumentMatchers.eq para este verify.

    NÃO usar fenced code blocks no action. NÃO testar buscarPorAluno() (per D-04). NÃO usar @SpringBootTest. Criar objetos anêmicos com new + setters — nunca Mockito.mock() para Aluno/Turma/Matricula (per RESEARCH anti-patterns).
  </action>

  <verify>
    <automated>cd /home/marcelino/Documentos/erp-matricula-ddd-estudo/erp-matricula-camadas && mvn test -Dtest=MatriculaServiceImplTest -pl . 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `mvn test -Dtest=MatriculaServiceImplTest` no módulo erp-matricula-camadas termina com BUILD SUCCESS e "Tests run: 7, Failures: 0, Errors: 0"
    - grep -c "@Mock" MatriculaServiceImplTest.java retorna 4
    - grep -c "@InjectMocks" MatriculaServiceImplTest.java retorna 1
    - grep -c "DIAG-03" MatriculaServiceImplTest.java retorna pelo menos 1
    - grep -c "DIAG-02" MatriculaServiceImplTest.java retorna pelo menos 1
    - grep -c "DIAG-06" MatriculaServiceImplTest.java retorna pelo menos 1
    - grep -c "import org.springframework" MatriculaServiceImplTest.java retorna 0 (zero imports Spring)
    - grep -c "assertThatThrownBy" MatriculaServiceImplTest.java retorna pelo menos 4 (testes de exceção)
    - O arquivo contém "Aluno inativo não pode ser matriculado" — mensagem exata conforme MatriculaServiceImpl.java:76
    - O arquivo contém ".hasMessageContaining(\"Limite de 6 disciplinas\")" — mensagem parcial conforme MatriculaServiceImpl.java:151
  </acceptance_criteria>

  <done>
    MatriculaServiceImplTest.java existe em erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/,
    mvn test passa com 7 testes, zero imports Spring, 4 campos @Mock visíveis com comentário DIAG-03,
    comentários DIAG-02 e DIAG-06 presentes nos pontos-chave.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Testes invocam MatriculaServiceImpl diretamente (sem rede); sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | MatriculaServiceImplTest.java | accept | Arquivo de teste; não é executado em produção; sem dados sensíveis |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Fase sem novas dependências — todos os pacotes já declarados no pom.xml e verificados no Package Legitimacy Audit do RESEARCH.md |
</threat_model>

<verification>
- mvn test -pl erp-matricula-camadas -Dtest=MatriculaServiceImplTest retorna BUILD SUCCESS
- 7 testes passam (deveMatricularAlunoComSucesso, deveLancarExcecaoQuandoAlunoNaoEncontrado, deveLancarExcecaoQuandoAlunoInativo, deveAdicionarDisciplinaComSucesso, deveLancarExcecaoQuandoMatriculaNaoEstaAtiva, deveLancarExcecaoQuandoLimiteDisciplinasAtingido, deveCancelarMatriculaComSucesso)
- grep -c "import org.springframework" MatriculaServiceImplTest.java == 0
</verification>

<success_criteria>
MatriculaServiceImplTest.java criado com 7 testes Mockito puros, 4 campos @Mock com comentário DIAG-03,
padrão Given-When-Then com comentários em português, AssertJ fluente, zero imports Spring.
A leitura do arquivo evidencia visualmente o custo de isolar o God Service.
</success_criteria>

<output>
Criar .planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-01-SUMMARY.md quando concluído.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Purpose |
|---|---|---|
| `erp-matricula-camadas/src/test/java/br/com/escola/matricula/service/MatriculaServiceImplTest.java` | new test class | Testes unitários do MatriculaServiceImpl com Mockito puro — evidência TCAM-01 |
| `MatriculaServiceImplTest` (class) | JUnit 5 test class | @ExtendWith(MockitoExtension.class), 4 @Mock fields, @InjectMocks MatriculaServiceImpl |
| `configurarHappyPath()` (method) | @BeforeEach | Setup happy path com stubs padrão dos 4 repositórios |
| `deveMatricularAlunoComSucesso()` | @Test | Happy path de matricular() |
| `deveLancarExcecaoQuandoAlunoNaoEncontrado()` | @Test | Error path: aluno ausente |
| `deveLancarExcecaoQuandoAlunoInativo()` | @Test | Error path: aluno inativo (DIAG-03 contraste) |
| `deveAdicionarDisciplinaComSucesso()` | @Test | Happy path de adicionarDisciplina() |
| `deveLancarExcecaoQuandoMatriculaNaoEstaAtiva()` | @Test | Error path: matrícula cancelada |
| `deveLancarExcecaoQuandoLimiteDisciplinasAtingido()` | @Test | Error path: limite 6 disciplinas (DIAG-06) |
| `deveCancelarMatriculaComSucesso()` | @Test | Happy path de cancelar() |
</output>
