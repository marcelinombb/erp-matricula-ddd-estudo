---
phase: 09-testes-unit-rios-do-m-dulo-camadas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - erp-matricula-camadas/src/test/java/br/com/escola/matricula/controller/MatriculaControllerTest.java
autonomous: true
requirements:
  - TCAM-03
must_haves:
  truths:
    - "Desenvolvedor executa testes do MatriculaController e vê @WebMvcTest — Spring context parcial necessário para testar regras no controller (contraste com @ExtendWith(MockitoExtension.class) dos services)"
    - "O teste deveBloquearPeriodoAntigo_nuncaChamarService confirma via verify(matriculaService, never()) que o service não foi chamado — a regra parou no controller (DIAG-05)"
    - "O teste deveBloquearNomeDisciplinaCurto confirma via verify(matriculaService, never()).adicionarDisciplina() que a regra de comprimento é invisível para o service (DIAG-05)"
    - "O happy path POST /matriculas retorna 201 e verify(matriculaService).matricular() confirma que o service foi chamado quando as regras do controller são satisfeitas"
  artifacts:
    - path: "erp-matricula-camadas/src/test/java/br/com/escola/matricula/controller/MatriculaControllerTest.java"
      provides: "Testes unitários do MatriculaController com @WebMvcTest — evidência TCAM-03"
      contains: "@WebMvcTest(MatriculaController.class)"
  key_links:
    - from: "MatriculaControllerTest.java"
      to: "MatriculaController.java"
      via: "@WebMvcTest + MockMvc HTTP in-process"
      pattern: "@WebMvcTest(MatriculaController.class)"
    - from: "MatriculaControllerTest.deveBloquearPeriodoAntigo"
      to: "DIAG-05"
      via: "verify(matriculaService, never())"
      pattern: "never\\(\\)\\.matricular"
---

<objective>
Criar MatriculaControllerTest.java no módulo erp-matricula-camadas evidenciando TCAM-03:
a dificuldade de testar regras de negócio que vivem no controller — exige Spring context parcial
(@WebMvcTest) e MockMvc para ativar Bean Validation e interceptar as regras DIAG-05.

Purpose: Demonstrar que as regras "período 199x" e "nome < 3 chars" existem APENAS no controller —
verify(service, never()) prova que o service nunca é chamado quando essas regras são violadas,
tornando o bug de bypass (batch jobs, chamadas diretas ao service) visível através do teste.

Output: erp-matricula-camadas/src/test/java/br/com/escola/matricula/controller/MatriculaControllerTest.java
com 3 métodos de teste cobrindo os cenários DIAG-05 (D-08): bloqueio de período 199x,
bloqueio de nome curto, e happy path POST /matriculas.
</objective>

<execution_context>
@/home/marcelino/.claude/gsd-core/workflows/execute-plan.md
@/home/marcelino/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/ROADMAP.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-CONTEXT.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-RESEARCH.md
@/home/marcelino/Documentos/erp-matricula-ddd-estudo/.planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar MatriculaControllerTest com 3 testes @WebMvcTest — evidência DIAG-05</name>
  <files>erp-matricula-camadas/src/test/java/br/com/escola/matricula/controller/MatriculaControllerTest.java</files>

  <read_first>
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/controller/MatriculaController.java — fonte da verdade: regra "199x" (linha 116), regra "nome < 3 chars" (linha 158), assinatura de matriculaService.matricular() (linha 123-128), assinatura de matriculaService.adicionarDisciplina() (linha 165), mapeamento POST /matriculas e POST /matriculas/{id}/disciplinas
    - erp-matricula-camadas/src/main/java/br/com/escola/matricula/service/MatriculaService.java — assinaturas exatas dos métodos para montar o stub when() e o verify() corretos
    - erp-matricula-camadas/pom.xml — confirmar spring-boot-starter-test scope=test e presença de spring-boot-starter-web (necessário para MockMvc)
  </read_first>

  <action>
    Criar o arquivo no novo diretório src/test/java/br/com/escola/matricula/controller/.

    Pacote: br.com.escola.matricula.controller

    Imports de classe (per D-07, PATTERNS.md):
    - br.com.escola.matricula.service.MatriculaService
    - com.fasterxml.jackson.databind.ObjectMapper
    - jakarta.validation.Valid (não necessário — apenas para referência do controller)
    - org.junit.jupiter.api.DisplayName, org.junit.jupiter.api.Test
    - org.springframework.beans.factory.annotation.Autowired
    - org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest
    - org.springframework.boot.test.mock.mockito.MockBean
    - org.springframework.http.MediaType
    - org.springframework.test.web.servlet.MockMvc
    - java.util.UUID
    Imports estáticos: org.mockito.ArgumentMatchers.any; org.mockito.Mockito.never, verify, when; org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post; org.springframework.test.web.servlet.result.MockMvcResultMatchers.status

    Anotação da classe: @WebMvcTest(MatriculaController.class) + @DisplayName("MatriculaController — Regras na Interface (DIAG-05)")

    Campos da classe (per D-07):
    - @Autowired MockMvc mockMvc
    - @MockBean MatriculaService matriculaService — adicionar comentário DIAG-05: "// DIAG-05: Para testar regras no Controller, precisamos de Spring context parcial (@WebMvcTest)." + "// Contraste com MatriculaServiceImplTest: aquele usa @ExtendWith(MockitoExtension) — zero Spring."
    - @Autowired ObjectMapper objectMapper

    Três métodos @Test (per D-08, D-10, D-11, D-12):

    1. deveBloquearPeriodoAntigo_nuncaChamarService() — @DisplayName("deve bloquear período 199x e nunca chamar o service")
       // given: JSON com periodoInicio "1999-02-01" e periodoFim "1999-06-30"; usar text block Java com .formatted(UUID.randomUUID(), UUID.randomUUID()) para alunoId e turmaId
       // when: mockMvc.perform(post("/matriculas").contentType(MediaType.APPLICATION_JSON).content(json)).andExpect(status().isBadRequest())
       // then: verify(matriculaService, never()).matricular(any(), any(), any(), any())
       Comentário final: "// DIAG-05: regra temporal existe APENAS no controller" + "// Se um batch job chamar matriculaService.matricular() diretamente com '1999-02-01'," + "// a matrícula será criada sem erro — a regra só existe aqui."

    2. deveBloquearNomeDisciplinaCurto() — @DisplayName("deve bloquear nome de disciplina com menos de 3 caracteres")
       // given: JSON {"nomeDisciplina": "AB"} — 2 chars, abaixo do mínimo de 3
       // when: mockMvc.perform(post("/matriculas/{id}/disciplinas", UUID.randomUUID()).contentType(MediaType.APPLICATION_JSON).content(json)).andExpect(status().isBadRequest())
       // then: verify(matriculaService, never()).adicionarDisciplina(any(), any())
       Comentário: "// DIAG-05: regra de comprimento existe só no controller" + "// MatriculaService.adicionarDisciplina() aceita 'AB' sem erro se chamado diretamente"

    3. deveMatricularComSucesso() — @DisplayName("deve matricular com sucesso no happy path")
       // given: when(matriculaService.matricular(any(), any(), any(), any())).thenReturn(UUID.randomUUID()); JSON com periodoInicio "2026-02-01" (não começa com "199")
       // when: mockMvc.perform(post("/matriculas").contentType(MediaType.APPLICATION_JSON).content(json)).andExpect(status().isCreated())
       // then: verify(matriculaService).matricular(any(), any(), any(), any())

    IMPORTANTE — fallback para @WebMvcTest:
    Se o teste falhar na inicialização com erro de MyBatis (UnsatisfiedDependencyException ou BeanCreationException mencionando MybatisAutoConfiguration ou DataSource), criar TAMBÉM o arquivo src/test/resources/application.properties com conteúdo:
    spring.autoconfigure.exclude=org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration,org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
    Este fallback é descrito no RESEARCH.md Pitfall 1. Tentar sem o arquivo primeiro; criar só se o contexto falhar.

    NÃO usar @SpringBootTest. NÃO usar MockMvcBuilders.standaloneSetup() (não ativa Bean Validation). NÃO declarar @MockBean para repositórios — o controller só injeta MatriculaService.
  </action>

  <verify>
    <automated>cd /home/marcelino/Documentos/erp-matricula-ddd-estudo/erp-matricula-camadas && mvn test -Dtest=MatriculaControllerTest -pl . 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `mvn test -Dtest=MatriculaControllerTest` no módulo erp-matricula-camadas termina com BUILD SUCCESS e "Tests run: 3, Failures: 0, Errors: 0"
    - grep -c "@WebMvcTest" MatriculaControllerTest.java retorna 1
    - grep -c "@MockBean" MatriculaControllerTest.java retorna 1 (apenas MatriculaService)
    - O arquivo contém "never()" — verify(matriculaService, never()) em pelo menos 2 testes
    - O arquivo contém "DIAG-05" em pelo menos 2 locais
    - O arquivo contém "1999-02-01" como valor de periodoInicio no teste de período antigo
    - O arquivo contém '"AB"' como nomeDisciplina no teste de nome curto
    - O arquivo contém ".andExpect(status().isCreated())" no happy path test
    - O arquivo NÃO contém "MockMvcBuilders.standaloneSetup"
  </acceptance_criteria>

  <done>
    MatriculaControllerTest.java existe em erp-matricula-camadas/src/test/java/br/com/escola/matricula/controller/,
    mvn test passa com 3 testes, @WebMvcTest + @MockBean MatriculaService, verify(service, never())
    nos dois testes de bloqueio, comentários DIAG-05 explicando que as regras existem APENAS no controller.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → HTTP layer | MockMvc faz requests HTTP in-process; sem rede real; sem dados sensíveis |
| Spring context parcial → beans | @WebMvcTest sobe apenas web layer; beans de infraestrutura (MyBatis, DataSource) não são instanciados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | MatriculaControllerTest.java | accept | Arquivo de teste; não é executado em produção; sem dados sensíveis |
| T-09-04 | Information Disclosure | @WebMvcTest context startup | accept | Contexto parcial isolado; nenhum segredo de produção carregado; application.yml de produção não é carregado no contexto de teste por padrão |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Fase sem novas dependências — todos os pacotes já declarados no pom.xml |
</threat_model>

<verification>
- mvn test -pl erp-matricula-camadas -Dtest=MatriculaControllerTest retorna BUILD SUCCESS
- 3 testes passam (deveBloquearPeriodoAntigo_nuncaChamarService, deveBloquearNomeDisciplinaCurto, deveMatricularComSucesso)
- Os dois testes de bloqueio contêm verify(matriculaService, never())
- O happy path retorna 201 Created
</verification>

<success_criteria>
MatriculaControllerTest.java criado com 3 testes @WebMvcTest, @MockBean MatriculaService,
verify(service, never()) nos testes de bloqueio, comentários DIAG-05 explicando
a invisibilidade das regras para chamadores que bypassam o HTTP.
</success_criteria>

<output>
Criar .planning/phases/09-testes-unit-rios-do-m-dulo-camadas/09-03-SUMMARY.md quando concluído.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Purpose |
|---|---|---|
| `erp-matricula-camadas/src/test/java/br/com/escola/matricula/controller/MatriculaControllerTest.java` | new test class | Testes unitários do MatriculaController com @WebMvcTest — evidência TCAM-03 |
| `MatriculaControllerTest` (class) | JUnit 5 test class | @WebMvcTest(MatriculaController.class), @MockBean MatriculaService, @Autowired MockMvc |
| `deveBloquearPeriodoAntigo_nuncaChamarService()` | @Test | Teste DIAG-05: período 199x bloqueado no controller, service nunca chamado |
| `deveBloquearNomeDisciplinaCurto()` | @Test | Teste DIAG-05: nome < 3 chars bloqueado no controller, service nunca chamado |
| `deveMatricularComSucesso()` | @Test | Happy path POST /matriculas retorna 201, service foi chamado |
| `erp-matricula-camadas/src/test/resources/application.properties` (condicional) | test resource | Fallback para excluir MyBatisAutoConfiguration se @WebMvcTest falhar na inicialização |
</output>
