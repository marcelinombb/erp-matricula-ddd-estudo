---
phase: 04-interface-docker-e-material-didatico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - erp-matricula-app/pom.xml
  - erp-matricula-app/src/main/resources/application.yml
  - erp-matricula-app/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql
  - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java
  - erp-matricula-app/src/main/resources/mapper/MatriculaMapper.xml
  - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java
  - erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java
  - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java
autonomous: true
requirements:
  - IFX-03
  - DCK-01

must_haves:
  truths:
    - "A aplicação sobe como servidor web Spring Boot na porta 8080 (sem web-application-type: none)"
    - "Flyway V3 adiciona coluna adicionada_em em itens_matricula com DEFAULT NOW() sem quebrar seeds V2"
    - "MatriculaDto retorna a lista de disciplinas com {nome, adicionadaEm} em cada item"
    - "DemoRunner não falha na segunda execução pelo índice único uq_matricula_aluno_periodo_ativa"
  artifacts:
    - path: "erp-matricula-app/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql"
      provides: "Migration que adiciona adicionada_em em itens_matricula"
      contains: "ADD COLUMN adicionada_em"
    - path: "erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java"
      provides: "DTO com campo disciplinas incluído"
      exports: ["MatriculaDto", "ItemDto"]
  key_links:
    - from: "MatriculaMapper.xml (ResultMap)"
      to: "ItemMatriculaRow.adicionadaEm"
      via: "column alias item_adicionada_em"
      pattern: "item_adicionada_em"
    - from: "MatriculaRowMapper.toDomain"
      to: "ItemMatricula"
      via: "item.adicionadaEm passado na construção"
      pattern: "adicionadaEm"
---

<objective>
Ativar a camada web Spring Boot e preparar a infraestrutura de dados que suporta o campo `disciplinas` no DTO de resposta da API.

Purpose: Sem este plano, a aplicação não serve requisições HTTP (web-application-type: none) e o MatriculaDto não inclui a lista de disciplinas com timestamps. Todos os planos subsequentes dependem desta fundação.

Output:
- pom.xml com spring-boot-starter-web e spring-boot-starter-validation
- application.yml sem web-application-type: none
- V3__adicionar_adicionada_em.sql (Flyway migration)
- ItemMatriculaRow com campo adicionadaEm
- MatriculaMapper.xml com alias item_adicionada_em
- MatriculaRowMapper atualizado para mapear adicionadaEm
- MatriculaDto com List<ItemDto> disciplinas + record ItemDto
- DemoRunner protegido contra falha na segunda execução
</objective>

<execution_context>
@/home/marcelino/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcelino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-interface-docker-e-material-didatico/04-CONTEXT.md
@.planning/phases/04-interface-docker-e-material-didatico/04-RESEARCH.md
@.planning/phases/04-interface-docker-e-material-didatico/04-PATTERNS.md

<interfaces>
<!-- Interfaces e estruturas relevantes extraídas do codebase. Executor não precisa explorar. -->

De erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/modelo/ItemMatricula.java:
  public record ItemMatricula(NomeDisciplina disciplina) {}
  // NOTA: o record não tem campo adicionadaEm ainda — não alterar o modelo de domínio.
  // adicionadaEm fica no DTO (camada de aplicação), não no domínio.

De erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java:
  public UUID matriculaId;
  public String disciplina;
  // Adicionar: public LocalDateTime adicionadaEm;

De erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java (record atual):
  public record MatriculaDto(String matriculaId, String alunoId, String statusDescricao, int totalDisciplinas)
  // Adicionar: List<ItemDto> disciplinas
  // factory method: MatriculaDto.de(Matricula) — atualizar para mapear getDisciplinas()

De erp-matricula-app/src/main/resources/mapper/MatriculaMapper.xml (collection atual):
  <collection property="itens" ofType="ItemMatriculaRow" notNullColumn="item_disciplina">
    <result property="disciplina" column="item_disciplina"/>
  </collection>
  // SELECT inclui: i.disciplina AS item_disciplina
  // Adicionar: i.adicionada_em AS item_adicionada_em + <result> no ResultMap

De erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java:
  var disciplinas = row.itens.stream()
      .map(item -> new ItemMatricula(new NomeDisciplina(item.disciplina)))
      .toList();
  // ItemMatricula não muda — adicionadaEm fica no MatriculaDto.ItemDto, não no domain model

De erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java:
  @Component public class DemoRunner implements CommandLineRunner { ... }
  // Adicionar @ConditionalOnProperty(name = "demo.runner.enabled", havingValue = "true", matchIfMissing = false)
  // OU envolver o run() em try/catch que loga WARNING em vez de lançar exception
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Ativar dependências web no pom.xml e remover web-application-type do application.yml</name>
  <files>
    erp-matricula-app/pom.xml
    erp-matricula-app/src/main/resources/application.yml
  </files>
  <read_first>
    - erp-matricula-app/pom.xml (ler inteiro antes de modificar — entender onde inserir as dependências após spring-boot-starter)
    - erp-matricula-app/src/main/resources/application.yml (ler inteiro — identificar o bloco spring.main.web-application-type: none a remover)
  </read_first>
  <action>
No pom.xml: adicionar duas dependências sem versão (gerenciadas pelo BOM spring-boot-starter-parent 3.5.3) logo após a declaração de spring-boot-starter (linha ~54). As dependências são spring-boot-starter-web e spring-boot-starter-validation, ambas com groupId org.springframework.boot. Remover o comentário pedagógico da Fase 3 que diz "spring-boot-starter-web NÃO está incluído intencionalmente" (linhas 5-9 do bloco de dependências) e substituir por comentário que explica a Fase 4 agora ativa a camada web. Manter o estilo de indentação e declaração existente (sem versão explícita).

No application.yml: remover o bloco completo spring.main.web-application-type: none incluindo o seu bloco de comentário pedagógico (linhas 33-40 aproximadamente — o bloco que começa com "# web-application-type: none" e inclui a explicação pedagógica). Substituir por um comentário curto que explica: "Fase 4 ativa o servidor HTTP. Spring Boot detecta spring-boot-starter-web automaticamente." Manter todas as outras configurações (datasource, flyway, mybatis, logging) intactas. Não adicionar server.port — 8080 é o default do Spring Boot.
  </action>
  <verify>
    <automated>grep -c "spring-boot-starter-web" erp-matricula-app/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - pom.xml contém spring-boot-starter-web como dependência (sem versão explícita)
    - pom.xml contém spring-boot-starter-validation como dependência (sem versão explícita)
    - application.yml NÃO contém a linha "web-application-type: none"
    - application.yml NÃO contém a linha "web-application-type" em nenhum contexto
    - application.yml mantém datasource, flyway, mybatis e logging intactos
    - grep "web-application-type" erp-matricula-app/src/main/resources/application.yml retorna vazio
  </acceptance_criteria>
  <done>pom.xml declara as duas dependências web; application.yml não contém web-application-type: none</done>
</task>

<task type="auto">
  <name>Tarefa 2: Flyway V3 e atualização do modelo relacional (ItemMatriculaRow + MatriculaMapper.xml)</name>
  <files>
    erp-matricula-app/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql
    erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java
    erp-matricula-app/src/main/resources/mapper/MatriculaMapper.xml
  </files>
  <read_first>
    - erp-matricula-app/src/main/resources/db/migration/V1__schema.sql (padrão de cabeçalho SQL e estilo de comentários)
    - erp-matricula-app/src/main/resources/db/migration/V2__seeds.sql (confirmar que seeds inserem em itens_matricula SEM adicionada_em — o DEFAULT NOW() deve cobrir isso)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java (ler inteiro — adicionar campo adicionadaEm)
    - erp-matricula-app/src/main/resources/mapper/MatriculaMapper.xml (ler inteiro — identificar o SELECT buscarPorId e o ResultMap da collection de itens para adicionar alias e result)
  </read_first>
  <action>
CRIAR erp-matricula-app/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql: seguir o padrão de cabeçalho de V1__schema.sql (bloco de comentário com separadores ===). O cabeçalho deve explicar que a coluna adicionada_em é necessária para MatriculaDto.ItemDto (D-04) e que DEFAULT NOW() garante retrocompatibilidade com os seeds V2. O corpo SQL deve conter: ALTER TABLE itens_matricula ADD COLUMN adicionada_em TIMESTAMP NOT NULL DEFAULT NOW(); seguido de COMMENT ON COLUMN com mensagem em português explicando o propósito.

MODIFICAR ItemMatriculaRow.java: adicionar import java.time.LocalDateTime no topo (após import java.util.UUID). Adicionar campo público LocalDateTime adicionadaEm com Javadoc no padrão existente da classe: "Timestamp de quando a disciplina foi adicionada — coluna itens_matricula.adicionada_em (Fase 4, D-04)".

MODIFICAR MatriculaMapper.xml: no SELECT buscarPorId (e no SELECT buscarPorAlunoId, se existir), adicionar o alias SQL i.adicionada_em AS item_adicionada_em à cláusula SELECT. No ResultMap MatriculaResultMap dentro do bloco collection de itens, adicionar: result property="adicionadaEm" column="item_adicionada_em". Manter o comentário pedagógico existente sobre notNullColumn e jdbcType=OTHER intacto.

NÃO modificar o INSERT inserirItens — o campo adicionada_em usa DEFAULT NOW() no banco, então não é passado no INSERT.
  </action>
  <verify>
    <automated>grep -c "adicionada_em" erp-matricula-app/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo V3__adicionar_adicionada_em.sql existe em erp-matricula-app/src/main/resources/db/migration/
    - V3 contém "ALTER TABLE itens_matricula" e "ADD COLUMN adicionada_em"
    - V3 contém "NOT NULL DEFAULT NOW()"
    - ItemMatriculaRow.java contém "public LocalDateTime adicionadaEm"
    - ItemMatriculaRow.java contém "import java.time.LocalDateTime"
    - MatriculaMapper.xml contém "item_adicionada_em" como alias de coluna no SELECT
    - MatriculaMapper.xml contém result property="adicionadaEm" column="item_adicionada_em" dentro do bloco collection
    - O INSERT inserirItens em MatriculaMapper.xml NÃO menciona adicionada_em (usa DEFAULT do banco)
  </acceptance_criteria>
  <done>V3 criada; ItemMatriculaRow tem campo adicionadaEm; MatriculaMapper.xml retorna o timestamp via alias SQL</done>
</task>

<task type="auto">
  <name>Tarefa 3: Atualizar MatriculaDto com ItemDto, MatriculaRowMapper e proteger DemoRunner</name>
  <files>
    erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java
    erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java
    erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java
  </files>
  <read_first>
    - erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java (ler inteiro — entender o factory method de(Matricula) antes de modificar)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/modelo/ItemMatricula.java (confirmar que ItemMatricula só tem NomeDisciplina, sem adicionadaEm — a timestamp fica no DTO)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/MatriculaRowMapper.java (ler inteiro — entender toDomain e o mapeamento de itens nas linhas 75-77)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java (ler inteiro — entender o CommandLineRunner.run() para proteger contra falha na segunda execução)
  </read_first>
  <action>
MODIFICAR MatriculaDto.java: adicionar import java.time.LocalDateTime e import java.util.List. Adicionar campo List&lt;ItemDto&gt; disciplinas ao record como último campo. Adicionar record interno público estático ItemDto(String nome, LocalDateTime adicionadaEm) — padrão Java 21 record aninhado, sem Lombok. No factory method de(Matricula): no bloco de construção do MatriculaDto, mapear matricula.getDisciplinas() para List&lt;ItemDto&gt; usando stream().map(item -> new ItemDto(item.disciplina().valor(), null)).toList() — adicionadaEm é null aqui porque o domínio não carrega o timestamp (ItemMatricula não tem esse campo). O timestamp só é populado quando MatriculaDto é construído a partir de MatriculaRow (caminho de leitura via repositório). Adicionar Javadoc explicando que adicionadaEm é null quando construído via factory method de(Matricula) diretamente — é apenas populado pelo MatriculaRowMapper.

MODIFICAR MatriculaRowMapper.java: atualizar o método toDomain() para que o mapeamento de itens passe o adicionadaEm: row.itens.stream().map(item -> ...). Como ItemMatricula é um record sem campo de timestamp, o adicionadaEm permanece apenas nos rows. O MatriculaRowMapper precisa de um método ou adaptação para montar MatriculaDto diretamente com timestamps. Estratégia: adicionar método público MatriculaDto toDtoWithTimestamps(MatriculaRow row) que constrói MatriculaDto passando a lista de ItemDto com adicionadaEm populado de item.adicionadaEm. Os UseCases que retornam MatriculaDto a partir de um Aggregate (não via row) continuam usando MatriculaDto.de(matricula). O MatriculaRepositorioMyBatis.buscarPorId() retorna Matricula (domain model) — o Controller usa MatriculaDto.de(matricula) para resposta. Ver nota abaixo sobre abordagem simplificada.

NOTA SOBRE ABORDAGEM DE adicionadaEm: como o Controller não precisa expor GET /matriculas/{id} nesta fase (apenas os três POST), e os UseCases retornam MatriculaDto.de(matricula) com adicionadaEm=null, é aceitável para esta fase. O campo fica no DTO preparado para quando uma query de leitura for adicionada. Não criar complexidade adicional — simplesmente garantir que MatriculaDto.ItemDto existe e adicionadaEm pode ser null.

MODIFICAR DemoRunner.java: envolver todo o corpo do método run() em um bloco try/catch(Exception e). No catch, logar como WARNING: "DemoRunner: fluxos já executados ou erro esperado na re-execução — {e.getMessage()}". Isso evita que o DemoRunner derrube o startup na segunda execução quando o índice único uq_matricula_aluno_periodo_ativa já existe. Adicionar Javadoc ao método run() explicando que o try/catch protege contra re-execução (seeds já inseridos, matrícula já criada) e que na Fase 4 o ponto de entrada principal são os Controllers REST.
  </action>
  <verify>
    <automated>grep -c "ItemDto" erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java</automated>
  </verify>
  <acceptance_criteria>
    - MatriculaDto.java contém "List&lt;ItemDto&gt; disciplinas" como campo do record
    - MatriculaDto.java contém record aninhado "ItemDto" com campos "nome" e "adicionadaEm"
    - MatriculaDto.java contém import java.util.List
    - MatriculaDto.java contém import java.time.LocalDateTime
    - DemoRunner.java contém bloco try/catch em torno do corpo do método run()
    - grep "try {" erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java retorna resultado positivo
    - MatriculaRowMapper.java compilável — nenhuma referência a campo inexistente em ItemMatricula
  </acceptance_criteria>
  <done>MatriculaDto inclui ItemDto com adicionadaEm; DemoRunner não falha na segunda execução</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway migration | Script SQL aplicado automaticamente no startup — deve ser idempotente |
| application.yml | Configuração de datasource com credenciais em texto — aceitável para dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | V3__adicionar_adicionada_em.sql | accept | Migration Flyway é checksumada; qualquer alteração pós-aplicação causa falha de startup — comportamento nativo do Flyway |
| T-04-02 | Information Disclosure | application.yml (credenciais datasource) | accept | Credenciais são de desenvolvimento local (matricula/matricula); Docker Compose (Plano 03) usa env vars com defaults — sem credenciais hardcoded no Compose |
| T-04-SC | Tampering | Maven BOM (spring-boot-starter-web, spring-boot-starter-validation) | accept | Módulos oficiais Spring Boot gerenciados pelo BOM declarado; origem Maven Central; auditoria no RESEARCH.md confirma disposição Aprovado |
</threat_model>

<verification>
Verificação de completude do Plano 01:
1. grep "spring-boot-starter-web" erp-matricula-app/pom.xml — deve retornar 1+ linhas
2. grep "web-application-type" erp-matricula-app/src/main/resources/application.yml — deve retornar vazio
3. ls erp-matricula-app/src/main/resources/db/migration/V3__adicionar_adicionada_em.sql — deve existir
4. grep "adicionadaEm" erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/persistencia/ItemMatriculaRow.java — deve retornar o campo
5. grep "item_adicionada_em" erp-matricula-app/src/main/resources/mapper/MatriculaMapper.xml — deve aparecer no SELECT e no ResultMap
6. grep "ItemDto" erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java — deve aparecer
7. grep "try {" erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java — deve aparecer no método run()
</verification>

<success_criteria>
- application.yml não contém web-application-type: none
- pom.xml declara spring-boot-starter-web e spring-boot-starter-validation
- V3__adicionar_adicionada_em.sql existe com ALTER TABLE itens_matricula ADD COLUMN adicionada_em TIMESTAMP NOT NULL DEFAULT NOW()
- ItemMatriculaRow tem campo public LocalDateTime adicionadaEm
- MatriculaMapper.xml retorna item_adicionada_em no SELECT e mapeia para adicionadaEm no ResultMap
- MatriculaDto tem List&lt;ItemDto&gt; disciplinas e record interno ItemDto(nome, adicionadaEm)
- DemoRunner.run() está protegido por try/catch que loga WARNING em vez de propagar exception
</success_criteria>

<output>
Criar .planning/phases/04-interface-docker-e-material-didatico/04-01-SUMMARY.md quando concluído
</output>
