---
phase: 04-interface-docker-e-material-didatico
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/MatriculaController.java
  - erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java
autonomous: true
requirements:
  - IFX-01
  - IFX-02
  - IFX-03

must_haves:
  truths:
    - "POST /matriculas aceita {alunoId, turmaId, periodoInicio, periodoFim} e retorna 201 com MatriculaDto"
    - "POST /matriculas/{id}/disciplinas aceita {nome} e retorna 200 com MatriculaDto atualizado"
    - "POST /matriculas/{id}/cancelamento aceita corpo vazio e retorna 200 com MatriculaDto cancelado"
    - "Payload inválido em qualquer endpoint retorna 400 com {erro, mensagem, campos: [{campo, mensagem}]}"
    - "DisciplinaJaMatriculadaException retorna 409; LimiteDisciplinasExcedidoException retorna 422 com limite e atual"
    - "O domínio nunca importa HttpStatus ou ResponseEntity — apenas ExcecaoHandler conhece HTTP"
  artifacts:
    - path: "erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/MatriculaController.java"
      provides: "Controller REST com os três endpoints"
      exports: ["MatriculaController", "MatricularAlunoRequest", "AdicionarDisciplinaRequest"]
    - path: "erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java"
      provides: "Handler global de exceções com mapeamento domínio → HTTP"
      exports: ["ExcecaoHandler"]
  key_links:
    - from: "MatriculaController.matricular()"
      to: "MatricularAlunoUseCase.executar(MatricularAlunoCommand)"
      via: "construção de Aluno/Turma/PeriodoLetivo a partir de primitivos HTTP"
      pattern: "matricularUseCase.executar"
    - from: "ExcecaoHandler"
      to: "dominio/excecao/*.java"
      via: "@ExceptionHandler por tipo de exceção"
      pattern: "@ExceptionHandler"
---

<objective>
Implementar a camada de interface REST: MatriculaController com os três endpoints de negócio e ExcecaoHandler como único ponto de tradução exceção-de-domínio → resposta HTTP.

Purpose: Este é o entregável central do IFX-01..03 — a API REST que permite testar os três fluxos de matrícula via HTTP. O ExcecaoHandler é o ponto pedagógico explicitado no Success Criteria 2 da fase: demonstra que exceções de domínio cruzam a fronteira para HTTP em um único lugar, e que o domínio nunca importa tipos HTTP.

Output:
- Pacote br.com.escola.matricula.interfaces/ com dois arquivos Java
- MatriculaController com @RestController, três @PostMapping, records de request sem Lombok, injeção por construtor
- ExcecaoHandler com @ControllerAdvice, seis @ExceptionHandler mapeando todas as exceções de domínio
</objective>

<execution_context>
@/home/marcelino/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcelino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-interface-docker-e-material-didatico/04-CONTEXT.md
@.planning/phases/04-interface-docker-e-material-didatico/04-RESEARCH.md
@.planning/phases/04-interface-docker-e-material-didatico/04-PATTERNS.md
@.planning/phases/04-interface-docker-e-material-didatico/04-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos do codebase — executor não precisa explorar. -->

Pacote br.com.escola.matricula.aplicacao (UseCases e Commands):

MatricularAlunoUseCase.executar(MatricularAlunoCommand command) → MatriculaId
MatricularAlunoCommand(Aluno aluno, Turma turma, PeriodoLetivo periodo)

AdicionarDisciplinaUseCase.executar(AdicionarDisciplinaCommand command) → MatriculaDto
AdicionarDisciplinaCommand(MatriculaId matriculaId, NomeDisciplina disciplina)

CancelarMatriculaUseCase.executar(CancelarMatriculaCommand command) → MatriculaDto
CancelarMatriculaCommand(MatriculaId matriculaId)

MatriculaDto(String matriculaId, String alunoId, String statusDescricao, int totalDisciplinas, List&lt;ItemDto&gt; disciplinas)
MatriculaDto.ItemDto(String nome, LocalDateTime adicionadaEm)

Pacote br.com.escola.matricula.dominio.excecao (todas as exceções de domínio):

DisciplinaJaMatriculadaException extends RuntimeException
  → getLimite() NÃO existe; getMessage() retorna mensagem com nome da disciplina
MatriculaCanceladaException extends RuntimeException
LimiteDisciplinasExcedidoException extends RuntimeException
  → getLimite() → int, getAtual() → int, getMessage() → String
AlunoInativoException extends RuntimeException
MatriculaNaoEncontradaException extends RuntimeException
MatriculaDuplicadaException extends RuntimeException
  → mapear como 409 (variante de DisciplinaJaMatriculada)
PeriodoFechadoException extends RuntimeException
  → mapear como 422

Pacote br.com.escola.matricula.dominio (VOs e modelos necessários no Controller):

Aluno(AlunoId id, Cpf cpf, String nome, boolean ativo)
Turma(TurmaId id, String nome, PeriodoLetivo periodo, int capacidadeMaxima)
AlunoId(UUID valor) — record com validação no construtor compacto
TurmaId(UUID valor) — record com validação
MatriculaId(UUID valor) — record
Cpf(String numero) — record com validação de dígito verificador
PeriodoLetivo(int ano, int semestre) — record
NomeDisciplina(String valor) — record com validação de não-branco

ATENÇÃO: Cpf valida dígito verificador — usar CPF do seed: "52998224725" como placeholder no Controller (CPF de Maria Silva do V2__seeds.sql). Não usar CPF inválido como "00000000000".

Padrão de construção Aluno/Turma no Controller (per DemoRunner.java, per PATTERNS.md):
  new Aluno(new AlunoId(UUID.fromString(request.alunoId())), new Cpf("52998224725"), "N/A", true)
  new Turma(new TurmaId(UUID.fromString(request.turmaId())), "N/A", periodo, 0)

Inferência de PeriodoLetivo a partir de LocalDate (per MatriculaRowMapper.java):
  new PeriodoLetivo(periodoInicio.getYear(), periodoInicio.getMonthValue() &lt;= 6 ? 1 : 2)

Nota: MatricularAlunoUseCase.executar() retorna MatriculaId, não MatriculaDto.
Para obter o MatriculaDto de retorno do POST /matriculas, o Controller deve:
  1. Chamar matricularUseCase.executar(command) → MatriculaId
  2. Chamar buscarMatriculaUseCase OU construir MatriculaDto diretamente com os dados disponíveis.
  Alternativa mais simples: o Controller retorna MatriculaDto com apenas matriculaId populado
  (alunoId=request.alunoId(), statusDescricao="ATIVA", totalDisciplinas=0, disciplinas=[]).
  Isso é aceitável para esta fase — o executor deve escolher a abordagem mais simples.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Implementar MatriculaController com os três endpoints REST</name>
  <files>
    erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/MatriculaController.java
  </files>
  <read_first>
    - erp-matricula-app/src/main/java/br/com/escola/matricula/infraestrutura/config/DemoRunner.java (padrão completo de construção de Aluno, Turma, PeriodoLetivo a partir de primitivos — reutilizar diretamente)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatricularAlunoUseCase.java (assinatura de executar() e tipo de retorno MatriculaId)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/AdicionarDisciplinaUseCase.java (assinatura de executar() e tipo de retorno MatriculaDto)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/CancelarMatriculaUseCase.java (assinatura de executar() e tipo de retorno MatriculaDto)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/aplicacao/MatriculaDto.java (estrutura atual incluindo ItemDto adicionado no Plano 01)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/vo/Cpf.java (verificar validação para usar CPF placeholder correto)
  </read_first>
  <action>
CRIAR erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/MatriculaController.java.

Package: br.com.escola.matricula.interfaces

Imports necessários: imports dos três UseCases e Commands do pacote aplicacao; imports de domínio (Aluno, Turma, AlunoId, TurmaId, MatriculaId, Cpf, NomeDisciplina, PeriodoLetivo) do pacote dominio; imports Spring MVC (RestController, RequestMapping, PostMapping, RequestBody, PathVariable, ResponseStatus, HttpStatus); import jakarta.validation.Valid; imports java (LocalDate, UUID, List).

Javadoc de classe (obrigatório — ponto pedagógico per D-06): "Fronteira HTTP do Bounded Context Matrícula. Traduz primitivos HTTP para objetos de domínio e delega para os UseCases correspondentes. Zero lógica de negócio — validações de regra de negócio pertencem ao Aggregate. Exceções de domínio propagam naturalmente ao ExcecaoHandler."

Anotações de classe: @RestController @RequestMapping("/matriculas")

Campos privados finais: matricularUseCase, adicionarUseCase, cancelarUseCase. Injeção por construtor único sem @Autowired (padrão do projeto).

RECORDS DE REQUEST (declarar como classes internas estáticas ou no mesmo arquivo — preferir arquivo separado NÃO, manter no mesmo arquivo como records internos para simplicidade):
  MatricularAlunoRequest: record com campos String alunoId (@NotNull), String turmaId (@NotNull), LocalDate periodoInicio (@NotNull), LocalDate periodoFim (@NotNull). Mensagem de validação em português em cada @NotNull.
  AdicionarDisciplinaRequest: record com campo String nome (@NotBlank, mensagem em português).

ENDPOINT POST /matriculas (método matricular):
  Anotações: @PostMapping @ResponseStatus(HttpStatus.CREATED)
  Parâmetros: @RequestBody @Valid MatricularAlunoRequest request
  Retorno: MatriculaDto
  Lógica de construção (per DemoRunner.java e PATTERNS.md):
    1. Construir PeriodoLetivo: new PeriodoLetivo(request.periodoInicio().getYear(), request.periodoInicio().getMonthValue() <= 6 ? 1 : 2)
    2. Construir Aluno: new Aluno(new AlunoId(UUID.fromString(request.alunoId())), new Cpf("52998224725"), "N/A", true) — Javadoc: "CPF placeholder — em produção viria do BC Aluno via API ou evento"
    3. Construir Turma: new Turma(new TurmaId(UUID.fromString(request.turmaId())), "N/A", periodo, 0)
    4. Construir MatricularAlunoCommand e chamar matricularUseCase.executar(command) → MatriculaId novaId
    5. Retornar new MatriculaDto(novaId.valor().toString(), request.alunoId(), "ATIVA", 0, List.of())
  Comentários inline numerados (// 1. Construir PeriodoLetivo..., // 2. Construir Aluno placeholder..., etc.)

ENDPOINT POST /matriculas/{id}/disciplinas (método adicionarDisciplina):
  Anotações: @PostMapping("/{id}/disciplinas")
  Parâmetros: @PathVariable UUID id, @RequestBody @Valid AdicionarDisciplinaRequest request
  Retorno: MatriculaDto
  Lógica: construir AdicionarDisciplinaCommand(new MatriculaId(id), new NomeDisciplina(request.nome())); chamar adicionarUseCase.executar(command); retornar o MatriculaDto resultado diretamente.

ENDPOINT POST /matriculas/{id}/cancelamento (método cancelar):
  Anotações: @PostMapping("/{id}/cancelamento")
  Parâmetros: @PathVariable UUID id, @RequestBody(required = false) Object corpoIgnorado — aceita corpo vazio (per D-08)
  Retorno: MatriculaDto
  Lógica: construir CancelarMatriculaCommand(new MatriculaId(id)); chamar cancelarUseCase.executar(command); retornar o MatriculaDto resultado diretamente.
  ALTERNATIVA mais simples para @RequestBody vazio: não declarar @RequestBody — Spring MVC aceita POST sem body se não houver @RequestBody.

ANTI-PATTERNS A EVITAR (per RESEARCH.md):
  - Nenhum try/catch no Controller — exceções propagam ao ExcecaoHandler
  - Nenhum if/else de validação de negócio — apenas @Valid de formato
  - Não retornar Matricula (Aggregate) diretamente — sempre MatriculaDto
  </action>
  <verify>
    <automated>grep -c "@PostMapping" erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/MatriculaController.java</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo MatriculaController.java existe no pacote br.com.escola.matricula.interfaces
    - Contém @RestController e @RequestMapping("/matriculas")
    - grep "@PostMapping" retorna 3 linhas (um por endpoint)
    - Contém MatricularAlunoRequest com @NotNull em alunoId, turmaId, periodoInicio, periodoFim
    - Contém AdicionarDisciplinaRequest com @NotBlank em nome
    - Não contém nenhum import de jakarta.persistence ou org.springframework.data
    - Não contém nenhum bloco try/catch (exceções propagam ao ExcecaoHandler)
    - Contém "new Cpf(\"52998224725\")" como CPF placeholder
    - O endpoint POST /matriculas retorna @ResponseStatus(HttpStatus.CREATED)
    - grep "import org.springframework.http.HttpStatus" MatriculaController.java retorna resultado (HttpStatus.CREATED é necessário)
  </acceptance_criteria>
  <done>MatriculaController.java existe com três endpoints delegando aos UseCases sem lógica de negócio</done>
</task>

<task type="auto">
  <name>Tarefa 2: Implementar ExcecaoHandler com mapeamento completo exceções de domínio → HTTP</name>
  <files>
    erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java
  </files>
  <read_first>
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/LimiteDisciplinasExcedidoException.java (campos getLimite(), getAtual() — usados no handler 422)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/AlunoInativoException.java (assinatura e getMessage())
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaNaoEncontradaException.java (assinatura e getMessage())
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/DisciplinaJaMatriculadaException.java (assinatura e getMessage())
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaCanceladaException.java (assinatura e getMessage())
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/PeriodoFechadoException.java (assinatura — mapear como 422)
    - erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/MatriculaDuplicadaException.java (assinatura — mapear como 409)
  </read_first>
  <action>
CRIAR erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java.

Package: br.com.escola.matricula.interfaces

Imports: imports de todas as exceções em dominio.excecao.*; imports Spring MVC (ControllerAdvice, ExceptionHandler, ResponseEntity); import org.springframework.http.HttpStatus; import org.springframework.web.bind.MethodArgumentNotValidException; import java.util.List; import java.util.stream.Collectors.

Javadoc de classe (OBRIGATÓRIO — ponto pedagógico central per D-06 e CONTEXT.md specifics):
"Único ponto do sistema onde exceções do pacote dominio.excecao.* se transformam em respostas HTTP.
O domínio nunca importa HttpStatus, ResponseEntity ou qualquer tipo jakarta.ws — essa responsabilidade pertence exclusivamente a esta classe.
Demonstra que a camada de domínio é independente do protocolo HTTP: o mesmo domínio poderia ser exposto via CLI, gRPC ou mensageria sem alterar nenhuma linha das classes de exceção."

Anotação de classe: @ControllerAdvice

RECORDS DE RESPOSTA DE ERRO (declarar como classes internas estáticas — padrão Java 21 record, sem Lombok, per PATTERNS.md):
  ErroResponse(String erro, String mensagem)
  ErroLimiteResponse(String erro, String mensagem, int limite, int atual)
  ErroCamposResponse(String erro, String mensagem, List&lt;CampoErro&gt; campos)
  CampoErro(String campo, String mensagem)

Campo erro em português conforme D-06: "erro" (não "error").

HANDLERS (mapeamento completo per D-07):

1. DisciplinaJaMatriculadaException → 409 Conflict
   @ExceptionHandler(DisciplinaJaMatriculadaException.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="DISCIPLINA_JA_MATRICULADA", mensagem=e.getMessage()

2. MatriculaDuplicadaException → 409 Conflict
   @ExceptionHandler(MatriculaDuplicadaException.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="MATRICULA_DUPLICADA", mensagem=e.getMessage()

3. MatriculaCanceladaException → 409 Conflict
   @ExceptionHandler(MatriculaCanceladaException.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="MATRICULA_CANCELADA", mensagem=e.getMessage()

4. LimiteDisciplinasExcedidoException → 422 Unprocessable Entity
   @ExceptionHandler(LimiteDisciplinasExcedidoException.class)
   ResponseEntity&lt;ErroLimiteResponse&gt; com erro="LIMITE_DISCIPLINAS_EXCEDIDO", mensagem=e.getMessage(), limite=e.getLimite(), atual=e.getAtual()

5. AlunoInativoException → 422 Unprocessable Entity
   @ExceptionHandler(AlunoInativoException.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="ALUNO_INATIVO", mensagem=e.getMessage()

6. PeriodoFechadoException → 422 Unprocessable Entity
   @ExceptionHandler(PeriodoFechadoException.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="PERIODO_FECHADO", mensagem=e.getMessage()

7. MatriculaNaoEncontradaException → 404 Not Found
   @ExceptionHandler(MatriculaNaoEncontradaException.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="MATRICULA_NAO_ENCONTRADA", mensagem=e.getMessage()

8. MethodArgumentNotValidException → 400 Bad Request
   @ExceptionHandler(MethodArgumentNotValidException.class)
   ResponseEntity&lt;ErroCamposResponse&gt;: iterar bindingResult.getFieldErrors() para construir List&lt;CampoErro&gt;
   com erro="DADOS_INVALIDOS", mensagem="Campos inválidos na requisição", campos=listaDeCampoErro
   Para cada FieldError: new CampoErro(fieldError.getField(), fieldError.getDefaultMessage())

9. Handler de fallback genérico: Exception.class → 500 Internal Server Error
   @ExceptionHandler(Exception.class)
   ResponseEntity&lt;ErroResponse&gt; com erro="ERRO_INTERNO", mensagem="Erro interno do servidor"
   Logar a exceção com log.error() para diagnóstico

Logger: private static final Logger log = LoggerFactory.getLogger(ExcecaoHandler.class) — padrão do projeto (per DemoRunner.java linha 50 e FinanceiroEventListener.java linha 33).
  </action>
  <verify>
    <automated>grep -c "@ExceptionHandler" erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo ExcecaoHandler.java existe no pacote br.com.escola.matricula.interfaces
    - Contém @ControllerAdvice
    - grep "@ExceptionHandler" retorna 9 linhas (8 mapeamentos específicos + 1 fallback genérico)
    - Contém handler para LimiteDisciplinasExcedidoException com e.getLimite() e e.getAtual()
    - Contém handler para MethodArgumentNotValidException construindo List&lt;CampoErro&gt;
    - Contém record interno ErroResponse com campos "erro" e "mensagem" (não "error")
    - NÃO contém import de jakarta.persistence ou org.springframework.data
    - Contém Javadoc de classe com menção explícita à independência do domínio de HTTP
    - grep "ERRO_INTERNO" ExcecaoHandler.java retorna resultado (handler de fallback presente)
  </acceptance_criteria>
  <done>ExcecaoHandler.java mapeia todas as exceções de domínio para respostas HTTP semânticas com formato JSON em português</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body | Dados não confiáveis do cliente cruzam para o domínio via Controller |
| Path parameter {id} | UUID fornecido pelo cliente — Spring MVC converte automaticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | MatriculaController — alunoId/turmaId como String UUID | mitigate | UUID.fromString() lança IllegalArgumentException → Spring MVC lança MethodArgumentTypeMismatchException para @PathVariable UUID; para campos String no body, usar @Pattern ou aceitar falha 400 via MethodArgumentNotValidException no ExcecaoHandler |
| T-04-04 | Denial of Service | POST /matriculas — criação irrestrita de matrículas | accept | Projeto didático sem auth; VerificadorElegibilidade limita duplicatas via índice único no banco; escopo v1 sem rate limiting |
| T-04-05 | Information Disclosure | ExcecaoHandler — stack trace em 500 | mitigate | Handler genérico Exception.class retorna mensagem genérica "Erro interno do servidor"; log.error() para diagnóstico interno — sem expor stack trace na resposta HTTP |
| T-04-06 | Tampering | Request body — injeção via campos de texto (disciplina nome) | mitigate | NomeDisciplina VO valida no construtor compacto; Bean Validation @NotBlank garante não-vazio antes de chegar ao domínio |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote de terceiros instalado neste plano; apenas módulos Spring Boot BOM |
</threat_model>

<verification>
Verificação de completude do Plano 02:
1. ls erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ — deve listar MatriculaController.java e ExcecaoHandler.java
2. grep "@RestController" erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/MatriculaController.java — deve retornar resultado
3. grep "@ControllerAdvice" erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java — deve retornar resultado
4. grep -c "@ExceptionHandler" erp-matricula-app/src/main/java/br/com/escola/matricula/interfaces/ExcecaoHandler.java — deve retornar 9
5. grep "import org.springframework.http\|import jakarta.ws" erp-matricula-app/src/main/java/br/com/escola/matricula/dominio/excecao/*.java — deve retornar vazio (domínio sem imports HTTP)
</verification>

<success_criteria>
- MatriculaController.java existe em interfaces/ com três @PostMapping (POST /matriculas, POST /matriculas/{id}/disciplinas, POST /matriculas/{id}/cancelamento)
- POST /matriculas retorna 201, os outros retornam 200
- Nenhum try/catch no Controller — exceções propagam
- ExcecaoHandler.java existe em interfaces/ com @ControllerAdvice e pelo menos 8 @ExceptionHandler
- Mapeamento correto: 409 para conflitos (Duplicada, JaMatriculada, Cancelada), 422 para violações de invariante (LimitesExcedido, AlunoInativo, PeriodoFechado), 404 para não encontrado
- LimiteDisciplinasExcedidoException retorna JSON com campos "limite" e "atual"
- MethodArgumentNotValidException retorna JSON com campo "campos" listando erros por campo
- Javadoc do ExcecaoHandler menciona explicitamente que é a única fronteira domínio → HTTP
</success_criteria>

<output>
Criar .planning/phases/04-interface-docker-e-material-didatico/04-02-SUMMARY.md quando concluído
</output>
